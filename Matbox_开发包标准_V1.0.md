# Matbox 开发包标准 V1.0

> ⚠️ **本版已于 2026-09-10 被《Matbox 开发包标准 V2.0》取代。**
>
> 留档不删，用途是**对照 V2.0 改了什么**（见 V2.0 §12）。
> 已知内伤：正文引用的 P29~P32 四个编号从未被定义、也没有任何程序实现；
> 开发前条数写 29，检查器实际跑 31。**不要再按本版执行。**
>
> 2026-09-10 又核出一条：§3 那段「实测数字」说**专项21b 缺 7 个章节**，
> 是**误报**。逐行看过它的标题，§13~§28 一节不缺；该文件最后一次改动是
> 2026-09-03，早于立标日，文件没变过。真正的原因是当时的判据**绑了排版**
> ——按「Webhook、回调及异步任务规范」这种带顿号的标题去找，而 21b 写的是
> 「Webhook/回调及异步任务规范」。换个分隔符就判成缺。
> 这跟后来 R7 把 20 份已指派的工作包判成 UNASSIGNED 是同一个根因。
> **误报比漏报更能骗人**：它让人回头去改本来没问题的东西。

**适用范围**：全部约 200 个功能包，无例外。
**立标日期**：2026-09-09
**来源**：从「代码持续质检与安全自检 Gate」（DQ）这个已经摊开做完的样板里**抽出来**的，不是另写一份。

---

## 0｜为什么有这份标准

2026-09-09 一天之内，撞出来的洞：

| 撞到的 | 性质 |
|---|---|
| 自动发布的 workflow 语法就是坏的，**从建立起一次都没跑起来过** | 推三次红三次，没人看红的原因 |
| 「10 轮 180 条断言全过」**早就是死的**，崩在第 1 轮 | 改了措辞之后没人再跑过 |
| 测试里 4 处「找不到就当没事」，**两边都没找到就判过** | 在测空气，还报绿 |
| Word 导出只增不删，撤掉的包**旧文件永远留着还会发到公开站** | 只增不删的生成器 |
| 站点悄悄旧了一整天，**没有任何东西提示** | 靠人记得 |

**这五个洞，补得都对，但没有一条是从标准推出来的 —— 全是撞上了才补。**

没有标准就只能靠撞。DQ 一个包撞了这么多次，**200 个包就是撞 200 遍**。

用户原话：「**是没有一个硬执行的标准**」。这份文件就是那个标准。

---

## 0.5｜验收权在谁手里（2026-09-09 用户定，位阶高于本文其余全部条款）

用户原话：

> **「你现在所有的标准都是自己制定自己的，你没有站在开发的交付审核。」**

这句话是对的，必须写在最前面：本文第 3~5 节那 41 条，**是我定的、我产的、我判的**，
三个角色都是同一个。「DQ 21/21 全满足」的真实含义只是
**「我按自己出的题给自己判了满分」**——那不叫验收，叫自评。

**所以验收权重新定义如下，本条与下列各条冲突时以本条为准：**

| | 谁说了算 |
|---|---|
| **最终验收标准** | **opencode 拿到材料能不能马上开发，无问题** |
| 本文第 3~5 节 42 条 | **只是代理指标** —— 猜「材料齐到什么程度它才能开工」 |

**代理指标全绿 ≠ 通过。** 全绿只意味着「可以送去试」。

**代理指标是可被推翻的**：opencode 每卡一次，就说明代理指标漏了一条 →
补进标准，并注明**是被哪一次实跑逼出来的**，不是谁拍脑袋加的。
今天已经证明过：我自己写的检查里有 5 处误报、3 处虚假的 ✅，
都是跑起来才发现的，不是想出来的。

### 0.5.1 四条硬性约定（用户 2026-09-09 定）

1. **每个开发包改完，必须 42 条全绿才算通过，并且要把「全是通过」摆出来给人看。**
   不接受「大致齐了」「主要的都有了」这类说法。
2. **最终验收以 opencode 能否马上开发为准**，本文的检查只是它的前置筛子。
3. **每个开发文档必须全部通过才算「准备就绪」**——不是 12 块里 10 块绿就叫 ready，
   **差一条就是没就绪**。
4. **就绪之后进对应模块的卡片弹窗**，与「代码持续质检与安全自检 Gate」现有弹窗同款，
   分 **开发前 / 开发中 / 运营中** 三段展示。

### 0.5.2 一条不许走的路

立标当天 37 条里有 10 条是⚪（标准已定、检查未建）；标准后扩到 41 条。同日建掉 M0 / M5 / D1 / D4，
**现在还剩 2 条**：M6（变异测试）、M7+D2（留一手跑的机制）——这两条都要真实 CI 环境才建得了。

**未建的检查不许在弹窗里显示成绿。** 只有两种合法做法：

- 把剩下的检查建出来，全绿才是真绿；
- 或者弹窗里如实写「N 条已守 / M 条未建」。

**把未建当成通过，就是本文第 1 节要拆掉的那种假绿。**

---

## 1｜铁律：机器判不了的，不算标准

> **一条要求，如果不能由程序判定「满足 / 不满足」，它就不是标准，只是建议。
> 建议会烂，而且烂了没有提示。**

- ❌「文档要写清楚」—— 判不了，不收
- ✅「验收标准必须有编号，且每个编号必须被某个功能认领，未认领的当场报错」—— 收

今天所有教训的共同点都是这一条：**能假绿的机制必须拆掉，靠人记得的环节必须去掉。**

所以本标准每一条都必须写满四样，缺一样这条就进不来：

| 字段 | 意思 |
|---|---|
| **要什么** | 这条要求的内容 |
| **怎么判** | 哪个程序、判什么、判据是什么 |
| **拦在哪** | 不满足时被谁拦下、拦在哪个环节 |
| **出处** | 从 DQ 的哪里抽出来的，或哪次事故催生的 |

> **格式细则**（2026-09-09 复查时补 —— 当时本文件自己违反了上面这条）：
> 「拦在哪」允许在**整节节首统一声明**，前提是该节所有条目确实由同一个程序拦。
> 逐条列一遍完全相同的值是噪音，但**整节不声明就是漏**。
> 复查发现五张表里有四张既没有列、节首也没声明 —— 已补齐。
> **一份自己都不守自己规矩的标准，没有资格要求别人守。**

---

## 2｜四个阶段

| 阶段 | 回答的问题 | 谁来判 |
|---|---|---|
| **开发前** | 这个包能不能开工 | `_check_ready_to_build.py` |
| **开发中** | 在建过程中不许发生什么 | CI，按 diff 判 |
| **开发后** | 这个包算不算做完 | CI，按测试结果判 |
| **运行中** | 上线以后跑得怎么样 | 运行指标（本标准不覆盖，样板已有） |

> ⚠️ **「开发后」此前无家可归。** 样板的「开发中」tab 实测 0 次功能编号、0 个状态徽章
> ——它是流程动画，不管进度；「运行中」是系统上线后的指标。
> **「这个包到底做完没做完」以前没有任何地方管。** 本标准第 4 节补上。

---

## 3｜开发前 · 29 条硬项

**全部 29 条都必须能机器判 —— 已逐条核过，无例外。**
（原为 24 条；2026-09-09 补上「产出类」P25~P28 —— 见 §3.5，此前整类漏掉。）

判据里的 `§N` 指专项报告的章节号。**27 份含 DDL 的专项报告用的是同一套章节结构**，
所以同一套判据对所有模块通用。

> **实测数字（2026-09-09，先写错过一次，这里是改正后的）**：
> 我起初写「19 个章节 27/27 全有」，跑了一遍才发现**不对** —— 14 个关键章节里有 7 个是 26/27。
> 缺的全集中在**同一份**：`专项21b 企业业务流配置AI员工`，它一份就缺 7 个章节
> （Webhook / 错误码 / 测试集 / 成本性能安全 / 开发阶段 / 风险 / 源码目录）。
> 其余 **26 份全部完整**。
>
> **这是本标准的第一个真实产出**：立标当天就点出一个过不了「开发前」的模块，
> 而且指明缺哪 7 节。这比「结构都一致」这句好听但不准的话有用得多。

### 3.1 源文档必须提供的（16 条，判据 = 该章节存在且非空）

| # | 要什么 | 怎么判 | 出处 |
|---|---|---|---|
| P1 | 技术栈定死（语言/框架/DB/迁移工具） | 源文档有技术栈章节且点名到具体版本 | DQ: FINAL §4.18.1 |
| P2 | 源码目录结构 + 依赖方向铁律 | 专项报告 §22 存在，含目录树与依赖方向声明 | DQ: 专项01 §22 |
| P3 | API 端点清单（方法/路径/幂等键/对应 Feature） | §13 存在，含 `\| GET\|POST\|PUT\|DELETE \|` 表 | DQ: 专项01 §13 |
| P4 | Request / Response Schema | §14 存在 | DQ: 专项01 §14 |
| P5 | OpenAPI 3.0 文档 | §15 存在 | DQ: 专项01 §15 |
| P6 | 鉴权 / 权限 / 多租户规范 | §16 存在 | DQ: 专项01 §16 |
| P7 | 数据库 DDL + Migration | §20 含 `CREATE TABLE` | DQ: 专项01 §20 |
| P8 | Provider Adapter 接口规范 | §17 存在 | DQ: 专项01 §17 |
| P9 | Webhook / 回调 / 异步任务规范 | §18 存在 | DQ: 专项01 §18 |
| P10 | 错误码 / 重试 / 超时 / 降级 | §19 存在 | DQ: 专项01 §19 |
| P11 | 测试集 / Golden Set / 验收脚本 | §23 存在 | DQ: 专项01 §23 |
| P12 | 前后端开发任务拆分 | §21 存在 | DQ: 专项01 §21 |
| P13 | 成本 / 性能 / 并发 / 安全要求 | §24 存在 | DQ: 专项01 §24 |
| P14 | 开发阶段 / 优先级 / 工作量估算 | §25 存在 | DQ: 专项01 §25 |
| P15 | 风险 / 阻塞项 / 备用方案 | §26 存在 | DQ: 专项01 §26 |
| P16 | 给未来 AI 开发者的完整执行说明 | §28 存在 | DQ: 专项01 §28 |

**拦在哪**：`_check_ready_to_build.py`，任一条不满足则该模块判为「不能开工」。

### 3.2 加工过程必须产出的（5 条）

**拦在哪**：`_check_ready_to_build.py`（P17/P19/P21）与 `_check_docs_consistency.py`（P18/P20）；任一条不满足 → 该模块判为「不能开工」。

| # | 要什么 | 怎么判 | 出处 |
|---|---|---|---|
| P17 | 七类施工面逐条核实 | 每个功能的七类施工面都有明确内容，无空缺 | RC5 硬条件；DQ: .docx §10 |
| P18 | 验收标准写成 Given-When-Then **且逐条有稳定编号** | 每条匹配 `<MOD>-T\d{3}`，编号连续无跳号 | DQ: .docx §13 → DQ-T001~024 |
| P19 | 允许 / 禁止修改的具体路径（可机检） | `<mod>_protected_scope.yml` 存在，且其中每个目录名都能在 §22 目录树里找到 | DQ: dq_protected_scope.yml |
| P20 | 工具版本锁到完整 40 位哈希 | 正则 `[0-9a-f]{40}`，不接受 tag 或短哈希 | DQ: .docx §15 第6条 |
| P21 | **验收标准 ↔ 功能的对应表存在，且不是单人推导** | 见 §3.4 —— 本标准新增，2026-09-09 挖出来的洞 | 今日事故 |

### 3.3 现实必须成立的（4 条 —— 文档说了不算，要真跑过）

**拦在哪**：`_check_ready_to_build.py` 的 R 系列，判据去问 git / CI / 真实仓库。

| # | 要什么 | 怎么判 | 出处 |
|---|---|---|---|
| P22 | 本地环境跑得起来（几条命令、多久） | **CI 真跑过一次**，不是纸面写了步骤 | 2026-09-09 由 CI 实测证实 |
| P23 | Implementer 已指派 | 不是 `UNASSIGNED`，是具体名字 | RC5 第19条 |
| P24 | 项目骨架已初始化，clone 下来能跑 | 代码仓库里真有该模块的落脚目录 | 2026-09-09 空 backend/ 事故 |
| P24b | **代码仓库 CI 是绿的** | 去问 GitHub 默认分支最近一次 run 的 conclusion，必须是 success | 2026-09-09 当天事故，见下 |

> **P24b 为什么是单独一条（当天真实发生）**：给全球多语言建完
> `backend/modules/localization` 骨架后，P24/R8「落脚目录都真实存在」当场转绿，
> 整套检查跑出 **9/9 全满足**——**而 CI 是红的**。
> 那两个测试是拷 dev-quality 的改包名生成的，引着 localization 根本没有的类，
> 8 处 `cannot find symbol`；修完编译又撞上 Flyway：两个模块共用一个库、
> 各有一个 `V1__`，历史表打架，`Validate failed`。
>
> **「目录建了」和「它编得过」是两件事。** P24 判前者，判不了后者。
> 只有 P24 而没有 P24b，就会出现「检查全绿、东西是坏的」——
> 正是本标准第 1 节要拆掉的假绿。

> **P22~P24 是「现实类」**：文档能自圆其说，现实不会。
> 判据必须去问真实世界（git / CI / 仓库），不许读我们自己写的文档。

### 3.4 P21 详解 —— 今天挖出来的那个洞

**问题**：源文档给了两张表（N 条验收标准、M 个功能），**但没给它们之间的连线**。
DQ 那 35 条连线是我一个人推的，工作包里自己写着「推导的，不是原文，Stage 10 必须回头确认」
—— 而「必须回头确认」**没有任何机制保证它真会发生**。

判错的后果：某个功能以为自己不用管某条标准 → **那条没人测，直接漏出去**。

**标准怎么定**（三选一，按优先级）：

1. **源文档直接带这一列**（最好）。验收标准表加一列「考哪个功能」。
   这一条从「判的」变成「抄的」，问题永久消失。**新写的专项报告必须这样。**
2. **从第三张表机械推**。若源文档有「组件目录 → 功能编号」表（DQ 的专项01 §22 有，13/13 全），
   且验收标准正文点名了组件，则可自动连线。
   **实测：DQ 用这个方法自动推对 13/24。**
3. **两方独立连线 + 比对**。实现方之外的第二方（不同模型/上下文）独立连一遍，
   **一致的放行，不一致的必须有人裁**。人只看分歧那几条。

**判据**：对应表必须标注自己是哪一种来源。**标 3 的必须附比对结果**；
标 2 的必须列出机器推不出来的那几条并说明怎么定的。**不允许出现无来源标注的对应表。**

### 3.5 产出类（4 条 —— 2026-09-09 补，此前整类漏掉）

**拦在哪**：`_check_docs_consistency.py`（P25/P26/P27）与 `_check_standard.py`（P28）。

| # | 要什么 | 怎么判 | 出处 |
|---|---|---|---|
| P25 | **工作包已生成**：每个功能一份可单独交付的文件 | `docs/工作包/<MOD>/` 下有 `<前缀>NNN.md`，份数 = 功能数 | DQ 有 13 份；全球多语言 0 份 |
| P26 | **存放处可下载**：模块页面列出全部工作包 + Word，可点开可下载 | 页面里出现「开发交接包存放处」与 `下载` 按钮，且份数与 P25 一致 | DQ 15 个下载按钮；全球多语言 0 个 |
| P27 | **流程图存在且已进内嵌副本** | `<mod>_flow.html` 存在，且在 `flow_embeds.js` 里（C1 判，**FAIL 不是警告**） | 今日事故 |
| P28 | **模块页面用的是样板本体做模板** | 页面的样式与脚本与 `quality_gate_flow.html` 逐字节一致 | 用户：颜色/字体/排版是标准的一部分 |

> **为什么这一整类此前是漏的（必须写下来）**
>
> 前 24 条是从 DQ 的**清单**里抽的，不是从 DQ 的**交付物**里抽的。
> 于是标准只管「源文档里有没有这些内容」，完全不管「最后有没有产出东西」。
>
> 后果当场就出现了：全球多语言按旧标准判 **21/21 全绿**，
> 而它的页面**没有存放处、没有一个下载按钮、没有流程图** ——
> **标准判绿了一个明显不完整的东西。**
>
> 用户原话：「最起码开发交接包存放处没有，是不是你的标准一开始定的就有问题」。
> 是。这是标准的漏洞，不是执行的漏洞。

### 3.7 模块页面文件包标准（2026-09-09 用户第 4 次指出后立的）

用户原话：

> **「你的全球语言包都没有代码持续质检的弹窗。上面是文件下载、施工面检查展、
> 开发资料标准、施工面验收标准。你理解的标准是随心所欲的乱放位置、
> 想丢内容就可以丢内容？」**

**是我丢了内容。** 把 DQ 样板逐块拆开之后，事实如下：DQ 的「开发前」有**三块**，
而我给全球多语言做的那一页**只有第一块**，外加一张我自己造的检查清单——
第二、三块整块没有。那不是"格式差异"，是把标准里的东西丢掉了。

**丢的原因不是源文档没有**：DEV.docx §6 里 20 个功能 × 7 类施工面 × 5 个字段
全写着，**是我没去取**。

#### 3.7.1 每个模块页面必须有的位置，顺序不许变

| # | 位置 | 里面必须装什么 | 内容从哪来 | 谁检查 |
|---|---|---|---|---|
| **A** | **开发交接包存放处** | **① 总账（完整开发文档）在最前**，②该模块全部工作包 + Word；都可点开可下载，工作包份数 = 功能数 | `modules.json` 的 `ledger` + `docs/工作包/<MOD>/` + `导出_Word/` | P25/P26/**C12** |

> **A 区为什么单独强调「总账在最前」**（2026-09-09 用户第 5 次指出）：
> DQ 的存放处第一行就是 147 KB 的完整开发文档，然后才是 13 份工作包，
> 状态行写「总账 1 份 + 工作包 14 份」。
> 而全球多语言那一版**总账整行没有**，用户问「开发文档下载去哪了」。
>
> 更要命的是我的检查也没抓到——它查了「存放处在不在、下载按钮几个」，
> **就是没查总账在不在**。原因：那些检查是照**我自己列的清单**写的，
> 不是照 DQ 那一块**实际长什么样**写的。
> **拿自己的理解替代标准，检查就会跟着一起错。**
> 所以 C12 现在直接比对 `modules.json` 里登记的 ledger 份数。
| **B** | **开发前资料摘要** | 12 块的状态徽章（已备/缺/未建），**不用点开也不用悬停就能看见** | `<mod>_ready_state.json` | P29 |
| **B2** | **⑥ 施工面检查展开** | **每个子功能 × 七类施工面逐条列出**，一个不落；每格含 Owner / 允许范围 / 禁止 / 契约依赖 / 验收证据 | 源文档的施工面章节（DQ: .docx §10；LOC: DEV.docx §6） | P30 |
| **C** | **开发资料 Ready-to-Build Package** | **12 张卡**，见 3.7.2 | 见 3.7.2 逐张的来源 | P31 |
| **D** | 开发中 | 该模块的流程演示（场景 / 阶段 / tab 切换） | `<mod>_flow.html` 的流程定义 | P27 |
| **E** | 运行中 | R 系列运行指标；**没有真实数据就写"待接入"，不许编数字** | 运行观测；无数据时如实标 | P32 |

**顺序不许变**：A（先拿东西）→ B（一眼看齐不齐）→ B2（逐条核实）→ C（完整资料）→ D → E。
乱序 = 取件的人得自己找，等于没定标准。

#### 3.7.2 C 区那 12 张卡，每张装什么

| 卡 | 标题 | 必须装 | 来源（LOC 的对应） |
|---|---|---|---|
| ① | 身份与状态 | DocID / 版本 / 状态 / FeatureID 范围 | `readiness.json` + `features.json` |
| ② | Scope In / Out | In Scope 每个子功能一句；Out of Scope 明确列 | DEV.docx §1.2 / §1.3 |
| ③ | Own / Reuse / Must Not Rebuild | 自研什么、复用什么、**禁止重造什么** | DEV.docx §3 |
| ④ | 依赖 & 冻结 Contract | 上游依赖 + 冻结契约字段 | DEV.docx §4.4 |
| ⑤ | 允许 / 禁止修改范围 | 具体路径；**给不出来就如实说给不出来并写明为什么** | `<mod>_protected_scope.yml` |
| ⑥ | Implementation Surface 施工面 | 七类逐项 Applicable / N/A + 负责人 | 同 B2 数据源 |
| ⑦ | **验收标准 Given-When-Then** | **全部**验收标准，G-W-T 句式 + 稳定编号 + 原文对照 | DEV.docx §9 → 40 条 AC-LOC |
| ⑧ | 存疑 / 待确认 | **不脑补，显式标注**：推导的、缺的、被阻塞的 | 归属表 derived 标记 + 源缺口 |
| ⑨ | 安全威胁检查 | STRIDE-lite 六类逐条 | DEV.docx §8 Permission/Action Contract |
| ⑩ | 责任角色 | Owner / Implementer / Reviewer / Approver | 施工面里的 Owner 字段 |
| ⑪ | 并行调度 | 谁能同时开工；**审核产能是真实上限** | `readiness.json` 的 parallel_readiness |
| ⑫ | 验收交付清单 | 通过时必须交出什么 | 补全 §28 |

#### 3.7.3 三条不许犯的（都是全球多语言这一轮真犯过的）

1. **不许用自己造的东西替换标准里的块。**
   我拿一张自己设计的「41 条检查清单」放进了 C 区的位置，把 12 张卡挤掉了。
   检查清单可以有，但它是**额外**的，不能顶替标准位置。
2. **不许把内容藏进 tooltip / 折叠。**
   第一版把条目名塞进 `title=` 悬停提示，直接违反「任何功能都不许藏在点击/悬停后面」。
3. **抽不到内容时不许留空，要显式标"缺"并写原因。**
   `_extract_loc_surfaces.py` 抽出 19/20，第 20 个（LOC-F020）源文档里根本没有——
   具名 + 写理由 + 页面上显示成缺。**留空 = 又丢一次内容，而且看不出来丢了。**

#### 3.7.4 后面 200 个包一律照此

新模块进来时，A~E 六个位置**逐个对**，缺一个就是没就绪。
**位置是固定的，内容由各自源文档填**——这就是"标准"和"随心所欲"的区别。

### 3.6 一条关于「内嵌副本」的说明（用户 2026-09-09 问）

> 「为什么是内嵌副本，不是直接内嵌？」

因为模块页面要能**双击打开**（`file://`）。那种情况下 `fetch`、`XHR`、
`<iframe src>` 全部被浏览器拦死，**只有 `<script src>` 还能加载**。
所以流程图内容被打包成 JSON 字符串放进 `flow_embeds.js`，再用 srcdoc 渲染。
这不是选出来的方案，是 file:// 只剩这一条路。

**但要说清楚**：「副本」和「直接内嵌」本质是同一件事，都是拷一份。
**拷贝会过期这个问题，不会因为放进同一个文件就消失。**
真正防过期的只有「每次自动重新生成 + 有检查拦住」：

- 生成端：`_build_flow_embeds.py` 已改为**扫目录自动发现**，不再手工登记
  （出事那次就是我加了文件没登记）
- 检查端：C1 **从警告升为 FAIL**
  （出事那次它报了却仍打印「✅ 硬性检查通过」——发现了没拦住，正是假绿）


---

## 4｜开发中 · 8 条红线

**判据全部按 diff / CI 判，不看任何一方的自述。**

依据：实测编码模型有 **12%** 的情况会主动破坏那些用来发现它自己行为不端的代码；
最强模型检测别的 agent 作弊，54 类手法只能查出 **63%**。**模型互审兜不住，确定性检查不能省。**

| # | 要什么 | 怎么判 | 拦在哪 | 状态 |
|---|---|---|---|---|
| M0 | 测试引用的编号必须真实存在 | HEAD 里每个 `<mod>T\d{3}_` 的编号都要在验收标准里查得到 | `_check_test_integrity.py` T0 | ✅ 已建 |
| M1 | 测试方法名带 TestID | 方法名匹配 `<mod>T\d{3}_`；`@Tag` 不算数（Surefire 不写进 XML） | C10 + CI | ✅ 已建 |
| M2 | 不许动不归自己的测试 | 比对方法**原文**，非归属的方法有任何改动即拦 | `_check_test_integrity.py` T1 | ✅ 已建 |
| M3 | 不许删掉或禁用已有测试 | base 有 head 无；或新增 `@Disabled`/`@Ignore` | 同上 T2 | ✅ 已建 |
| M4 | 不许把测试掏空留壳 | 同一方法断言条数变少 | 同上 T3 | ✅ 已建 |
| M5 | 改动不许越出允许范围 | `git diff --name-only` 比对 `<mod>_protected_scope.yml` 的 *root 通配；越界文件**逐个列出**不合并 | `_check_scope.py` | ✅ 已建 |
| M6 | 测试必须是真的（变异测试） | 把被测代码拿掉，测试必须失败 | **未建** | ⚪ 标准已定，检查待建 |
| M7 | 留一手：每个行为类别留 1 条，由验收方写 | 隐藏比例 30–60% 且每类都有；验收方 ≠ 实现方 | `_plan_holdout.py` 已算出选题，**跑的机制未建** | ⚪ 部分 |

> **⚪ 的两条（M6/M7）是诚实标注，不是拖延**。标准定了但检查没建 = 这条现在拦不住任何人。
> 未建的检查**不得计入「已满足」**，否则就是本标准第 1 节要拆掉的那种假绿。

### 4.1 M7 补充：为什么第二个 AI 就够，不必卡人工

> **同一个 AI 写代码又写测试，你拿到的不是测试，是一面镜子。**

成熟做法是 Coordinator–Implementor–Verifier：一个 agent 照契约写实现，
**另一个 agent 照同一份契约写测试、且不看实现**。
**独立性来自它读的是什么，不是它是谁。**

跟「让模型当裁判审另一个模型」要分开：那条只能查出 63%。
**这里模型只负责写测试，判定是测试跑出来的，是确定的。**

留一手留的是**对答案的机会**，不是题目本身：实现方看得到全部验收标准（它得照着建），
但拿不到验收方那份测试，也没有反复试的机会。

---

## 5｜开发后 · 5 条完工判据

**「做完了」的唯一定义：下面 5 条全绿。任何一方说「做完了」都不作数。**

| # | 要什么 | 怎么判 | 拦在哪 | 状态 |
|---|---|---|---|---|
| D1 | 认领的每条验收标准都有对应测试**且通过** | 读 surefire XML；**失败、跳过(skipped)都不算通过；没有报告也不算通过** | `_check_delivery.py` | ✅ 已建 |
| D2 | 留一手那批也通过 | 验收方的测试在 Gate 时跑一次，全绿 | Gate（待建） | ⚪ 待建 |
| D3 | 无孤儿测试 | 编号必须**真实存在**（T0）+ 必须**归自己**（T1）。T0 扫的是 HEAD 里全部测试文件，不只是这次改动的，所以「验收标准被撤销、旧测试变孤儿」也能查到 | CI | ✅ 已建 |
| D4 | 无覆盖缺口 | 认领的编号必须全部出现在测试里，一条不缺，缺的逐条列出 | `_check_delivery.py` | ✅ 已建 |
| D5 | 三 Gate 全 PASS | 代码质检 / 安全质检 / 自动化测试三关 | DQ 本身 | 由 DQ 提供 |

**D1 + D4 是双向的**（DO-178C 的正反向追溯）：
正向查「有标准没测试」= 覆盖缺口；反向查「有测试没标准」= 孤儿测试。**两边都要查。**

---

## 6｜这份标准自己怎么不烂

标准本身也会过期 —— 今天「10 轮 180 条全过」就是这么死的。所以：

1. **每一条都必须指向一个真实存在的检查程序**。指向不存在的程序 = 这条是⚪，必须如实标。
2. **⚪ 不得计入满足**。任何汇总数字必须把⚪单列，不许混进「已满足」。
3. **判据变了要同步**。生成器改了措辞而检查器没跟上，就是今天那个死掉的测试。
   → 检查器抠不到值时必须**当场停并点名哪条正则过期**，绝不许退化成默认值继续跑。
4. **新增标准必须附负向验证**：故意违反一次，确认真的被拦下。**没做过负向验证的检查不算数。**

---

## 7｜与现有文件的对应

| 本标准 | 落在哪 |
|---|---|
| 开发前 P1~P24 | `<mod>_ready_state.json` + `_check_ready_to_build.py` |
| 开发中 M0~M4 | `_check_test_integrity.py`（T0~T3）+ `_check_docs_consistency.py` C10 |
| 开发中 M5 | `_check_scope.py` |
| 开发中 M6（变异测试）| **无对应程序 —— 未建** |
| 开发中 M7 选题 | `_plan_holdout.py` → `<mod>_holdout.json`（跑的机制未建）|
| 开发后 D3 | `_check_test_integrity.py`（T0+T1）|
| 开发后 D1/D4 | `_check_delivery.py` |
| 开发后 D2（留一手跑的机制）| **无对应程序 —— 未建** |
| 开发后 D5 | 由 DQ 自身的三 Gate 提供 |
| 全链自检 | `_check_docs_consistency.py` C1~C14 |
| **上面每一条的负向验证** | `_negtest_devphase.py`（39 条，覆盖登记表里全部模块）|
| 全链接线（登记了就必须接进来）| `_build_all.py` 的 `module_steps()` guard |
| 哨兵模块归属表 | `_build_sentinel_ownership.py` |
| 公开站按模块隔离 / 不公开模块不上站 | `_build_public_export.py` 的 `is_public()` |
| 取件页每条链接都能下载 | `_build_public_export.py` 取件页链接自检 |

---

**出处**：本标准全部条目抽自 DQ 已完成的样板与 2026-09-09 当天的实测；
每条的来源列已标注。凡标「今日事故」的，是撞出来的，不是设计出来的 ——
**那正是立这份标准的理由。**


---

## 8｜第二个模块规则（2026-09-10 立，直接原因是当天的 7 个洞）

### 8.1 先说事实

假装成 opencode，拿全球多语言的 LOC-F001 从头走一遍真实流程，
在**已经全绿**的仓库里连撞出 7 处，全是同一类错：

| # | 出事的地方 | 写死了什么 | 后果 |
|---|---|---|---|
| 1 | `_check_test_integrity.py` | `dqT(\d{3})_` / `dq_test_ownership.json` | 开发中三条红线对第二个模块跑不起来 |
| 2 | `_check_delivery.py` | `"DQ-T"` 前缀 / 同上 | D1/D4 跑不起来；串模块时把 `locT001_` 记成 `DQ-T001` **发了一条假绿** |
| 3 | `_build_all.py` | 整张步骤表只有 DQ | 改了 LOC 的源头重跑全链，**LOC 不会重算**，下游照旧渲染旧内容，全绿 |
| 4 | `_negtest_devphase.py` 之前不存在 | —— | 这两个检查器从来只被正向跑过 |
| 5 | `_build_public_export.py` 的 `MODULES` | 登记表的第二份手抄件，只抄了 DQ | 站上只有 1 个取件页；被指派 LOC 的 AI 打开网站找不到自己那一份 |
| 6 | 同上，工作包目录扫描 | 不认 `public: false` | 哨兵模块的占位工作包**被发上了公开站** |
| 7 | 同上，`^F-[A-Z]+-\d{3}\.md$` | 写死编号**形状**（LOC 是 `LOC-F001`）| 取件页渲染出一张只有表头的空表，页面还是绿的 |

第 8 处是 EXTRA 里写死 DQ 的三份材料，导致 LOC 取件页的边界文件 404。

### 8.2 这一类错为什么特别难发现

它们坏掉的方式不是报错，是**安静地什么都不做，然后发绿灯**。
DQ 有 13 个包次次全绿，恰好把「它只认一个模块」盖得严严实实。
正向跑一万遍都是绿的 —— 因为正向跑的一直是同一个模块。

### 8.3 三条硬规则

1. **任何检查器 / 生成器，必须在「不是 DQ 的模块」上跑通过一次才算建好。**
   只在 DQ 上验过的，一律记⚪，不许记✅。

2. **每条检查必须有负向验证，且负向验证要覆盖登记表里的每一个模块。**
   §6 第 4 条只要求"故意违反一次"，那不够 —— 违反的如果一直是 DQ，
   照样发现不了单模块盲区。现在由 `_negtest_devphase.py` 按登记表逐模块跑。

3. **模块登记表 `docs/modules.json` 是唯一事实源，任何脚本不许再抄一份。**
   需要的字段就往登记表里加，不要在脚本里建第二个字典。
   已经因为"抄一份"栽过的：公开导出的 `MODULES`、`_build_all.py` 的步骤表。

### 8.4 哨兵模块（ZZ）为什么必须常驻、且必须参加考试

ZZ 不是真模块，它的工作包内容全是占位，唯一价值是**存在**：
任何"只认 DQ / 只认 LOC"的写死，都会在第三个模块上露出来。

2026-09-10 发现哨兵自己缺席了 —— 它没有归属表，反向测试遇到它直接跳过，
三个模块只考了两个。**一个不参加考试的哨兵等于没有哨兵。**
现在由 `_build_sentinel_ownership.py` 从它的工作包正文导出归属表，
`_negtest_devphase.py` 把它当第三个模块一起考。

ZZ 在登记表里 `public: false`，不上公开站；这一条本身也是被撞出来的
（见 8.1 第 6 行），现在由导出脚本的 `is_public()` 统一判。

### 8.5 一条给"页面是绿的"的额外规则

**页面渲染成功 ≠ 页面有用。** 当天两次都栽在这上面：
取件页表格只有表头也是绿的，取件页链接 404 也是绿的。
所以凡是"给人下载/取件"的页面，必须额外查两件事：

- 该列的东西**列出来了几条**（0 条要当故障，不是当空）
- 页面上每一条链接**都真能打开**

落在 `_build_public_export.py` 的空表拦截 + 取件页链接自检。
