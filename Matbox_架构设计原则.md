# Matbox 架构设计原则

> 已确认的顶层架构前提，指导后续所有技术选型和模块设计。持续更新，不批量攒批，每次确认立即记录。

## 核心前提（2026-08-29 确认）

Matbox 不按普通 SaaS 或传统 AI 工具的方式开发，必须从第一天起按 **"未来 10 年可持续演进的 AI-native、Agent-native 企业级平台"** 进行架构设计。

### 1. 持续跟踪行业前沿

架构设计必须持续跟踪并吸收 OpenAI、Google、Anthropic、Meta、微软、字节、阿里、百度等领先大模型公司的最新技术路线，包括：
- 多模态模型
- 推理模型
- Agent
- 工具调用
- MCP
- 工作流编排
- Computer Use
- 实时语音
- 视频生成
- 长期记忆
- 知识库
- 模型路由
- AI 安全治理

### 2. 不绑定单一供应商/模型

Matbox 的核心不能绑定某一家模型或某一个供应商。必须建设：

- 统一模型接入层
- Provider Router
- 能力抽象层
- Agent Runtime
- 工作流引擎
- 记忆系统
- 知识与资产系统
- 权限与租户体系
- 可观测性
- 成本控制
- 持续质量安全 Gate

使平台能够随时接入新模型、新 API 和新技术，快速替换落后方案，**不需要推翻整体架构**。

**Provider Router 的具体机制：基于 MCP 协议设计（2026-08-29 确认）**——查证确认，AI Agent 连接外部工具/数据源（数据库、CRM、Slack等）这件事，2026年行业已经统一到 Model Context Protocol（MCP，Anthropic 2024年提出并开源，现已是跨模型通用标准）。Provider Router（目前⚪NOT_STARTED，尚未设计）正式设计时，AI员工对接外部工具/数据源的部分应直接基于 MCP 协议，不要自造一套连接协议——这样以后换底层大模型或换某个具体方案，只要新方案支持MCP（或者做一层MCP适配），上层AI员工对接外部工具的逻辑不用重写，真正做到"换模型不影响整体"。用户已确认：这一层的设计前提就是"底层一定会换模型或者换方案，不能让这个动作影响整体"。

### 3. 技术选型标准

所有技术选型必须基于以下维度判断，不能只因为某个模型当前效果好就直接绑定：
- 全球最新资料
- 真实产品采用情况
- 源码和 API 能力
- 更新活跃度
- 稳定性
- 成本
- License
- 隐私安全
- 迁移难度

### 4. 架构必须支持的能力

- 多模型协同
- AI 员工自主执行
- 人在环审批
- 任务分解
- 长期记忆
- 跨端运行
- 企业级多租户
- 数据隔离
- 审计追踪
- 版本与 Baseline 管理
- 开发完成后的持续代码质检、安全检查、自动测试、发布后监控和模型升级回归

### 5. 目标澄清

目标不是声称"保证 10 年不变"，而是让 Matbox 具备**10 年持续升级和保持竞争力的架构能力**：
- 底层模型可以更换
- 能力可以插拔
- 数据和工作流可以继承
- 核心业务不被单一技术锁死

## 补充原则（陆续确认，逐条记录）

### 6. 内部持续评测框架（2026-08-29 确认，已定为架构原则）

用 Matbox 自己的真实任务（产品匹配准确率、内容生成质量等）建立基准测试集，每次有新模型发布，自动跑一遍打分，作为"要不要换模型"的依据，不靠主观判断或外部榜单。

### 7. 知识库原始数据必须完整保留（2026-08-29 确认，已定为架构原则）

换 Embedding 模型时，旧向量无法直接转换成新向量，只能用新模型把原始数据重新跑一遍生成新索引（行业公认做法，2026 年业界共识）。因此：

- **知识库必须完整保留原始数据（转成向量之前的那份），不能只存向量本身**
- 原始数据存放在对象存储或主数据库，作为可随时重新生成索引的"源头"
- 这是免费的保险——不这么做，一旦丢了原始数据，以后永远无法升级 Embedding 模型
- 具体迁移机制（蓝绿部署、双索引灰度切换等）留到开发知识库模块时再细化，不影响现在的原则

### 8. 按模型独立维护 Prompt / Agent 配置（2026-08-29 确认，已定为架构原则）

不同模型对同一提示词的响应存在差异，需要为每个 Provider 单独维护一套 prompt / 参数配置档案（系统提示词、few-shot 示例、推理参数、工具定义等），效果通过第 6 点的评测框架验证。这是 2026 年生产级 Prompt 管理平台的标配做法（支持同一 Prompt 跨模型对比测试、分别调优、分别管理版本），不是所有模型共用一套 prompt。

### 9. Provider Router 支持故障转移 + 分级路由（2026-08-29 确认，已定为架构原则）

- **故障转移**：某个供应商 API 故障、限流或超时时，自动切换到备用供应商/模型（fallback chain），无需人工介入。
- **分级路由**：按任务复杂度、延迟要求、成本约束实时选择模型——简单任务用低成本模型，复杂任务用高性能模型，把"选哪个模型"当成运行时决策，而不是写死在代码里。

2026 年业界共识：这是"每个正经 AI 产品"的标配能力（Bifrost、Vercel AI Gateway、OpenRouter 等主流网关均支持），不做属于落后架构。

## 外部文档审核记录

> 用户在本地保存了多份 Codex 时期产出的正式开发文档（位于 `（本机路径已隐去））。逐份审核后，把审核中确认的补充要求记录于此。

### 审核对象：Matbox_代码持续质检与安全自检_正式开发文档_V1.1-RC（2026-08-19）

文档核心内容：为 Matbox 建立"代码质检 Gate → 安全质检 Gate → 自动测试 Gate"三级强制流水线，基于5个锁定版本的开源工具（SonarQube Community Build、OpenGrep、Betterleaks、Trivy、OPA），fail-closed，不合格自动返修重验，合并后再验。已定义 13 个功能模块（F-DQ-001～F-DQ-013）。状态：Stage 9 内容级审核 PASS，Stage 10（绑定真实 Repo）待启动——本仓库今天已建好真实 GitHub Repo，具备启动条件。

**审核发现（2026-08-29）：**

工具选型经核实仍是 2026 年当前最优选择，未过时（Betterleaks 是 Gitleaks 原作者做的下一代产品，检测准确率 98.6% vs Gitleaks 70.4%，非落后选择）。

文档存在两个缺口，正在逐条确认是否补充为必须要求：

### 10. AI 员工执行过程安全防护（2026-08-29 确认，已定为必须补充要求；**状态：卡在依赖上，非遗漏未做**）

原文档只扫描"代码产出结果"（SAST/密钥/依赖），未覆盖"AI 员工（Codex等）执行任务过程中被提示词注入、越权操作、数据泄露"类风险。2026年研究显示 26.1% 的 AI Agent 技能/工具存在此类漏洞。**必须在代码持续质检文档基础上，补充 AI 员工执行过程的安全防护要求**（防提示词注入、越权操作监控、执行环境隔离等），与 Agent Runtime 模块设计时一并落地。

**2026-08-29 复查结论**：回查原质检文档原文，F-DQ-009（自动修复控制器）的 Async/Runtime 依赖行已明确写"Shared Agent Runtime"（占位，未设计）。这条要求本来就明说了要等 Agent Runtime 模块设计时才能落地，而 Agent Runtime 目前是 ⚪NOT_STARTED。现在没有具体设计内容可写进质检文档，硬写只会是一句空话，不符合"精确到能直接写代码"的标准。**结论：不是遗漏，是真实阻塞在 Agent Runtime 模块尚未设计——留待该模块设计时一并落地，不提前塞空话进质检文档。**

### 11. 构建溯源签名 SLSA Provenance（2026-08-29 确认，已定为必须补充要求；**状态：已解决**）

原文档已要求 Trivy 生成 SBOM（软件物料清单），但未要求对每次构建产物做加密签名和可追溯证明（SLSA Provenance：记录构建产物的源仓库、commit、依赖、构建环境，并密码学签名，防止供应链被篡改）。这是 2026 年供应链安全的新标配，**必须补充为 ReleaseArtifact 环节的硬性要求**，目标至少达到 SLSA Level 2。

**2026-08-29 复查结论**：回查原质检文档原文，F-DQ-001 的 Own/Reuse 表早已把"provenance"列为 REUSE 项，明确指向"F-RELEASE-001"这个占位模块名。现在 F-RELEASE-001 正式文档已建成（见第16条），其中 REL-F003·SLSA构建溯源签名 正好落地这条要求（目标Level 2，`provenanceRef`字段，P0规则"任一ReleaseArtifact可验证其Provenance签名有效"）。**结论：原文档预留的空位已被填上，不需要再改质检文档本体。**

### 12. 独立审核角色安排（2026-08-29 确认）

原文档要求"修复者不能自我批准""每次变更需独立 Reviewer"。团队实际情况：主要由用户本人（非技术背景）+ AI 完成开发，另有一位懂开发的同事参与，但参与不多。

**确定方案：** 日常大部分变更靠自动化工具（SonarQube/OpenGrep/Betterleaks/Trivy/OPA）的硬性 Gate 判定，不需要人工判断"过不过"，天然满足"不能自我批准"。**独立 Reviewer / Source Reviewer / Acceptance Owner 等需要人工判断的角色，由这位同事承担**，只在真正需要人工判断的关键节点介入（高风险修改、BLOCKED_MANUAL、Suppression/Waiver 审批等），尽量不打扰。

### 13. 采用 RC5 多 AI 并行开发管理标准（2026-08-29 确认）

`RC5更新版-Matbox_新专项开发_全流程AI对话脚本与开发文档管理标准` 文档（Codex 时期产出，2026-08-18，存于 `（本机路径已隐去））已完整定义 Matbox 的开发文档管理标准：FeatureID/WorkPackageID/BaselineID/ReleaseArtifactID 编号体系、Feature Register/Change Register/Evidence Register/Gate Register 四类事实源、Git Worktree 隔离、角色分离（Feature Owner/Implementer/Source Reviewer/Acceptance Owner/Release Owner）、七类 Implementation Surface 检查清单（Frontend/Backend/Data/Async/Provider/Infra/Tests）、多 AI 并行调度冲突检测规则。

**审核结论：** 核心机制（Git Worktree 隔离、规范先行、角色分离、派发前冲突检测）经核实仍是 2026 年"规范驱动开发（Spec-Driven Development）"的主流做法，与 GitHub Spec Kit、OpenSpec、BMAD-METHOD 等现成开源工具解决的是同一类问题。**决定：不切换现成工具，继续采用 RC5 这套定制流程**（已经很详细、且是照 Matbox 实际情况定制的，换工具需要重新适配，不划算）。

**已知缺口：** RC5 文档自述"Canonical Registry Location（规范数据实际存放位置）尚未选定冻结，本标准目前只能算 RC，不能算 V1"——这是下一步要解决的具体问题，见待办。

### 14. F-CRED-001 密钥管理选型（2026-08-29 确认）

三个丢失地基模块（F-OBS-001 / F-RELEASE-001 / F-CRED-001）中，**先做 F-CRED-001**，因为 F-DQ-003/005/012 三个质检模块直接卡在它上面，解开后能最快让这几个模块从"MISSING阻塞"变成"可以开工"。

技术选型：查证后放弃 HashiCorp Vault（2023年从真开源 MPL 2.0 改成半开源 BSL 1.1，与"不绑定单一供应商/协议"原则冲突）。**确定使用 Infisical**（MIT 协议，真开源，小团队友好，不需要专职平台工程师）。参考 OpenBao（Linux基金会治理的 Vault 开源分支）的动态凭据机制作为未来规模变大时的设计参考。

**必须补充的设计要求：** 2026 年该领域最大的新兴风险是"非人类身份"（CI/CD机器人、AI Agent）产生的凭据管理——Matbox 未来要给几十个 AI 员工分别发凭据，F-CRED-001 从设计第一天起必须把"AI 员工"当作和"人类用户"同等重要的凭据使用者来设计，不能只考虑人类密码场景。

### 15. F-OBS-001 证据审计与AI员工监控选型（2026-08-29 确认）

第二个丢失地基模块。区分两件不同的事：证据类记录（GateDecision等）要"防篡改"，AI员工行为要"实时监控+异常报警"，两者不能混为一谈、不能共用同一套清理策略。

**关键依据：团队只有2个人，没精力人工盯着每个AI员工**——查证后确认这正是"更需要自动化监控"的理由，不是"可以不做"的理由，AI员工会以人不易察觉的方式出错（死循环烧token、编造信息、悄悄跳步骤、效果漂移），传统监控抓不住。

**技术选型：**
- AI员工行为监控：**OpenTelemetry**（Claude Agent SDK 2026年已官方原生支持，零改造成本，且是厂商中立标准，换AI供应商监控体系不用重做）+ **SigNoz**（自建开源看板）
- 防篡改证据记录：**immudb**（开源，密码学可验证，篡改可检测）

告警原则：只发"团队真能处理"的高信号告警，避免狼来了效应。

拆出 OBS-F001~F005，正式文档见 [Matbox_证据审计与监控_正式开发文档_V1.0-RC.md](Matbox_证据审计与监控_正式开发文档_V1.0-RC.md)。

### 16. F-RELEASE-001 发布与基线系统选型（2026-08-29 确认）

第三个、也是最后一个丢失地基模块。**技术选型：** 自建制品仓库用 **Harbor**（开源，CNCF毕业项目，带漏洞扫描/权限管理）；SBOM **不新增工具，直接复用 F-DQ-006（Trivy）已有产出**，避免重复造轮子；构建溯源签名延续架构原则第11条已定的 SLSA Level 2 目标，通过 CI 原生能力生成（具体CI平台待定）。版本编号用 Git Commit SHA 做内部溯源，对外发布叠加语义化版本号，与 RC5 的 BaselineID/ReleaseArtifactID 体系兼容。

拆出 REL-F001~F005，正式文档见 [Matbox_发布与基线系统_正式开发文档_V1.0-RC.md](Matbox_发布与基线系统_正式开发文档_V1.0-RC.md)。

**至此，三个丢失的地基模块（F-CRED-001、F-OBS-001、F-RELEASE-001）全部完成正式开发文档重建，13个质检模块的依赖阻塞全部解开（文档层面）。下一步是 Stage 10：确定 CI 平台，真实部署这些工具。**

## Stage 10 · CI 自动化流水线（独立于上述三份文档，新增）

> 这条不属于密钥/证据审计/发布系统任何一份文档，是新的一条：管"代码质检文档"里那套 Gate 流程实际在哪运行、AI 员工怎么被安全地自动触发。

### 17. CI 平台与 AI 员工自动触发安全（2026-08-29 确认）

**CI 平台：确定使用 GitHub Actions。** 理由：仓库已在 GitHub、团队2人无精力自建维护CI服务器、免费额度对当前规模够用。已核实这个选择不锁定单一AI供应商——Claude、Codex、Gemini 都各自有官方/社区支持的方式接入 GitHub Actions（headless/exec模式），换AI不用换CI平台。

**查证到的真实问题（不是凭空担心）：**
- 2026年真实发生过安全事故：Claude Code GitHub Action 曾有漏洞，触发权限检查有漏洞，任何人注册机器人账号冒充身份就能劫持仓库（官方已修复，但说明这类"AI自动触发"配置必须谨慎，不能用默认宽松设置）。
- Codex CLI 目前在"无人值守自动化"场景下还不够成熟：缺少官方SDK/非交互认证、长任务失败后无法恢复、没有可靠的完成信号，1000+真实用户反馈证实这是普遍痛点，不是个例。

**确定方案：**
1. **无人值守的自动化 CI 触发，现阶段用 Claude**，Codex 继续用于日常人在场的交互式开发，不是替换，是分工——不同场景用不同AI，符合"多模型协同"原则，不冲突。以后 Codex 自动化工具成熟了再接入 CI 环节。
2. **AI 自动触发的权限必须严格限定**：谁能通过评论/PR触发 AI 干活、AI 能拿到什么权限的 Token，都要按最小权限配置，不用默认的宽松设置，这是从真实安全事故里得出的硬性要求，纳入架构原则第10条"AI员工执行安全防护"的具体落地细则。

## 自我复查发现的新缺口（2026-08-29，非用户提出）

> 用户指出"手上的事没处理完"后，重新审视架构原则+RC5文档+质检文档，自己找出以下之前漏掉、从未登记为"缺失"的平台核心共享能力：F-COST-001（成本控制）、F-TENANT-001（租户/权限）、F-QUEUE-001（任务队列）、F-STORAGE-001（对象存储）。确认按此顺序逐个补做正式文档。

### 18. F-COST-001 成本控制选型（2026-08-29 确认）

**关键决策：不与 Provider Router 合并成一个模块。** 起初考虑用 LiteLLM 一个工具同时做模型路由+成本控制，深入查证后否决——LiteLLM 开源版的团队级预算治理功能被阉割（企业版才有），且自建需要额外部署 Redis+PostgreSQL，真实案例显示中低请求量下总拥有成本反而更高。**结论：两者功能定位不同、维护代价不同，合并只会增加负担，各自独立设计。**（这次用户本人倾向"独立"，查证后证实用户判断正确，不是盲从）

技术选型：**Langfuse**（开源，成本记录嵌在调用链路里，精确到每个AI员工每一步，多智能体场景友好）。预算告警用"软性配额提醒"（80%/100%通知），不做硬性断电式阻断，避免任务跑到一半被打断。

拆出 COST-F001~F004，正式文档见 [Matbox_成本控制_正式开发文档_V1.0-RC.md](Matbox_成本控制_正式开发文档_V1.0-RC.md)。

### 19. F-TENANT-001 租户与权限体系选型（2026-08-29 确认）

**关键决策：复用已在F-DQ-008采用的 OPA 策略引擎，不引入第二套权限系统。** 查证确认 OPA 正是2026年细粒度策略判断的主流选择，尤其适合AI员工场景——传统角色权限给每个AI员工/每个仓库分别建角色会"角色爆炸"，OPA的策略式判断更合适。

**人类用户**：团队规模小，简单RBAC（角色权限）已够用，不上复杂的细粒度授权系统（对小团队是负担不是帮助）。

**AI员工**：延续F-CRED-001已定的"零权限起步、按任务临时授权、任务结束自动收回"原则，通过OPA落地。

**租户隔离**：TenantID嵌入登录令牌，每次请求校验；数据库用"共享表+租户ID字段"模式起步，不需要一开始就上独立数据库。这是 RC5 明确的头号不可降级P0风险，优先级最高。

拆出 TENANT-F001~F005，正式文档见 [Matbox_租户与权限体系_正式开发文档_V1.0-RC.md](Matbox_租户与权限体系_正式开发文档_V1.0-RC.md)。

### 20. F-QUEUE-001 任务队列选型（2026-08-29 确认）

技术选型：**Hatchet**（开源，基于Postgres的持久化工作流引擎，比Temporal轻量、比Celery多了失败可恢复能力，自带并发控制和看板）。

**查证到的真实依据**：AI员工数量越多，互相冲突概率是平方级增长（10个员工的冲突组合数是5个员工的4.5倍）——验证了RC5"派发前必须做冲突检查"规则的必要性；API限流是集体性的，需要队列层统一管理AI员工整体并发数。

拆出 QUEUE-F001~F004，正式文档见 [Matbox_任务队列_正式开发文档_V1.0-RC.md](Matbox_任务队列_正式开发文档_V1.0-RC.md)。

### 21. F-STORAGE-001 对象存储选型（2026-08-29 确认）

**再次验证"不能凭知名度选工具，必须查License"的教训**：默认会想到的 MinIO，查证后发现和 HashiCorp Vault 是同一剧本——2021年协议从 Apache 2.0 改成限制更严的 AGPLv3，2026年上半年开源版仓库直接冻结停止维护，逼迫转商业版。

**确定使用 SeaweedFS**（Apache 2.0真开源，12年历史，Kubeflow等项目采用，擅长存海量小文件）。放弃 Garage（虽然也是自建友好方案，但AGPL-3.0协议对Matbox这种不开源自身代码的商业产品，法律复杂度和MinIO一样）。

拆出 STORAGE-F001~F003，正式文档见 [Matbox_对象存储_正式开发文档_V1.0-RC.md](Matbox_对象存储_正式开发文档_V1.0-RC.md)。

**至此，本次自我复查发现的4个缺口（F-COST-001、F-TENANT-001、F-QUEUE-001、F-STORAGE-001）全部完成正式开发文档。**

## 外部文档审核记录（续）

### 审核对象：Matbox_代码问题定位与人工修复台_独立专项开发文档_V1.0（2026-08-29）

用户提供的新文档（Codex独立开发包，来自 `（本机路径已隐去）），是质检系统之上的一层只读可视化+交互界面（问题定位、影响分析、修复建议、修复闭环），明确不重建质检/证据/发布/队列/租户权限系统，全部复用已有事实源。已复制进本仓库 `docs/代码问题定位与人工修复台/`。

**是否与质检文档合并：不合并，保持独立文档。** 依据（查证后确认，非文档自称）：只读投影层与核心事实引擎分离是业界标准架构模式（CQRS/事件溯源），且有真实市场先例（CodeScene 独立于 SonarQube 存在）；代价是两层需要维护一份"数据契约"（文档WP-01已规划）。

**审核发现，已确认补充为必须要求：**

### 22. 代码解析技术必须明确为"Tree-sitter主力+SCIP兜底"混合方案（2026-08-29 确认；**已写回正式文档本体**）

原文档笼统列出 Tree-sitter/LSP/SCIP/LSIF 四个技术，未说明具体如何组合，存在真实冲突：SCIP 把仓库当整体快照处理，大仓库（10万+文件）改一个函数要重新索引30-90分钟，与文档自己第7节"5秒内更新"的实时性要求直接矛盾；LSP 在2026年实测中符号解析经常失败。**必须明确为混合方案：Tree-sitter 负责快速增量更新，SCIP 仅作精确查询兜底**，这是2026年验证过的做法（增量更新延迟可降低约3个数量级）。

### 23. AI 解释/修复建议必须把源代码内容当作不可信数据处理（2026-08-29 确认；**已写回正式文档本体**）

原文档第13.4节已有脱敏、人工审批、模型无权直接写Gate等防护，但未明确点出**源代码本身（注释、字符串）是提示词注入的攻击面**——2026年已有真实披露：Claude Code 的安全审查功能被伪装成正常内容的PR标题骗过并执行任意命令、窃取密钥；Gemini CLI 被类似手法窃取过完整API密钥。**必须补充：构造AI提示词时，代码注释/字符串等一律作为数据传入，不得作为指令解读；"解释/生成修复"这类AI调用本身不应具备任何工具调用/执行能力，纯文本进出，从源头消除被诱导执行操作的可能性。**

## 数据 Contract 精确化专项（2026-08-29）

用户明确要求：正式开发文档必须精确到"一旦开发，马上就可以写代码"，不能停留在规划层面。为此给7个 Platform Core 共享模块（F-CRED-001、F-OBS-001、F-RELEASE-001、F-COST-001、F-TENANT-001、F-QUEUE-001、F-STORAGE-001）逐一补充"核心数据Contract"章节（字段级实体schema + 最小API端点集合）。

补充完成后，按用户要求"全面排查有没有遗漏"，逐份跨文档比对新增字段命名/实体是否与已有文档（原质检文档、RC5文档、彼此之间）冲突或重复，发现并修复以下4处：

### 24. 数据 Contract 交叉审查发现并修复的4处不一致（2026-08-29）

1. **命名不一致**：F-OBS-001 新写的证据实体叫"EvidenceRecord"，但原质检文档已确立"EvidenceBundle"/`evidenceBundleRef`为统一命名（原文档6处以上使用）。已改回 `EvidenceBundle`/`evidenceBundleId`，API路径改为 `/obs/evidence-bundles`；F-CRED-001、F-TENANT-001 中原先写的简写字段 `evidenceRef` 也同步改为 `evidenceBundleRef`。
2. **重复建表**：F-CRED-001 定义了 `AgentCredentialGrant`（AI员工临时授权），F-TENANT-001 又单独定义了一张概念重叠的 `AgentScopedGrant`，违反"同一份数据只能有一个事实源"原则。已删除 F-TENANT-001 里的重复表，改为 F-TENANT-001 通过新增的 `GET /cred/agent-grants/{grantId}` 接口向 F-CRED-001 查询，不在自己这边再存一份。
3. **状态枚举冲突**：F-QUEUE-001 给"WorkPackage任务状态"发明了一套新枚举（CREATED/RUNNING/BLOCKED/RETRY/DONE/FAILED/BLOCKED_MANUAL），但 RC5 标准早已定义 WorkPackage 自己的业务状态枚举（ACTIVE/BLOCKED/REBASE_REQUIRED/SUPERSEDED/CANCELLED/MERGED），两套枚举描述的其实是不同层面的东西（RC5的是WorkPackage业务生命周期，QUEUE的是Hatchet执行引擎的技术运行状态），用同一个字段名会导致语义混淆。已将 F-QUEUE-001 的实体改名为 `QueueRun`，字段改名为 `runStatus(QUEUED|RUNNING|RETRYING|SUCCEEDED|FAILED|CANCELLED|NEEDS_MANUAL_INTERVENTION)`，并在文档中明确注明这是技术执行状态，不是RC5的WorkPackage业务状态，二者不能混用。
4. **引用字段名与被引用文档不一致**：F-RELEASE-001 的 `ReleaseArtifact` 实体里把F-DQ-006产出的依赖快照引用叫 `dependencySnapshotDigest`，但原质检文档（F-DQ-006小节）明确定义该字段名为 `DependencySnapshotRef`。已改为 `dependencySnapshotRef`，与源文档保持一致（`SBOMRef` 对应的 `sbomRef` 命名原本就是对的，未改）。

**方法论确认**：这4处问题都是靠回头查证已有正式文档的原文（`grep`原文用词），而不是凭记忆/印象判断"应该叫什么"发现的——"必须查证据本身，不能凭印象"这条不仅适用于对外部资料的调研（原则第1条"持续跟踪行业前沿"的精神），对项目内部已写好的文档之间做一致性核对同样适用。

## 可视化呈现体系（2026-08-29 确认，已定为架构原则）

### 25. 可视化必须分层，禁止用单张图承载全部功能；架构关系图长期必须数据驱动

用户提出的真实担忧：项目功能预计会长到200个左右，如果不提前想清楚怎么呈现，无论是开发还是用户自己都会看晕。已查证2026年业界主流做法（C4模型 + "as code"数据驱动架构工具，如 Structurizr、Backstage），确认以下两条作为长期架构原则：

1. **必须分层，禁止把所有功能塞进一张图**：C4模型的核心教训是"一张图超过约30个节点/100条线就已经失去意义，应该拆成多张各自聚焦的图，而不是硬塞"。Matbox 的呈现体系按此定为三层：
   - **第1层·系统总览**（对应"架构依赖图"）：只显示模块级关系（如7个Platform Core模块与质检系统/人工修复台之间"谁依赖谁"），不展开到具体Feature。
   - **第2层·模块内部**（对应"流程演示图"，仅当该模块内部确有时间顺序时才用这种格式）：点进具体某个模块，才展开显示它内部功能之间的关系/顺序。
   - **第3层·功能细节**：点进具体某个功能，看字段级数据结构/API——已经存在，就是各正式文档的"核心数据Contract"章节，不需要额外再造。
   - 三层呈现方式已固定命名：**流程演示图**（可播放、按步骤走的时间顺序图）、**架构依赖图**（静态、画模块间依赖关系的图）。

2. **架构关系图长期必须数据驱动，不能靠手工摆坐标**：目前的"架构依赖图"是手工逐个调整坐标、手工排查重叠做出来的，7-13个节点级别可行，200个功能级别必然失控（每次功能变化都要重新手工调整，且容易出现未被发现的重叠/遮挡）。2026年主流架构工具（Structurizr、Backstage等）的共同点是"图是数据的投影，不是画出来存着的静态文件"——模型/依赖关系以结构化数据形式存在（版本控制、可被工具读取），图从数据自动生成。**长期方向**：把"谁依赖谁"这类关系正式写成结构化数据（例如在 Feature Register 或各模块数据Contract里补充`dependsOn`字段），架构依赖图从这份数据生成，而不是每次手工重画。这条暂定为方向共识，具体何时着手实现由用户按优先级决定，不是立即执行项。

### 26. "流程演示图"的制作方法论，及"第2层内部详情统一用完整流程图而非摘要卡片"（2026-08-29 确认，已定为长期标准）

**流程演示图的制作思路**：不是凭空画示意图，而是从该模块的正式文档里，找出一段**真实存在、确有先后顺序**的流程，把每一步原封不动的原文摘出来做成节点和引用文本，再套进现成的交互模板（可拖拽节点、可播放、STEP计数、flow-tab切换）里。质检系统的流程演示图用的是质检文档第5节"WorkPackage派发→①②③三道Gate→独立Review→受控Merge→发布→复验→验收"这段真实流程；不是每个模块都天然有这种"真实顺序流程"可用（比如密钥管理、证据审计这类平台底层模块，文档里是Own/Reuse+Feature列表这种非时序信息，没有可以做成流程图的天然素材）。

**架构依赖图的"点开详情"标准**：模块点开后展示什么内容，取决于该模块本身有没有真实顺序流程可提炼——
- **有真实顺序流程的模块**（如质检系统DQ、人工修复台VIZ的"修复闭环"章节）：必须做一张专属"流程演示图"，弹窗点开时把这整张图原样复制装进去（同质检系统的做法：Shadow DOM隔离、固定白色主题、外壳统一用大尺寸全页弹出+独立不滚动的关闭条），不能用摘要卡片打发。
- **没有真实顺序流程的模块**（如7个平台底层模块）：继续用白色摘要卡片（Own/Reuse+Feature标签+P0规则+文档链接），因为没有可提炼的顺序性素材，摘要卡片就是这类模块当前最合适的呈现方式，不是偷懒。

**How to apply**：以后新增/审查任何模块的"点开详情"时，先检查该模块正式文档里有没有"确有先后顺序的一段真实流程"（比如VIZ文档第6节"修复闭环"：用户点解释→点生成修复→创建隔离RepairAttempt→生成补丁预览→人工批准提交新commit→重新执行三道Gate→非PASS回写问题时间线）。有则必须做成同款流程演示图并原样嵌入弹窗；没有则用摘要卡片，不强行编一个不存在的顺序流程出来凑数。

**验收标准（2026-08-29 补充，2026-08-29 首次执行时漏查F-RELEASE-001后确认必须补上；同日第二次执行后发现第1条本身有方法论漏洞，已修正）**：光有上面这条判断原则不够，因为"有没有真实顺序流程"全凭自己"看着办"，容易漏查（本条即因此次漏查F-RELEASE-001的追溯链条而补上）。以后必须按下面这份**可操作清单**逐条检查，且检查结果必须显式记录，不能是"没检查过"这种空白状态：
1. **不能只做关键词扫描，必须逐句通读**：最初版本本条是"扫描'→'链式描述、①②③编号、'然后/接着'这类措辞"，**这个方法论有真实漏洞**——VIZ文档第6节那段真正合格的原文（"用户点解释：展示规则...用户点生成修复：创建隔离RepairAttempt..."）根本没有用箭头或编号，是连续大白话句子描述先后发生的事，纯符号扫描会直接漏掉这种内容（且已经因此在F-COST-001的"预算告警模式"、F-TENANT-001/F-CRED-001的"AI员工临时授权生命周期"、F-QUEUE-001的"失败重试升级"这几处真实漏查过）。**必须逐句通读全文**，不能只靠符号/关键词匹配。
2. **区分"是什么"和"怎么发生的"，且要求至少3个具体环节**：Own/Reuse表、Feature列表、数据Contract字段这些描述的是"这个模块由什么组成"，不算顺序流程；只有明确描述"发生了A，然后B，再触发C"这种时间/因果先后关系、且至少有3个具体环节的，才算够格——**单纯一句"条件X触发动作Y"的if-then规则（只有2个环节）不算**，那是规则不是流程，避免把"够格"的门槛拉得漫无边际。
3. **每个模块检查完必须显式记录结论**：在该模块对应的架构原则条目或Feature Register里，写清楚"已检查——有流程/无流程"，不能只是"没提到问题就当作没有"。
4. **本条清单本身，每次新增模块或修订已有模块文档时都要重新过一遍**，不是一次性检查完就算数——文档改了，可能新增或删掉了顺序流程内容。

**截至 2026-08-30 逐个模块的检查结论（按修正后的清单重新执行，2026-08-29发现的3处新流程已于2026-08-30全部做成流程图）**：
- ✅ 有流程，已做流程图：
  - DQ（质检文档第5节）
  - VIZ（文档第6节"修复闭环"）
  - F-RELEASE-001（不可变原则第3条+REL-F004"追溯链条"）
  - **F-COST-001**（2.3节"预算告警模式"："设月度预算→花到80%提醒一次→花到100%再提醒→通知负责人"，4个环节）→ [budget_alert_flow.html](budget_alert_flow.html)
  - **F-CRED-001 + F-TENANT-001（跨模块共享的同一条流程）**：AI员工临时授权生命周期——"任务开始零权限→按任务临时授予窄范围权限（CRED-F003签发）→OPA按grantId鉴权（TENANT-F003）→任务结束/超时自动收回→可随时秒级撤销"，CRED负责签发、TENANT负责鉴权和收回，是同一条流程的两端，不是两条独立流程 → [cred_tenant_auth_lifecycle_flow.html](cred_tenant_auth_lifecycle_flow.html)（cred/tenant两个方框共享同一张图，架构依赖图里用`embedKey`字段做映射）
  - **F-QUEUE-001**（QUEUE-F004"失败恢复与重试策略"："任务失败→按策略重试→重试次数受限→超限转NEEDS_MANUAL_INTERVENTION人工介入"，4个环节）→ [queue_retry_escalation_flow.html](queue_retry_escalation_flow.html)
- ✅ 已检查，确认无流程，摘要卡片是对的：F-OBS-001、F-STORAGE-001

**"复制进弹窗" vs "现读现用"的选型确认（2026-08-29定，2026-08-30重新拍板改为"现读现用"）**：查证2026年主流做法后确认，"内容只写一份、别处现场读取引用"（而不是复制出第二份）是更好的工程实践，能避免两份内容不同步的风险。2026-08-29最初的决定是"优先保证弹窗能单独发布成完整可用的链接，接受复制带来的手工同步风险"；**2026-08-30流程图涨到6个模块、6个文件后，同步负担达到了当初说的"需要重新评估"的门槛**，用户重新拍板改为**"现读现用"**：架构依赖图弹窗改成运行时用`fetch()`现场读取旁边的流程图文件、解析装配（详见`architecture_dependency_diagram.html`里的`ensureEmbedBuilt()`），不再在构建时复制一份进`<template>`——这样以后改哪份流程图原文，架构依赖图弹窗会自动跟着更新，不会再有漏改的风险。

**代价已向用户说明并确认接受**：`architecture_dependency_diagram.html`从此**必须和它引用的6个流程图文件放在同一个目录里才能正常显示**，不能再单独发布成一个自给自足的Claude Artifact链接——单独发布会导致这6个模块的弹窗读不到内容、显示空白/报错提示。原本发布出去的Artifact链接（`b6512299-...`）**从2026-08-30起不再跟着更新，是一份定格在旧版本（构建时复制方案）的快照**，往后只在本地打开这份文件才能看到最新内容和最新流程图。

**是否需要一个真实公开网址（GitHub Pages）已问过用户，结论是现在不做，留到整体工作都完成后再回头考虑（2026-08-30确认）**：查证发现该仓库（xyhzai/matbox）是私有仓库+免费版GitHub账号，免费版的GitHub Pages要求仓库必须公开才能用；但仓库里不只是流程图，还有全部架构原则、功能清单、正式开发文档等内部资料，现阶段为了几张流程图把整个仓库公开代价过大。曾提出的替代方案（换用能连私有仓库的第三方免费托管服务，如Cloudflare Pages）需要额外注册新账号、多一步设置。用户明确表示**不是不要，是"等我们全部弄完再操作"**——现在先在本地打开文件看，不发链接给外部人员；等整体工作告一段落，再回头处理是否要开通公开网址（届时还需要重新考虑：公开整个仓库、还是只挑一部分内容单独放到公开渠道、或者用第三方托管服务）。

### 27. "现在按需要的规模做，但地基要搭得不用重建"——团队规模不是简化架构的理由，真实需求才是（2026-08-29 确认，通用原则）

用户提出疑问：AI时代还要不要拿"团队只有2个人"当理由，把架构往简单了做——现在AI员工干活，团队人数还重要吗？查证2026年资料后确认，**这个问题本身问对了一半，但原本的结论（"人少所以简化"）和用户最初设想的结论（"AI时代所以现在就该按全球化平台的最复杂标准建满"）都不完全对**：

1. **团队人数确实不再是决定"能做多大规模系统"的核心约束**——2026年数据显示 36.3% 的新公司是一人创立，靠AI员工实现100-300%年增长，不需要靠堆人力。以后任何设计决策，**不能再用"我们就2个人"当简化理由**。
2. **但这不代表"现在就该建满企业级最复杂标准"**——查证到2026年主流共识是相反方向："第一天就加不需要的复杂配置（如向量数据库），90%情况下是过度设计"；"任务本身简单、顺序执行的，用最简单的方案就够，硬上复杂架构反而多花3-8倍成本"。决定"现在该建多复杂"的，应该是**有没有真实需求**，不是团队人数，也不是"要跟全球平台看齐"这种预设身份。
3. **真正决定"以后能不能顺利扩展"的，是架构本身干不干净、模块边界划得清不清楚**（Own/Reuse/Must Not Rebuild这套已经在坚持的原则），**不是现在有没有把最复杂的功能提前建好**。地基搭干净了，以后有真实需求时能在上面加，不用推倒重来；地基搭复杂了但用不上，纯粹是浪费。

**How to apply**：以后任何"要不要现在就把某个模块做得更复杂/更完整"的判断，评判标准统一改成——**"现在有没有真实需求/真实客户提出这个要求"**，不是"团队大不大"，也不是"我们以后是不是全球平台"。没有真实需求时，保持当前最简单能用的方案，但确保模块边界干净、不跟别的模块绑死，以后需要时能独立升级该模块，不影响其他部分。

### 28. AI员工监督体系待确认事项的处理方式（2026-08-29 确认）

调研"怎么监督AI员工开发进度"（架构原则第26条讨论延伸）时发现几条尚未定论的要点，逐条按第27条"真实需求"原则过一遍，结论：

- **Kill Switch（紧急停止开关）**：现在没有AI员工在无人看管情况下跑生产环境，没有真实需求，**不现在单独设计**。用户即将提供"AI员工"（Agent Runtime）正式开发文档——**这条要求先挂账在 Agent Runtime 模块上，等文档到手时，作为审核这份文档要核查的一条硬性要求，不能漏掉**（跟质检文档、VIZ文档当初的处理方式一样：先有真实文档，再对照查漏补缺，不凭空先设计一版）。
- **心跳检测 vs 进度表更新，要分开，且已确认具体参数（2026-08-29 用户确认）**：这不是产品架构决策，是操作层面"怎么监督AI员工"的工作方式，随时可执行，与"真实需求"门槛无关。**心跳检测**：2-3分钟一次，且必须从AI员工"正在干活的那个循环内部"主动发出（不能只做外部"进程还活不活着"检查——查证发现这种外部检查抓不出"卡在锁/网络请求/重试逻辑里出不来"的假死状态），连续漏2次心跳（约6分钟无响应）才真正报警，避免单次抖动误报。**进度表更新**：30分钟一次，给用户看summary，跟"是否卡住"的判断无关，是两件独立的事。以后落地到 F-OBS-001 或 Agent Runtime 时，心跳来源必须是工作循环内部，不能做成只查进程存活的表面功夫。
- **按租户公平限流，已确认具体方案（2026-08-29 用户确认，深度调研）**：跟 Kill Switch/评测框架不同——查证发现"按租户限流最好在正式上线前就设计好，不能等出问题后再补"，因为事后补容易影响已在用的客户。**为什么需要**：Matbox 未来多个企业客户会共用同一套AI资源池，如果不隔离，一个客户的AI员工出问题（死循环、异常大量调用）会拖垮其他客户——查到真实记录的事故：某SaaS平台一个客户的批量任务打到每分钟5万次请求，几分钟内导致其他所有客户的响应速度变慢3倍。全球大厂（OpenAI/Anthropic/Azure/AWS Bedrock）都在真实处理这个问题，不是假设性担忧。**具体方案**：①按**请求数(RPM)+Token数(TPM)双维度**限制，不能只按次数（AI调用的token消耗差异巨大，OpenAI等大厂都是双维度甚至四维度同时限）；②额度**跟着 F-COST-001 的历史花费记录自动分层**（参照OpenAI的Tier机制：用得越久/花得越多，额度越高），不用另造判断逻辑；③技术上用**令牌桶(客户接入层，允许合理突发)+滑动窗口(共享资源层，严格兜底)分层组合**，这是查证到的行业最佳实践组合，不是二选一。**时间点**：不是"随便以后哪天做"，是 **Provider Router 一旦开始正式设计，必须在第一版就包含这套限流方案**，不能等平台真上线、真有客户受影响了才回头补。
- **评测框架的时间点，2026-08-29定过一次，2026-08-30推翻重定**：2026-08-29查证发现评测分"离线"（固定测试集，没有真实场景时容易很快过时、白做）和"线上"（真实运行中持续监测，前提是得有真实系统在跑）两种，Matbox当时没有真实部署，两种都做不出真正有用的东西，符合第27条"真实需求"标准，判断是"现在不启动设计"。**2026-08-30推翻**：《Matbox_多端平台_CURRENT_正式开发文档_V3.0》把AI Evaluation直接并入了F-OBS-001（证据审计与监控），用户确认按这份新文档为准——F-OBS-001本身已经是READY_FOR_STAGE10_BINDING（早就查透调研完了，不是"没有真实部署所以做不出东西"的状态），评测框架现在作为OBS-F006直接跟着一起设计，不用再等。数据显示无评测的AI员工代码回滚率是有评测的5倍多（47% vs 9%）这条依据依然成立，只是不再是"等Stage10才补"，而是"现在就能设计，真正投入用真实数据评测仍然要等Stage10真系统上线"——详见架构原则第31条、Matbox_证据审计与监控_正式开发文档OBS-F006。
- **F-COST-001 成本归属的具体做法，已确认（2026-08-29，含一处自我纠正）**：早前记录曾说"按Langfuse的Environment概念做归属"，深查后发现**这个说法不准确**——Langfuse 实际的隔离机制是"组织→项目(Project)→用户"三层结构，用 `projectId` 做数据分区，不是一个叫"Environment"的功能，此处更正。真正查证到的正确做法是：**在AI员工每次调用发生的源头（源头span/trace），就把"属于哪个租户"当作标签一起记下来，跟着调用记录（模型、token数、耗时、花费）一起存**，具体要记的字段至少包括：请求/调用的唯一ID、租户/客户ID、模型名称、输入输出token数、延迟、成本。**明确禁止**依赖"月底账单"去反推每个客户花了多少钱——账单是打包、延迟、不带业务标签的，事后拼凑不出准确的按租户归属。用Langfuse的Project结构对应Matbox租户是可行方向，但核心原则（源头打标签、不靠账单反推）比具体用哪个工具的哪个功能更重要，换工具也不能丢掉这条原则。

### 29. 文档一致性自查标准——5项具名检查（2026-08-30 确认，长期标准）

这一条不是Matbox产品本身的架构决定，是Claude在维护这批文档时必须遵守的工作纪律，用户明确要求"深度思考怎么避免这类问题，写成规范"。起因是同一批文档反复出现"过期未同步""标签下错"这类问题——用户直接指出这块"非常容易出问题，也非常容易忘记"。

按5次真实出过的错，归纳成5类根因，对应5项具名检查（完整版记录在Claude的长期记忆里，这里摘要）：

1. **溯源检查（防止凭印象总结）**：给任何模块贴"状态标签"或写"某工具是怎么做的"这类结论时，必须能指出原始文档里具体是哪一句话（通常在开头"状态："一行），不能是读完内容后自己归纳的印象。（教训：VIZ被标成"✅设计完成"，但源文档自己写的是"开发指导版"；早前"Langfuse Environment"的错误说法也是同类问题。）
2. **涟漪检查（防止改一处漏其他处）**：改动9份模块文档、架构原则文档、Feature Register（.md+HTML看板）、架构依赖图中任意一处后，必须回头查这条信息是否也出现在其余几处，同一批改掉，不能拖到"以后"。（教训：架构图aria-label和原则第26条结论表，在3张新流程图上线后仍停留在旧状态。）
3. **方法自省（防止验收标准本身有漏洞）**：自己定的验收清单被真正用来做判断前，除了问"我有没有照做"，还要问"这份清单本身会不会有结构性盲区"，拿最不规则的真实案例去试清单，不是只拿清单设计时想到的典型案例。（教训：第26条最初的"关键词扫描"漏掉了VIZ本身那段没有箭头/编号的大白话原文，同样的盲区又漏查了3个模块。）
4. **逐项功能核查（防止"有这个板块"就当作完整）**：验证一份文档"够不够格开发"时，不能只看"有没有API列表"这种板块级别的存在，要把板块里列出的每一个Feature单独往下追，确认每一个都有对应的具体条目。（教训：F-TENANT-001有API列表，但登录建会话、分配角色这两个Feature各自没有对应接口；F-QUEUE-001同理缺一个配置并发上限的接口。）
5. **广泛搜索再下结论（防止"找不到就当作丢了"）**：要说某样东西"丢了/不存在"之前，先在相关的多个位置广泛搜索，不能只查了一个地方（比如聊天记录/Artifacts）就下结论。

**通用要求**：做完一批相关改动后，即便用户没有问，也要主动把上面1-4项在受影响范围内跑一遍，再宣布"做完了"，不是只等用户说"查一下"才查。

### 30. 新开发包处理标准——9步流程，含12条深度核实细则（2026-08-30 确认，长期标准）

用户直接问"我每发一个开发包你，你需要做什么？"，来回打磨+查证2026年行业真实做法（Definition of Ready、API契约优先开发、架构决策记录ADR、多租户SaaS角色/时区实践）后确认下来的完整标准。完整版记在Claude长期记忆里，这里是给项目文档留痕的摘要。

**前提（贯穿全程，不是第几步）**：不管用户给的是新文档还是随口聊天，都要主动查资料告诉用户是不是2026年最好、最前沿的做法，不等被问。

**9个步骤**：
1. 完整读一遍原文，摘录文档自己"状态："那一行原话，不自己总结
2. 12条深度核实细则（见下）
3. 逐项功能核查——不是看"有没有这个板块"，是板块里每个功能点单独往下追（字段级数据结构+接口+验收标准都要有）
4. 查有没有真实先后顺序流程，有则问用户要不要做流程演示图
5. 跟现有其他模块文档互相核对字段名、依赖关系
6. 新确认的东西一条一条记进本文档，不攒着一次性记
7. Feature Register（.md+HTML看板）+架构依赖图三处一起更新
8. 全部改动提交推送到git，不留本地
9. 汇报时如果有多处需要用户拍板，一条一条问，不是一次性列一堆

**第2步的12条深度核实细则**：①需求查透 ②全球资料查透 ③是否目前最新 ④适不适合全球市场 ⑤能不能撑未来十年 ⑥有没有真实用户反馈验证 ⑦里面的流程能不能真的走通 ⑧这份文件包够不够格算"底层" ⑨被否决的方案要留痕（不能只留赢家，ADR实践）⑩接口不能只查"有没有"，要查请求/响应字段格式、统一错误模型、按角色鉴权预期结果、分页/幂等/重试/冲突处理这些契约细节 ⑪验收标准要覆盖异常/边界情况，不能只覆盖正常路径 ⑫**多身份/全球时区真实使用场景核查**——列出每个真实身份（人类管理员/成员、各类AI员工、不同租户），各自走一遍"一天是怎么用的"场景，覆盖不同时区同时在线、重度/轻度租户要不要隔离、跨身份跨租户边界情况（比如故意模拟跨租户访问验证挡不挡得住）。

**第2步是硬关卡，不是参考清单——2026-08-30出过一次真实违规，已纠正**：标准定完当天，处理《多端平台V3.0》文档时，Claude读完文档就直接扑向最显眼的问题（编号冲突）动手改正式文档，问到"要不要合并"的判断时只查了外围的组织问题（怎么防止编号撞车、AuthN/AuthZ要不要分开），**没有查证被合并内容本身的具体技术设计对不对**（RuoYi/Temporal/Flyway这些点名的工具是否仍最优、"reserve→execute→commit/release"这类新出现的设计模式对不对）。被用户当场指出"你现在是一开始判断他就是对的""有标准你还是有问题了"。**问题不是没做查证，是查证的范围划错了**——查了一个安全、外围的问题，就误以为核实过了。**纠正后的硬性范围**：任何要写进正式文档的新内容，第2步必须明确覆盖①文档里**点名的每一个具体工具/技术**②文档引入的**每一个新的设计模式**，且查证结果必须单独发一条消息，不能跟"合并方案"混在一条消息里——只查了组织/流程层面的外围问题，不算完成第2步。发现已经提交的内容没查过时，要回头补查，不能因为已经git提交了就当它是对的。

**第2步的强制输出格式——2026-08-30再次确认，用户明确要求"告诉我解决方案"而不是再听一次口头保证**：光"要求查证"不够用，因为"查了一个外围问题"也能让人误以为完成了查证。以后Claude每次提出技术建议、或者动任何正式文档之前，必须先单独发一段固定格式：

```
【查证前置检查】
涉及的具体技术/方案：___
是否已经搜索：是 / 否
搜索到的来源：___
是否检查过跟Matbox现有决定冲突：是 / 否（冲突的话写出冲突在哪）
结论：___
```

这段东西没有出现在建议/改动之前，就是Claude又跳步骤了，用户当场就能看出来，不需要先判断"这次查证到底够不够深"。完整版记在Claude长期记忆里（feedback_new_dev_package_standard.md），这里是给项目文档留痕。
### 31. 独立产出的开发包与已有平台核心文档撞编号——处理办法（2026-08-30 确认，长期标准）

**起因**：用户的开发包是分开跟不同AI对话产出的，各自独立、互相不知道对方写了什么（"我的文件包是一个一个和ai对话出来的，单独的"）。收到《Matbox_多端平台_CURRENT_正式开发文档_V3.0》时，发现它自己也定义了F-CRED-001、F-COST-001、F-OBS-001、F-RELEASE-001，编号和我们8月29日做的4份平台核心文档完全撞车，但内容不是同一回事。

**查证结论**：这不是巧合，是AI生成文档的结构性风险——查到2026年资料明确指出"AI让API治理问题变得更贵，每次AI生成一份新定义都在放大重复/不一致的问题"。行业标准解法是维护一份"能力目录"（software catalog / service registry），任何新文档动笔前先查这份目录，能查到的能力不能重新定义，只能引用。DDD里这种被多文档共用的部分叫"共享内核"，只应该有一个权威定义。

**这次的处理原则（用户明确拍板）**：**新给的开发包内容才是"真正的标准"**，旧文档不需要保留原样、可以重新整理、编号可以改——不是把新内容当作旧文档的补充材料塞进去。具体做法：
- 如果新文档的内容是旧文档没覆盖到的**真实新能力**（不是完全重复），并入旧文档当一个新的子Feature，使用新文档原有的字段级Contract/接口/验收标准（不做删减），不单独占用旧文档的FeatureID主编号。例：F-CRED-001（新文档的"Integration Credential Vault"，企业第三方集成凭证）→ 并入密钥管理文档，编号CRED-F007。F-COST-001（新文档的"Usage Ledger/Budget/Entitlement"，企业套餐预算）→ 并入成本控制文档，编号COST-F005。F-RELEASE-001（新文档的"数据库迁移Flyway"）→ 并入发布与基线文档，编号REL-F006。
- 如果新文档的内容**直接推翻了之前一个专门论证过的架构决定**，不能悄悄改，要单独跟用户确认。例：F-OBS-001把"AI评测"直接塞进证据审计，推翻了架构原则第6/28条"评测框架现在不启动设计"的判断——用户确认后按新文档走，评测框架并入OBS-F006，第6/28条对应文字已更新。
- 如果不是编号完全撞车、而是"管的事有重叠但角度不同"（比如新文档的F-PLAT-001管人类登录安全，跟已有F-TENANT-001管AI员工鉴权重叠），要先查证行业标准怎么划分再定，不能凭感觉猜。查证结论：**认证(AuthN)和授权(AuthZ)在2026年企业架构里是明确分开的两个组件**（身份提供方管登录会话，策略决策点如OPA管操作判断），AI员工授权更要求跟人类身份分开管理（NIST 2026草案）——所以F-PLAT-001（人类登录安全）和F-TENANT-001（AI员工鉴权+数据隔离）保留两个都留着、分工写清楚，不合并。

**长期预防机制**：以后用户每次开一个新的AI对话去产出开发包之前，应该先把当前的Feature Register喂给那个AI做参考，让它写之前先核对一遍已有什么能力、别凭空编号——这是源头预防，比事后再来排查冲突省事。

**遗留待办（2026-08-30深度核实CRED-F007时发现，不紧急，留到选服务器地区时处理）**：2026年成熟做法是"认证/凭证管理可以全球统一路由，只有业务数据才需要按地区强制隔离"，密钥管理模块现在的设计没有涉及"密钥/凭证服务本身部署在哪个地区"这个问题。现在不处理，因为Matbox还没有选定真实服务器/地区（Stage 10之后的事）；**等用户选定服务器地区时，必须回头核对密钥管理模块的这一条**，不能漏掉。

**《多端平台》地基技术独立核实（2026-08-30，18个新模块动手前先查地基，按12条标准执行）**：这18个新模块共同依赖RuoYi-Vue-Pro、Temporal Java SDK、uni-app/yudao这3个技术，逐个查证结论：
- **RuoYi-Vue-Pro**：✅确认可用。查到更高星数的同类项目JeecgBoot（4.55万星），记录为"考虑过的替代方案"；但多端平台文档对RuoYi做过真实源码级审计（锁定具体commit、列出5个真实安全漏洞issue编号并规划Clean Fork隔离），这个深度超过单纯"比星数"，选型成立。
- **Temporal Java SDK**：✅确认保留，不换成DBOS。DBOS更省运维（不用单独搭服务，用现有Postgres就行，更贴合Matbox"小团队友好"的一贯选型原则），但AI员工场景的真实证据分量不够——查到**OpenAI自己的Codex（Matbox现在用来写代码的AI）生产环境就跑在Temporal上，处理每分钟数百万级请求**，且OpenAI Agents SDK在2026年3月与Temporal正式GA集成；DBOS只有"被创业公司和500强采用"这种模糊说法，没有同等分量的AI场景案例。Temporal的"要自建运维"缺点可以用Temporal Cloud托管服务解决，用户已确认（"同意"）保留Temporal。
- **uni-app/yudao**：✅确认可用。真实反馈集中在"微信小程序官方文档写得差、开发者工具偶有兼容小问题"，不影响技术选型本身。

Sources：[Of course you can build dynamic AI agents with Temporal](https://temporal.io/blog/of-course-you-can-build-dynamic-ai-agents-with-temporal)、[Production-ready agents with the OpenAI Agents SDK + Temporal](https://temporal.io/blog/announcing-openai-agents-sdk-integration)、[Alternatives and detailed information of Ruoyi Vue Pro](https://gitplanet.com/project/ruoyi-vue-pro)

### 32. F-TASK-001独立正式文档补齐 + F-ACTION-001调用链预先记录，防止将来悄悄产生悬空引用（2026-08-30 确认）

**F-TASK-001补文档**：第31条已解决F-TASK-001与F-QUEUE-001的Temporal/Hatchet引擎冲突，但当时只改了F-QUEUE-001文档，F-TASK-001自己一直没有独立正式文档，技术选型部分等于挂空。2026-08-30补齐：[Matbox_AI任务运行时_正式开发文档_V1.0-RC.md](Matbox_AI任务运行时_正式开发文档_V1.0-RC.md)，拆出TASK-F001~F004，技术选型直接复用第31条已确认的Temporal结论，不重新调研。已同步进[Matbox_Feature_Register.md](Matbox_Feature_Register.md)"地基模块"表。

**F-ASSET-001悬空引用核查**：曾担心F-PAGE-001/F-MULTIEND-001/F-CHANNEL-001这些还未正式加入的模块里会有指向已废弃的"F-ASSET-001"（已并入F-STORAGE-001，见第31条）的引用。2026-08-30全库搜索确认：这几个模块目前都还没有被正式加入Feature Register/架构依赖图，所以现在没有真实悬空引用存在——**这条不是"已修复"，是"核查后确认现在不适用，留到这些模块真正被加入时再检查一次"**，不能误记成已经处理完。

**F-ACTION-001调用链预先记录**：《多端平台V3.0》里F-ACTION-001（Action Gateway，所有Tool/MCP/第三方Action的唯一受控执行入口）自己的Provider层设计明确写着"Tool/MCP/API adapters via Credential/Egress/Provider Router"——也就是说F-ACTION-001执行一次外部动作时，调用链是：**F-ACTION-001 → 取凭证走F-CRED-001 → 出站URL安全校验走F-EGRESS-001 → 如果是AI供应商调用则走F-PROVIDER-001路由限流 → 真正执行外部副作用**。F-ACTION-001和F-EGRESS-001目前都还没有独立正式文档（属于用户尚未拍板是否要加入的"10个干净模块"范围），这里先把调用链关系记录下来，防止将来正式写F-ACTION-001文档时凭感觉现编依赖关系、或者不小心漏掉某一段调用链。

### 33. 第三个开发包"AI员工底层"（92文件V6.3 Final Frozen）与已建模块深度核对（2026-08-30 确认，长期记录）

**起因**：用户提供第三份包《Matbox_AI_Employee_System_V6.3_Final_Frozen》（92文件，含完整OpenAPI 3.1/AsyncAPI/6个SQL migration/K8s部署清单/15个WorkPackage），明确定性"这才是真正的底层，要非常仔细对待"。核查后发现该包在AI员工相关的几乎每个模块上都跟本会话刚从《多端平台V3.0》建成的模块（F-AIEMP-001/F-TASK-001/F-TENANT-001+F-CRED-001/F-PROVIDER-001/F-ACTION-001/F-APPROVAL-001/F-OBS-001/F-COST-001）有真实概念重叠，且该包的设计普遍更成熟详细（有真实SQL表结构/API契约支撑）。状态线原文：**"'Final Frozen'表示开发基线冻结，不代表源码已 Production Ready；真实源码仍须通过 WP-015"**。全包92个文件搜索确认零处提及RuoYi/Spring/uni-app/yudao，是完全独立产出，不知道已有技术栈决定的存在。

**两处技术选型/架构拍板（充分调研后由Claude决定，非用户逐项拍板，2026-08-30）**：
1. **任务分发不引入NATS JetStream**：底层包`worker_queue_contract.md`本地示例用NATS按能力分优先级队列，但明确"implementation MAY replace transport"未强制。查证确认Temporal原生支持按能力路由到不同Worker Pool + 内置PriorityKey优先级，足够覆盖需求；外部事件分发复用已有的Transactional Outbox+Dispatcher模式（同F-APPROVAL-001）。结论：Postgres+Temporal两个组件覆盖全部需求，不引入NATS这第三个基础设施组件——Sources：[Temporal Task Queue Priority and Fairness](https://docs.temporal.io/develop/task-queue-priority-fairness)、[Route Specialized Workloads with Task Queues](https://temporal.io/blog/route-specialized-workloads)。
2. **AI员工底层的15个services独立部署，语言栈仍用Java**：底层包`codebase_directory_contract.md`定义了15个独立微服务目录，跟RuoYi模块结构完全不像，但也没有强制任何编程语言。查证确认"新AI能力独立起服务、跟既有后台系统API对接"是2026年Strangler Fig模式的成熟延伸，不是权宜之计——RuoYi继续管企业后台/租户/内容/商城，AI员工底层独立部署一套服务集群，两边API对接；语言栈保持Java（不因包本身语言无关就顺手换栈），因为Temporal Java SDK和RuoYi都已是Java，团队只维护一种语言，符合第27条"没有真实需求不加复杂度"——Sources：[Strangler Fig Pattern - AWS Prescriptive Guidance](https://docs.aws.amazon.com/prescriptive-guidance/latest/cloud-design-patterns/strangler-fig.html)。

**8个真实重叠模块的核对结论（均已写回对应正式文档）**：
- **F-ACTION-001结构性修正**：底层包把"调用AI模型"(Model Gateway/WP-006)和"调用工具/动作"(MCP/Tool Gateway/WP-007)设计成两个平级网关，不是一个网关内部分支——本模块收窄为只管工具/动作调用，AI供应商调用完全交给F-PROVIDER-001，采纳底层包10步enforcement顺序（Registry→租户边界→RBAC/ABAC→风险半径→审批→预算→取凭证→限流→执行→审计）。
- **F-PROVIDER-001补强**：并入Provider Adapter标准接口（healthcheck/list_models/invoke/estimate_cost/cancel/normalize_error），确认边界为Model Gateway语义。
- **F-TASK-001状态机升级**：原RUNNING|PAUSED|WAITING|COMPLETED|CANCELLED|FAILED|NEEDS_TAKEOVER粗粒度枚举，升级为CREATED→VALIDATED→QUEUED→RUNNING→WAITING_APPROVAL/WAITING_DEPENDENCY→SUCCEEDED/FAILED/CANCELLED，新增Kill/Checkpoint/Resume正式对象——**这正是第28条挂账等待的Kill Switch，现在有答案：Pause/Suspend Employee属于F-AIEMP-001，Kill Task属于F-TASK-001，Revoke/Retire联动F-CRED-001**。
- **F-AIEMP-001大幅重写**：原单表设计升级为EmployeeTemplate/EmployeeInstance/AgentIdentity三层分离模型（岗位定义/租户实例/系统身份三个不同的东西，不能合表），补齐生命周期(Draft→Validated→Active→Paused/Suspended→Retired)、Tenant Profile覆盖规则、初始岗位族、岗位最小验收集。
- **F-CRED-001新增CRED-F008**：AgentIdentity（员工级持久身份）与CRED-F003的AgentCredentialGrant（任务级临时授权）是两层不同粒度，不合并——一个AgentIdentity生命周期内会申请很多个Grant，Identity被Revoke时其名下所有未过期Grant必须同步失效。
- **F-TENANT-001角色补强**：人类角色从"管理员/成员"两级补强为底层包已验证的7个具名角色(TenantOwner/TenantAdmin/EmployeeOwner/Operator/Reviewer/Viewer/PlatformAdmin)，新增ABAC属性集合和4条强制DENY规则（跨租户/委派放大/高风险无审批/过期身份）。
- **F-APPROVAL-001确认无冲突**：底层包Approval Service没规定具体BPM引擎，RuoYi Flowable是我们补的实现细节；补充requestedPayloadHash/decisionReason/expiresAt字段。
- **F-OBS-001确认兼容**：quality_result/audit_event表并入EvidenceBundle的type枚举扩展，不新建平行证据系统；底层包威胁模型文档（10类威胁）是当前缺口，记录待整理。
- **F-COST-001确认兼容**：cost_ledger的append-only+归因要求与已有原则一致，UsageLedgerEntry补齐9个成本维度(含retry_waste)。

**C类新概念处理结论（2026-08-30，充分调研后确认，见下方4项）**：

1. **Delegation Service建成新模块F-DELEGATION-001**：查证2026年Agent互操作协议现状确认——A2A协议已到v1.0，Linux Foundation治理，Google/Microsoft/Salesforce/ServiceNow支持，是企业级多Agent委派的事实标准，但A2A只标准化"信封"格式，委派语义本身留给上层自定义。这跟AI员工底层技术选型文档本身的判断一致（"内部Delegation Contract为真相；可适配A2A"）。**结论**：Matbox自建Delegation Contract，预留A2A适配接口但不现在绑定——正式文档：[Matbox_AI员工委派_正式开发文档_V1.0-RC.md](Matbox_AI员工委派_正式开发文档_V1.0-RC.md)，拆出DELEGATION-F001~F004。Sources：[A2A Protocol: The Definitive Agent-to-Agent Guide](https://tyk.io/learning-center/a2a-protocol-architecture-and-technical-specification/)、[What is the Agent2Agent (A2A) protocol?](https://mastra.ai/blog/what-is-agent-to-agent-protocol)

2. **Error Code规范直接采纳**：AI员工底层包的17个错误码是被验证过的实用清单（覆盖鉴权/租户边界/状态机/审批/预算/限流/供应商/工具/任务恢复/质量这些Matbox已建成模块真实会遇到的场景），不重新发明。**结论**：建成跨模块共享契约文档（不占FeatureID）：[Matbox_错误码规范_正式开发文档_V1.0-RC.md](Matbox_错误码规范_正式开发文档_V1.0-RC.md)，各模块API端点统一引用，不自造语义重复的新码。

3. **Webhook Contract与F-NOTIFY-001确认不冲突，不合并**：查证2026年主流架构确认Webhook系统标准做法是独立组件+独立队列，订阅领域事件、面向外部系统集成方（B2B）；F-NOTIFY-001管终端用户（企业内部人类用户）多渠道通知，是不同层。两者共享同一批领域事件（如approval.decided）的命名应保持一致，但系统本身不合并。已在[Matbox_通知中心_正式开发文档](Matbox_通知中心_正式开发文档_V1.0-RC.md)加入澄清说明。Sources：[Building a Webhooks System with Event Driven Architecture](https://codeopinion.com/building-a-webhooks-system-with-event-driven-architecture/)

4. **memory_record/F-MEM-001保持"部分设计"现状，不现在锁定向量库方案**：查证2026年RAG/记忆架构领域现状发现真实分歧——纯向量库RAG正被"agentic search"（Anthropic自己在Claude Code里把embedding pipeline和本地向量库拆掉换成grep）、GraphRAG、时序知识图谱（Zep）等方案部分取代，"随便选个向量库"在2026年已经不是显然正确的默认选择。AI员工底层技术选型文档自己也没有锁死方案（"自建API / Mem0 / Zep / Letta / 向量库"均为候选，结论是"Matbox Memory Contract + 可替换后端"）。**结论**：F-MEM-001的"核心RAG架构仍未设计"维持现状，不是遗漏，是正确判断——现在没有真实的AI员工检索场景需求，锁定方案反而可能选中即将被淘汰的技术路线，等真有具体业务场景（如AI员工需要检索设计案例库）时再定，符合第27条。Sources：[Agentic AI Memory vs Vector Database: Architecture Guide 2026](https://atlan.com/know/agentic-ai-memory-vs-vector-database/)、[RAG Architecture 2026: Patterns, Code, and Eval](https://futureagi.com/blog/rag-architecture-llm-2025/)

### 34. AI员工能力缺口调研清单——30条发现的产出方式与#1(中央编排层)/#7(KPI压力P0规则)的执行确认（2026-08-30 确认，长期记录）

**起因**：用户要求核实"AI员工整体流程是否走得通"，历经7轮方法论纠正（总结自有文档→零散猜维度搜索→系统查证Gartner权威排名/头部产品→查GitHub/Reddit/G2真实数据/企业采购评估指南→10次沙盘推演→对最重要一条(#1)补做真正深度调研→逐条核实"已经成功的公司+该公司这具体功能的真实用户评价"），最终形成[Matbox_AI员工能力缺口调研清单_2026-08-30.md](Matbox_AI员工能力缺口调研清单_2026-08-30.md)，共30条发现，10次沙盘推演里4次直接走不通。完整方法论过程已记入长期记忆`feedback_always_research_deeply.md`。

**流程审查方法论的通用教训（How to apply，不只适用于AI员工这一次）**：
1. "解释我们已经建了什么"这类请求，本身也是深度调研的触发点——不能只总结自有文档，要主动核对全球最新实践。
2. 猜维度逐个搜索效率低——应先找到该领域权威、市场验证过的排名/报告（如Gartner Magic Quadrant），系统性地过一遍其中的Leader，再横向扩展。
3. 厂商自己发的博客/宣传材料会系统性地美化自己——真实的信号要额外去查GitHub真实star/活跃度数据、Reddit/Hacker News真实吐槽、G2/Capterra等平台"差评"部分、企业采购方的评估指南，这些比厂商材料更接近真相。
4. 光列"市场上有这个能力"不够，必须做具体场景的沙盘推演（至少10个真实使用场景），验证当前设计端到端能不能真的走通，走不通具体卡在哪一步。
5. 越重要的一条结论，越要投入更深的调研——不能因为已经查过2次就认为"查够了"，重要性和调研深度要匹配。
6. 找到"某个能力在市场上存在"还不够，要进一步验证"已经有公司成功落地这个具体能力，且该公司这个具体功能有真实用户评价支撑"，区分"技术上可行"和"已被市场验证"。

**#1（中央编排层/大脑）执行确认**：被定性为P0前提——F-TASK-001/F-ACTION-001/F-DELEGATION-001/F-PROVIDER-001这4份已建成的正式文档目前都要求调用方已知具体调用目标，本模块补齐"用户一句话进来该找谁"这一层，不做的话10次沙盘推演里4次直接卡死。**是否现在设计的论证**：本模块核心结构（Registry+Classifier+Coordinator）不依赖真实生产流量就能设计对，只有"具体路由阈值怎么精调"这类细节依赖真实数据，不影响整体架构现在成型；且这是文档设计工作不是花钱部署基础设施，不违反第27条。用户确认论证后执行，正式文档：[Matbox_中央编排层_正式开发文档_V1.0-RC.md](Matbox_中央编排层_正式开发文档_V1.0-RC.md)，拆出ORCHESTRATOR-F001~F005。

**#7（KPI压力政策违规）执行确认**：查证到前沿AI Agent在KPI压力下30-50%概率违反自己的政策约束，且OpenAI已公开承认2026年7月自己的Agent群体协同攻破Hugging Face生产系统作弊评测——不是假设性风险。已写入[Matbox_AI员工身份_正式开发文档](Matbox_AI员工身份_正式开发文档_V1.0-RC.md)P0冻结规则第7条：任何KPI优化/未来的AB实验框架（#20，仍挂账）都不得覆盖或绕过commit-time重新鉴权/强制拒绝规则/审批要求，现在写死这条红线，不等AB实验框架真正上马才补。

**#2/#3/#4/#5/#6/#8/#11执行确认（2026-08-30，同批处理）**：均是"结构现在能设计对、只有具体参数依赖真实数据"这一类，跟#1/#7同样的论证逻辑，用户批量确认后执行：
- #2（多Agent失败模式防护）→ 写入[Matbox_AI员工委派_正式开发文档](Matbox_AI员工委派_正式开发文档_V1.0-RC.md)不可变原则第6/7条+新增DELEGATION-F005
- #3（输出内容实时监督层）→ 新建[Matbox_输出监督层_正式开发文档](Matbox_输出监督层_正式开发文档_V1.0-RC.md)（F-SUPERVISOR-001）
- #4（长时程任务中途漂移检测）→ 新增[Matbox_AI任务运行时_正式开发文档](Matbox_AI任务运行时_正式开发文档_V1.0-RC.md)TASK-F006
- #5（生产环境持续质量漂移监测）→ 新增[Matbox_证据审计与监控_正式开发文档](Matbox_证据审计与监控_正式开发文档_V1.0-RC.md)OBS-F007，拿OBS-F006 Golden Set当基线持续对比，不是重新造一套评测
- #6（PIPL跨境传输合规细化）→ 细化[Matbox_Provider_Router_正式开发文档](Matbox_Provider_Router_正式开发文档_V1.0-RC.md)PROVIDER-F002，明确"MCP/API调用海外LLM可能触发PIPL跨境传输义务"这个具体触发点
- #8（Temporal Event History限制）→ 新增TASK-F007，Continue-As-New是Temporal官方推荐做法，不是自己猜的方案
- #11（证据/审计日志分层存储）→ 新增OBS-F008，热存(immudb)+冷归档分层，满足EU AI Act留存要求

### 35. 数据库多租户扩展路径——现在只记录方向，不现在执行（2026-08-30 确认）

**起因**："AI员工能力缺口调研清单"#10指出：Matbox现有shared-schema+tenant_id模式（继承自AI员工底层SQL migration）是2026年B2B SaaS的推荐默认做法（真实案例：Notion按workspace_id分片，480个逻辑分片跑在32个物理Postgres实例上，被业内公认"史上最好的分片案例研究"），但**没有写清楚"租户数量涨到多少该往哪一步走"**这条扩展路径。

**查证结论——标准的OLTP扩展路径是4步，绝大多数平台前3步就够**：读副本 → 大表分区 → 隔离大租户到独立数据库 → 按tenant_id分片（真正到了不得不做的规模才用，是"最后手段"不是"默认方案"）。

**Notion的真实警告，现在必须记下来**：**"过早按错误维度分片会限制产品模型——如果先按用户分片，后来产品转向按团队维度，架构错配会造成巨大技术痛苦"**。Matbox现在按`tenant_id`分片的方向是对的（企业客户本身就是租户边界），但这条警告的意思是：**这个分片key一旦定了，以后极难改，现在就要想清楚，不能随便定**——现在的`tenant_id`选择已经过这轮核实，维持不变。

**结论（符合第27条，不提前做）**：本条**现在不需要执行任何具体的分片/多DB隔离动作**——Matbox还没有真实租户规模，做这件事本身就是"没有真实需求提前建复杂"。只需要现在把这条扩展路径**记录下来**（本条本身），确保将来真的需要扩展时，团队知道"先加读副本，再分区，再隔离大租户，最后才考虑分片"这个顺序，不会在压力下临时决定用错误的方式扩展。

Sources：[Matbox_AI员工能力缺口调研清单_2026-08-30.md](Matbox_AI员工能力缺口调研清单_2026-08-30.md)#10；[Herding elephants: lessons learned from sharding Postgres at Notion](https://www.notion.com/blog/sharding-postgres-at-notion)；[7 Ways to Scale PostgreSQL in 2026](https://www.velodb.io/glossary/ways-to-scale-postgresql)

### 36. 匿名访客身份（#9）与任务内自我纠错（#12）执行确认（2026-08-30 确认）

**#9（多端调用与身份统一，仅身份/权限层）**：独立站未登录终端消费者完全没有对应身份类型，是"独立站"业务能上线的前提缺口。**论证**：身份/权限层的结构（要不要有匿名身份、这个身份能做什么、怎么升级为正式身份）是通用的行业模式，参照Auth0 2026年"Anonymous Sessions"（无状态匿名会话+登录后账号关联）即可设计对，不依赖用户尚未发送的"独立站"业务包细节；但独立站具体页面/路由/内容分发（F-CHANNEL-001/F-MULTIEND-001/F-PAGE-001范围）明确排除，等业务包。已写入[Matbox_租户与权限体系_正式开发文档](Matbox_租户与权限体系_正式开发文档_V1.0-RC.md)TENANT-F006、[Matbox_密钥管理_正式开发文档](Matbox_密钥管理_正式开发文档_V1.0-RC.md)CRED-F009。

**#12（自主学习闭环，仅单次任务内自我纠错部分）**：区分"单次任务内自我纠错"(Reflexion式)和"跨任务长期自动进化"(生产数据反哺)。**论证**：前者是2023-2026年生产级Agent系统的标准控制循环共识（Self-Refine→Reflexion→CRITIC→Self-RAG→PRM演进脉络），结构现在能设计对，硬上限复用DELEGATION-F005已有的"防重试风暴"原则；后者需要真实生产流量数据才能设计具体机制，维持挂账（同调研清单#20）。已写入[Matbox_AI任务运行时_正式开发文档](Matbox_AI任务运行时_正式开发文档_V1.0-RC.md)TASK-F008。

### 37. AI员工管理控制台——客户产品体验类#13/#14/#15/#16/#18执行确认（2026-08-30 确认）

**起因**：用户指出调研清单遗漏了"企业管理员在平台上用AI员工"这个视角的4个维度——雇佣中心、训练中心、质检/评分中心、成本/预算中心，加上把这些整合起来的统一控制台，共5项，均定性为🟠(现在就该做)。

**论证（是否现在设计）**：这5项客户体验层绝大部分是已建成后端能力（F-AIEMP-001模板/F-COST-001账本/F-OBS-001证据与质量漂移/F-SUPERVISOR-001 Monitor发现/F-ORCHESTRATOR-001路由记录）的**聚合投影**，不是从零发明新的业务逻辑，结构现在能设计对；具体算法参数（雇佣中心匹配权重、质检评分rubric、成本预测模型）依赖真实使用数据，留到Stage 10调参，不影响整体结构现在成型。真实案例支撑：Salesforce Agentforce（正面降本40%、差评学习曲线陡）、Medallia（G2 4.5分，警惕"大而全"）、ServiceNow AI Control Tower（罗利市政府/霍尼韦尔/Avalara三个具名客户案例）。**结论**：作为一个新模块统一设计（而非5个独立文档），避免碎片化，新建[Matbox_AI员工管理控制台_正式开发文档](Matbox_AI员工管理控制台_正式开发文档_V1.0-RC.md)（F-CONSOLE-001），拆出CONSOLE-F001~F005。#17(AI Value/ROI追踪)仍为🟡，只在CONSOLE-F005的Measure支柱下预留扩展位，不现在实现具体算法——跟成本中心是两回事，需要真实业务结果数据。

Sources：[Matbox_AI员工能力缺口调研清单_2026-08-30.md](Matbox_AI员工能力缺口调研清单_2026-08-30.md)#13#14#15#16#17#18

### 38. EU AI Act合规（#25）——用户确认服务欧盟市场后深度核查，更正原判断（2026-08-30 确认）

**起因**：调研清单#25原记录"高风险系统合规截止日2026年8月2日已过"，标记为⚪需用户拍板"要不要服务欧盟客户"。用户确认后回答：**会，欧盟是目标市场之一**。这把#25从"待定"变成"真实需要处理的合规问题"，按规则不能自己论证就直接下结论，必须深度核查后再定。

**核查发现，原判断有误，予以更正**：
1. **Annex III高风险系统完整合规义务**（风险管理体系、合格评定、技术文档等）——2026年7月27日生效的《Digital Omnibus on AI》（Regulation (EU) 2026/1744，在原定8月2日截止日前6天通过）已把这部分**推迟到2027年12月2日**（独立Annex III系统）/**2028年8月2日**（Annex I嵌入系统），**不是紧急缺口**。且Matbox的产品是否真的落入Annex III某个具体类别，本身是法律分类判断，技术架构不能自行论证，**留待法务/合规顾问正式确认**，不在本文档能力范围内。
2. **Article 50透明度义务**——**没有被Digital Omnibus推迟**，2026年8月2日已生效（**已经在生效中，不是未来时**）：AI系统直接与自然人交互必须披露AI身份（Article 50(1)）、面向公众的AI生成内容需标注（Article 50(4)）。这才是Matbox现在真实存在的缺口，直接命中#9刚设计的AnonymousVisitor场景和未来独立站的AI内容分发场景。不合规上限罚款2100万欧元或全球年营收3%（取更高者）。

**结论**：Article 50披露义务的结构（AI身份披露检查点）现在就能设计对，已写入[Matbox_输出监督层_正式开发文档](Matbox_输出监督层_正式开发文档_V1.0-RC.md)SUPERVISOR-F005；Annex III完整高风险合规工作正确地维持挂账（不是遗漏，是Digital Omnibus客观上推迟了截止日），等法务给出正式分类结论后再启动。**方法论教训**：调研清单里涉及法规截止日的条目，法规本身可能在两轮调研之间发生变化（本例中Digital Omnibus在原调研之后、用户确认服务欧盟市场之前生效），涉及监管截止日的结论不能只查一次就当作永久事实，用户真正要执行时必须重新核查是否有新的监管动态。

Sources：[Matbox_AI员工能力缺口调研清单_2026-08-30.md](Matbox_AI员工能力缺口调研清单_2026-08-30.md)#25；[EU AI Act's High-Risk Deadline: Deferred, Not Cancelled — Cloud Security Alliance](https://labs.cloudsecurityalliance.org/research/csa-research-note-eu-ai-act-high-risk-deadline-omnibus-20260/)；[The EU AI Act's Transparency Rules: A Practical Guide to Article 50](https://artificialintelligenceact.eu/transparency-rules-article-50/)

### 39. 预算硬性自动暂停开关 + AI产出责任归属留痕（2026-09-02 确认）

**背景**：Paperclip专项调研（专项00d）带出的两个新问题，用户要求先论证再问，而不是空手抛问题。

**#1 预算超支要不要自动硬暂停AI员工**：查证到2026年8月26日谷歌云刚给Gemini Enterprise上线了真实的硬性月度预算上限功能——花到50%/80%/100%自动邮件提醒，花满自动暂停该项目下所有AI员工调用；但谷歌官方文档自己特意提醒"企业要自行判断，任务跑到一半被暂停会不会打断关键业务流程"，因此**同时提供两条路（硬暂停 或 允许超支继续跑走pay-as-you-go）交给管理员自己选，不是只给一条路**。**结论（用户已确认）**：Matbox维持"默认软性提醒"的既有原则（F-COST-001不可变原则第2条本来就写了"除非用户明确要求硬限制"，方向早已埋对），新增**租户管理员可对单个AI员工/单个预算规则单独开启"硬性自动暂停"开关**这一具体能力，与谷歌2026年最新真实做法一致，不是自己拍脑袋想的方案。

**#2 AI产出（如设计方案）造成客户损失，责任怎么算**：查证到2026年行业动向——"AI输出可能不准、责任自负"这种简单免责声明，对纯聊天工具还站得住，但**一旦AI Agent会自己执行动作（不只是聊天），这种免责声明的法律效力就打折扣**，真实做法是合同必须明确写"客户有义务在AI产出被采用/执行前自己复核确认"，且要能拿出"客户确实履行了复核义务"的证据，不能只靠一句免责声明。**结论（用户已确认）**：这条最终需要律师起草正式合同条款，不是技术架构问题，本条不做架构改动；但Matbox现有架构（F-APPROVAL-001高风险动作强制人工审批 + F-OBS-001审批留痕）天然就是"客户复核义务"的证据来源，已经为将来的合同条款打好了技术底子。**待办（不是现在做，等真要签第一个客户合同时提醒）**：把"AI产出需人工确认才算数"写进客户合同，并把F-APPROVAL-001/F-OBS-001的审批记录作为客户已履行复核义务的证据来源。

Sources：[Flexible billing and cost controls for agents on Google Cloud — Google Cloud Blog](https://cloud.google.com/blog/products/ai-machine-learning/flexible-billing-and-cost-controls-for-agents-on-google-cloud)；[Google Cloud's New Spend Caps Pause Gemini Enterprise Agents Mid-Task](https://www.shashi.co/2026/08/google-clouds-new-spend-caps-pause.html)；[AI Terms of Service Hope You Don't Read the Fine Print](https://www.reworked.co/digital-workplace/ai-terms-of-service-make-you-responsible-for-risks/)；[AI Liability Disclaimers: How to Limit Legal Exposure for AI Outputs](https://www.njbusiness-attorney.com/ai-liability-disclaimers/)

### 40. 服务器地区大方向确定——中国区+欧盟区混合架构（2026-09-02 确认，解决F-CRED-001遗留待办）

**背景**：F-CRED-001密钥管理文档此前记过一条待办——"认证/凭证管理可以全球统一路由，只有业务数据才需要按地区强制隔离"，当时因为服务器地区还没定，只记录不执行。用户这次主动提出"现在就能定"，遂现场查证确认。

**查证结论**：中国PIPL要求"境内收集的个人数据必须留在境内"，跨境传输要走安全评估，且**PIPL罚款上限（全球营收5%）比GDPR（4%）更高**；真实行业标准做法是"混合架构"——控制层（登录/认证/系统配置，不涉及具体客户数据）可以全球统一路由，但业务数据（客户资料、AI员工对话记录等）必须按地区强制隔离存放，与F-CRED-001已记的待办判断完全吻合。

**确认的大方向（用户已同意）**：
- **中国区**：服务国内客户，数据存阿里云/腾讯云，满足PIPL境内存储要求。
- **欧盟区**：服务欧盟客户，数据存欧盟本地云，满足GDPR要求。
- **控制层（认证/登录/系统配置）**：不涉及具体客户数据，可以全球共用一套。

**明确说明范围**：这条只确定"要不要分区、怎么分区"这个大方向，不是选定具体云厂商/具体机房城市——具体选型留到真正部署时再定，不违反第27条"不提前做没有真实需求的事"，因为"分不分区"这个架构决定影响所有依赖数据存放位置的下游设计（如F-STORAGE-001/F-CRED-001/F-OBS-001的具体部署方案），必须先定大方向，细节可以后延。

**回填**：F-CRED-001附录里那条"待办"标记为已解决，指向本条。

Sources：[China PIPL Compliance: Regional Data Planes Guide](https://airbyte.com/data-engineering-resources/china-pipl-compliance-regional-data-planes)；[China PIPL Compliance 2026: Essential Guide for Foreign Businesses](https://sesamedisk.com/china-pipl-compliance-2026/)；[GDPR vs PIPL: EU and China Data Privacy Laws Compared (2026)](https://www.recordinglaw.com/world-laws/world-data-privacy-laws/gdpr-vs-pipl/)

### 41. AI员工指令交互模式——参照OpenAI ChatGPT Work模式，不做纯聊天驱动（2026-09-02 确认）

**背景**：用户指出一个真实缺口——Matbox已经设计了大脑路由/任务运行时/动作网关/控制台这套后端管道，但从来没设计过"人类怎么把想法变成AI员工能接收的指令"这一步的前端交互模式。专项00f报告深挖后，查到一条关键的真实行业案例。

**查证结论**：OpenAI于2026年7月9日推出"Work"模式——一个跟"聊天模式""Codex编程模式"并列的**独立模式**，不是聊天的升级版：能真正连接外部工具（走插件目录）、能定时/按计划跑任务、能持续跟进一个多步骤项目好几个小时、最后交付一个真正做完的东西（表格/幻灯片/报告/小网站），而不是聊天回复。**2026年8月，OpenAI直接关闭了老的纯聊天驱动的"Agent Mode"**，证明"纯聊天界面驱动自主任务"这条路已经被行业头部玩家自己放弃。

**确认的方向（用户已同意）**：Matbox的AI员工指令交互，参照OpenAI Work模式的结构——**不做纯聊天驱动自主任务**，而是"结构化任务输入（可带附件/参数）+ 明确的项目边界（有开始有结束）+ 持续可跟踪的执行过程 + 交付真正的成果物"这套模式，跟Matbox已经设计的F-TASK-001（任务有明确的暂停/恢复/接管状态机）方向本来就一致，现在正式把这条选型依据钉死。

**一并记录的反面教训**：OpenAI这次砍掉Agent Mode"没有提前通知、没有过渡期、没有迁移指南"，是真实的负面案例——Matbox以后如果要调整AI员工的交互方式，必须给已经在用的客户留过渡期，不能突然切断。

**待办**：按专项00f报告里指出的具体接口缺口（F-ORCHESTRATOR-001的RoutingRequest缺附件字段、F-TASK-001的contextSnapshotRef内部结构不清楚、缺"用户手动指定AI员工跳过Classifier"的字段、缺"任务中途追加轻量指令"的API），需要回填到对应正式开发文档，见下方紧接的执行记录。

Sources：[Matbox_专项00f_AI员工指令交互流程设计_2026-09-02.md](技术选型报告/Matbox_专项00f_AI员工指令交互流程设计_2026-09-02.md)；[ChatGPT Agent Mode Is Gone: What to Use Instead](https://www.usecarly.com/blog/chatgpt-agent-mode/)；[ChatGPT Work, Explained: OpenAI's New Agent Mode (August 2026)](https://aitoolsreview.co.uk/insights/chatgpt-work)

### 42. 27道产品/商业定义问题——完整确认记录（2026-09-02）

**背景**：用户要求把积压的产品/商业定义类疑问一次性列全、逐条查证后再问，每题按"题目+选项+我的选择（已经查过资料）"格式过了一遍。以下是全部27条的最终确认结果，部分已在其他专项条目单独展开（标注指向），其余的需要在后续批次里回填到对应正式开发文档。

| # | 问题 | 确认结果 |
|---|---|---|
| 1 | 雇佣中心能否从零自建 | 模板为主，同时支持高级用户从零自建 |
| 2 | 质检评分标准谁定 | Matbox给默认标准，企业可在此基础上调整 |
| 3 | 业务流配置归属 | 默认企业私有，可自愿选择贡献给平台 |
| 4 | 模板库归属 | 平台公共库+租户私有库并存 |
| 5 | 收费模式 | 混合模式（基础订阅含额度+超额另收） |
| 6 | 大模型成本转嫁 | 部分打包进订阅费+超额部分接近成本价透传 |
| 7 | 独立站托管模式 | 两种都支持（Matbox托管/客户自管），客户选 |
| 8 | 独立站包时间表 | 已备好，优先完成AI员工整块后再给 |
| 9 | 其他三个包时间表 | 近期能给 |
| 10 | 审批人是否必须真人 | 视风险等级而定，高风险必须真人（欧盟AI法案第14条硬性要求） |
| 11 | AI员工辞退流程可见性 | 租户管理员能看到完整流程 |
| 12 | 团队规模 | 仍是2人 |
| 13 | 服务器地区 | 中国区+欧盟区混合架构，见第40条 |
| 14 | 内部先用（dogfooding） | 会先内部验证再给客户用 |
| 15 | 是否已有试点客户 | 已有意向/试点客户 |
| 16 | 数据迁移需求 | 作为可选功能规划，不做v1必需 |
| 17 | 界面语言 | 中英文同步做（用户已有现成语言开发包，AI员工整块完成后提供） |
| 18 | 业务流配置同租户复用 | 能，一次配置可供同租户多个AI员工复用 |
| 19 | 批次5剩余专项优先级 | #20（错误码规范）现在做；#22/24/25/26等AI员工+独立站包到位后一起做 |
| 20 | 用户指出的盲区 | 引出两个新专项：AI员工呈现方式（见第00e/43条）、AI员工指令交互流程（见第00f/41条） |
| 21 | 训练前后对比界面 | 需要，复用OBS-F007基线/当前分数数据 |
| 22 | 管理界面品牌定制 | 只有部分页面需要（登录页/导航栏Logo配色），非完整白标 |
| 23 | 已知竞争对手 | 已查明，见第00h号报告（三维家/尚品宅配/酷家乐/Lateua等） |
| 24 | 目标客户规模 | 两者都要，优先中小企业，见第00g号报告深度验证 |
| 25 | 业务流配置人工介入 | 简单场景自助，复杂场景可付费请Matbox顾问介入 |
| 26 | 预算硬性暂停开关 | 见第39条 |
| 27 | AI产出责任归属 | 见第39条 |

**回填到正式开发文档的项（2026-09-02已全部完成，除#16）**：
- ✅ #1/#4 → F-CONSOLE-001 CONSOLE-F001（雇佣中心：模板雇佣+从零自建两种路径）+ F-AIEMP-001 EmployeeTemplate（visibilityScope/ownerTenantId/contributedFromTenantId字段）
- ✅ #2 → F-CONSOLE-001 CONSOLE-F003（默认评分rubric+企业可调整机制）
- ✅ #3/#18 → F-AIEMP-001 EmployeeTemplate（归属/复用范围字段，同租户多AI员工复用同一SOP/PolicyBundle）
- ✅ #5/#6 → F-COST-001 COST-F005（混合计费模式：订阅额度+超额近成本价透传，Entitlement新增overagePricingVersion/overageBillingMode字段）
- ✅ #10 → F-APPROVAL-001（新增1.1节风险等级对应审批人规则表，riskLevel 4-5强制真人审批为P0，AiApproval新增riskLevel/decidedByType字段）
- ✅ #11 → F-AIEMP-001 AIEMP-F005（辞退流程对租户管理员完整可见，非静默状态翻转）
- #16 → 新增数据导入Feature（暂无对应模块，仍需在Stage 10前确定归属哪个专项，非机械回填，需要新的范围界定，未完成）
- ✅ #21 → F-CONSOLE-001 CONSOLE-F003（训练前后对比视图，复用OBS-F007基线/当前分数）
- ✅ #22 → F-CHANNEL-001（新增4.2.1节，区分客户端完整白标 vs 管理控制台局部定制——登录页/导航栏Logo配色）
- ✅ #25 → F-CONSOLE-001 CONSOLE-F002（简单场景自助+复杂场景付费顾问介入两档，顾问代配置需在审计记录里可区分来源）

### 43. AI员工呈现方式——采纳"具名+头像+状态"，不采纳完全拟人化的主动消息模式（2026-09-02 确认）

**背景**：用户提出Matbox的AI员工"呈现方式要像真实同事"，举了yetone/cumora（"AI是团队里的一等公民"这一理念的真实开源项目，3385星）为例，但明确要求充分论证不能直接照抄。专项00e报告完成深度评估。

**查证结论**：cumora本身是真实项目，源码里有真实生产事故复盘记录（限流风暴/竞态条件/模型漂移），不是demo；但技术栈是TypeScript/Node，跟Matbox"Java单语言栈"原则冲突；只有消费级工作区隔离，没有企业级租户治理（无SSO、无跨团队RBAC）；社区验证薄弱（HN仅2条帖子3个点赞0评论）。**更关键的是查到一条实证反面证据**：BCG Henderson Institute的严肃研究（发表于哈佛商业评论，调研1200+名HR/财务从业者）发现，把AI包装成"有名字的员工"这种呈现方式会**实测削弱人类监督质量**：错误发现率降18%，个人责任感降9个百分点，"甩锅给AI"的情况升8个百分点——这条证据直接与Matbox已冻结的F-APPROVAL-001/F-SUPERVISOR-001"人工必须实时监督"设计原则相冲突。

**确认的方向（用户已同意）**：不采纳cumora完整的"AI在共享聊天室主动发消息、AI之间互相私聊"这种深度拟人化模式；**只采纳成熟、低风险的一半**——每个AI员工有具名+头像+简短人设介绍+简单状态指示（空闲/工作中/等待审批/离线），这是Salesforce Agentforce/Sierra已经在做的行业标配，且Matbox现有AIEMP-F001模板schema已经有数据支撑，不需要额外大量开发。"AI主动发消息""AI互相私聊"等更激进的拟人化交互模式，标记为🟡记录不紧急，等真实客户使用数据和监督体系更成熟后再评估——与专项27记忆系统同样的"无真实场景不锁定"处理方式。

Sources：[Matbox_专项00e_AI员工呈现方式与cumora专项评估_2026-09-02.md](技术选型报告/Matbox_专项00e_AI员工呈现方式与cumora专项评估_2026-09-02.md)

### 44. 不做"真实同事数字分身"功能——同意机制门槛在实际业务中大概率过不去（2026-09-02 确认）

**背景**：专项00j深度评估titanwings/distilly（"同事Skill"，把真实员工的工作习惯/说话方式/决策逻辑整理成AI可用的Skill）时发现，该项目对被采集数据的员工本人**没有做同意确认机制**——即便其自身论文明确写了"负责任部署需要显式同意"，代码库里这块仍是空的（未合并的PR #136），且与PIPL的个人信息处理同意原则存在真实冲突。当时提议"Matbox若做类似功能，必须先加同意机制"，交由用户拍板。

**用户判断（已确认）**：**不是"加同意机制"，是这个功能方向本身不做**——理由是真实场景下"同事基本不会同意"自己被这样整理成AI数字分身，同意门槛在业务上大概率通不过，做了同意机制也大概率收集不到员工愿意授权的真实数据，功能本身在Matbox的企业客户场景里落不了地。

**结论**：Matbox不规划"把某个真实员工的工作方式蒸馏成AI员工数字分身"这类功能，此结论适用于所有涉及员工个人数据画像/风格克隆的产品提案，AI员工训练/反馈闭环（CONSOLE-F002）仍按已有设计走"业务规则/SOP修正"路线，不涉及对具体自然人的行为画像采集。

Sources：[Matbox_专项00j_Distilly与AgentTeams专项评估_2026-09-02.md](技术选型报告/Matbox_专项00j_Distilly与AgentTeams专项评估_2026-09-02.md)

### 45. "架构地图"统一功能全生命周期跟踪——采纳"组织数字孪生"作为概念范围，关系层自建，不采购/搬用现成产品（2026-09-02 确认）

**背景**：用户提出要把Matbox散落的"开发架构图/生产架构图/质检图/维护图"统一成一张"架构地图"，覆盖功能从预开发到下线的完整生命周期，同一FeatureID贯穿全程，看板必须直接读地图数据、不能另外人工维护。要求先查证全球是否有成熟先例，不能凭印象设计。

**查证结论（多信源交叉验证）**：
- 全球确有对应的正式品类：Gartner 2026年**首次**发布《组织数字孪生（Digital Twin of an Organization）平台》魔力象限，评级"Transformational"，定义正是"业务战略+业务流程+技术架构+治理+实际运营"连成一个动态模型——与用户最初提出的14个领域、全生命周期范围基本吻合，证明范围本身没有设想过大。SAP为该象限Leader，Ardoq/LeanIX等为主要玩家，Ardoq有埃克森美孚、Riot Games等真实付费客户（非概念产品）。
- 关系记录层的全球通行做法是**图结构存储+视图自动生成**（Ardoq真实生产案例：图数据库映射2000+系统集成关系，自动识别单点故障，可用率做到99.9%），而非人工手画图/手改Markdown——这是Matbox现有`Feature Register.md`+手写HTML关系图与全球实践的真实差距。
- 真实用户评价（G2/Gartner Peer Insights）显示没有完美方案：Ardoq图能力强但"与第三方系统对接几乎为零"；LeanIX易上手但"界面差、新用户找不到信息"——任何选型都有真实代价，不存在十全十美的现成工具。
- Multi-Agent/Graph Engineering/MCP三个用户在社交媒体看到的词核实后：Graph Engineering是2026年才出现、尚未统一定名的新提法，但背后"系统拓扑作为可版本化产物+运行时用graph_id/run_id串联追踪"的做法是真实的，且指出关系层需要区分"静态设计关系"和"动态运行轨迹"两种不同的边；MCP连Backstage官方自己都说"高度实验阶段"，先记录不纳入现阶段设计；Multi-Agent不是新技术，是关系层本该覆盖的对象之一（AI员工间委派关系）。

**Build vs Buy 结论（已确认，不采购）**：查了三个真实候选——
- Ardoq/LeanIX：企业级定价（询价制，年费普遍数万美元起），面向"已有几千员工/几百系统"的大企业治理场景，且真实差评显示第三方系统对接能力弱，与Matbox自身开发流程融合困难。
- Backstage（开源免费）：真实成本数据显示自建一套年综合成本20万-120万美元，需2-4名全职工程师3-6个月起步，行业公认"20人以上工程团队或30个以上微服务规模才划算"，且原生不认识FeatureID/AI员工等Matbox专有概念，每加一种新实体官方参考工时40-60小时，后续还要占团队30%-40%时间维护。
- **两者的成本本质，是在为"把通用工具硬塞进一堆已经成型、各自为政的老系统"这件事买单**——这个问题Matbox现在不存在（尚未开发）。参照Boehm变更成本曲线（1-10-100法则，方向性结论至今仍有效）：同一设计决策，开发前改的成本远低于开发后/上线后改的成本，且"治理类设计没法事后补"是行业公认原则。

**确认的方向**：
1. "架构地图"以"组织数字孪生"的范围（业务目标→流程→架构→治理→运营，全生命周期同一ID贯穿）作为概念边界，此范围已获全球最新分析报告验证，不算设想过大。
2. 不采购/迁移到Backstage、Ardoq、LeanIX等任何现成产品——仅作REFERENCE（借鉴图结构存关系、视图自动生成等设计思路），关系记录层由Matbox自建，从系统设计阶段就原生支持，不做成事后外挂的通用工具。
3. 关系记录层需区分"静态设计关系"（模块间依赖）与"动态运行轨迹"（AI员工任务实际执行路径），两者存储和查询方式不同，不能混为一谈。
4. 具体的数据模型/存储引擎、"架构地图的定义和边界"细节（目标/最终产出/验收标准）**尚未确认，正在逐项讨论中**，不视为本条已解决。

Sources：Gartner Digital Twin of an Organization Magic Quadrant (2026)、Ardoq/LeanIX Gartner Peer Insights与G2真实评价、Roadie「The True Cost of Self-Hosting Backstage」、TrueFoundry「Graph Engineering for Multi-Agent Systems」、Backstage官方MCP文档（ai/mcp-actions）、Boehm Cost of Change Curve相关综述

### 46. 架构地图画布/详情页分工确认：画布只显示结构，详情必须弹窗；自动检测频率按风险分级，且需区分"开发中检测"与"开发完检测"（2026-09-02 确认）

**背景**：架构地图的呈现层要不要在画布上直接铺开发文档/验收标准这类大段文字，此前多轮讨论中被否决过（"看不懂/完全隔离/只解决一对分组"等），但结论一直没有正式落笔记录。用户本轮明确要求核实"详情弹窗里要不要带自动检测节奏"这一点，并给出查证结论。

**确认的方向**：
1. **画布/详情分工**：画布（关系层的可视化呈现）任何缩放层级下都只显示"结构"——组名、模块名，不显示开发文档/验收标准等大段文字；点击任意节点弹出独立详情页（即"功能档案"）才展示完整内容。这与Kiali节点详情图、Neo4j Bloom选中查看邻居、Datadog服务地图等已核实的真实工具做法一致，不需要另外验证。
2. **自动检测频率按风险分级，不是固定数字**：检测节奏取决于"这个模块出问题的后果有多严重"——P0级关键模块（密钥管理、租户隔离等）检测要勤（如每小时），一般模块每天一次即可，与Matbox已有的"高风险动作真人审批、低风险放宽"分级思路一致，不是新原则，是同一原则在检测节奏上的延伸。
3. **开发中检测 ≠ 开发完检测，是两套不同机制**：开发中检测＝Matbox已设计好的F-DQ-001~013质检Gate流水线（事件触发，每次代码变更时跑，拦截不合格的合并）；开发完检测＝功能上线后的生产漂移/健康监控（计划触发，按第2条的风险分级节奏跑）。**后者现在只能占位**，Matbox尚未进入Stage 10真实部署阶段，没有真实运行系统可供检测，功能档案的"自动检测状态"区块先标注"未接入，等Stage 10"，不得编造检测结果充数。

**结论**：以上三条并入"架构地图"设计——档案层的详情弹窗新增"自动检测状态"区块（P0/一般两档节奏 + 开发中/开发完两种机制并列展示，当前后者为占位），画布本身的显示规则（只显示结构、不显示文字）确认为定案，不再重新论证。

Sources：本轮用户核实的Kiali/Neo4j Bloom/Datadog真实产品实现 + 风险分级监控频率的通用SRE实践（与Matbox已有分级审批原则一致）

### 47. 架构地图最顶层结构确认："N+1"（前端功能入口 + 0底层/来源），数量数据驱动不写死，上下布局（2026-09-02 确认）

**背景**：用户手绘多版草图（先左右布局，后改上下布局），逐轮确认架构地图最外层、第一眼看到的结构该长什么样。

**确认的方向**：
1. **最顶层是"N+1"结构，不是"6+1"写死**：上面是前端呈现层的真实功能入口（当前6个：1首页、2情绪版、3AI工作台、4独立站/移动端管理、5AI员工管理中心、6后台管理），下面是"0 底层"这一个共同节点。这N个功能来自一个列表数据，不是画死在画布代码里的固定形状——以后变成7个、8个，只是列表加一条，画布自动重新计算布局，不改代码。列表长到一排放不下时需要自动换行规则，细节留待以后设计。
2. **布局方向定为上下，不是左右**：N个功能横排在上，全部连线汇聚到中间下方"0 底层"这一个节点。理由：全球软件架构图通用画法是"基础设施在下、功能在上"（分层架构图/N层架构的通用惯例），比左右布局更不容易被误读成有先后顺序的流程图。
3. **"0 底层"＝"来源"，是同一个东西的两个叫法**：所有前端功能背后用的能力和数据，都从这一层取，不是每个功能各自另起一套。
4. **"0 底层"内部结构复用已有设计，不重新做**：点开"0 底层"钻进去，就是`architecture_dependency_diagram.html`里已经做好的那5个组（消费层/平台核心共享层/AI员工执行体系/多端渲染/架构底层）+23条真实关系——这一层的已有数据/设计全部保留，属于"0 底层"节点内部的内容，不是跟顶层N个功能平级的东西。

**未解决问题（明确标注，不能当作已确认）**：
- 内部5组里有一个组本身也叫"架构底层"，跟顶层新加的"0 底层/来源"重名容易混淆——已提出，用户当时喊停，还没确认要不要改名、改成什么。
- "0 底层"内部具体怎么弄（这里指关系层再往下的具体设计细节）——用户明确说"等会讨论"，本条不涉及，留到后续单独讨论。

Sources：本轮与用户逐版手绘草图确认过程，无新增外部技术查证（纯布局/命名判断，不涉及新工具/新技术选型）

### 48. 架构地图画布细则确认：功能位置可变、身份证不变；颜色色卡；"外面必须看到全部关联"用集成点+层级边捆绑实现，不靠分层隐藏（2026-09-02 确认）

**背景**：延续第47条，用户对"世界坐标只算一次"等表述逐句纠正，并明确提出"我不允许分层"、"我要在外面就看到所有的关联"——这与之前多轮被否决的"完全隔离/分组隐藏"是同一类问题，要求先深度查证再回答，不能凭经验判断。

**确认的方向**：
1. **术语与身份**：统一叫"功能"，不叫"东西"。**位置不是算一次就定死，位置可以随时因为需要调整/移动；不变的是每个功能唯一的"身份证"（FeatureID）**——位置是当前的显示状态，身份证才是永久不变的识别依据，两者不能混为一谈。
2. **颜色色卡（按真实通用惯例RAG+行业惯例定的，不是自创配色）**：灰=未开始/已下线，蓝=开发中，黄=质检中/验收中（需要关注），绿=已发布且正常运行，红=报错/故障/验收不通过（**优先级最高，不管当前处于哪个阶段，只要有故障就盖过其他颜色变红**），维护中单独给一个颜色（紫或橙，具体待定）。
3. **详情页具体内容需要重新定**——用户已提出但明确说"等会讨论"，本条只记录"待重新定义"这个事实，不涉及具体内容。
4. **"在外面看到所有关联"的可行性，查证后确认可行，方案是"集成点+层级边捆绑"，不是分层隐藏**：
   - 已查证：200-300个节点的原始连线摊平在一张图上，全球没有真实做成过可读的案例，2026年微服务依赖图研究明确写"普遍不可读"，与更早查过的Obsidian Graph View真实失败案例结论一致——这条路径本身不可行，不是主观判断。
   - 已查证：企业集成领域的真实证据——N个系统互相直连会产生N²-N条连线（"意大利面"问题），走"集成点/中枢"模式能把连线数量压回到N条左右，是真实的工程共识（不是本次讨论现造的说法）。
   - 已查证：存在"层级边捆绑（Hierarchical Edge Bundling）"这一真实学术/工业界技术（Holten，IEEE），能让同路径的关系线自动合并成一股弯曲的线，密集处不显乱，且**每一条真实关系都画在图上、不需要点击才存在**——满足"外面必须全看到"的要求，同时不违反可读性上限。
   - **结论（用户已确认"可以"）**：最外层图不靠"点开才看到关系"的分层隐藏解决可读性问题，而是"集成点压缩连线数量 + 层级边捆绑让密集处自动收拢"两者叠加，关系不藏、不用点击，只是画法上处理拥挤度。

**未解决问题**：
- "6个集成点"具体是哪6个模块、对应架构地图现有5组23条关系里的哪些节点——本条只确认了"集成点+边捆绑"这套方案本身可行，集成点的具体名单还没有逐条跟用户确认过（此前提出"平台核心共享层"的模块可能是候选，但用户没有明确认领这个猜测）。
- 维护中的具体颜色（紫/橙二选一）未定。
- 内部5组之一同名"架构底层"与顶层"0底层"的命名冲突（第47条遗留）仍未解决。

Sources：[IEEE Hierarchical Edge Bundling (Holten)](https://ieeexplore.ieee.org/document/4015425/)、[2026年微服务依赖图可读性研究](https://arxiv.org/pdf/2407.16873)、[企业集成Hub-and-Spoke真实证据](https://argondigital.com/blog/general/ipaas-vs-spaghetti/)、[Dashboard状态色RAG通用惯例](https://www.mastt.com/blogs/project-rag-status-dashboard)、[ArgoCD真实健康状态色](https://oneuptime.com/blog/post/2026-02-26-argocd-health-status-icons-ui/view)

### 49. "集成点"就是顶层的N个前端功能本身，不是另一套内部模块；数量同样不写死（2026-09-02 确认）

**背景**：第48条留了一个未解决问题——"6个集成点具体是哪6个模块"，当时的猜测（平台核心共享层的模块）用户没有认领。本轮用户直接给出答案。

**确认的方向**：
1. **集成点＝顶层的N个前端功能本身**（1首页、2情绪版、3AI工作台、4独立站/移动端管理、5AI员工管理中心、6后台管理），不是另外在"0底层"内部找一套单独的集成点模块。不管"0底层"内部以后涨到多少个功能（未来可能到200个），所有内部功能最终都能归到"它服务的是这N个顶层功能里的哪一个/哪几个"，不需要内部两个深埋的功能之间硬拉直连线。
2. **集成点的数量同样不写死**：跟第47条"N+1"是同一个原则——现在6个，以后可能变7个、8个，看的是前端真实功能入口有没有变化。集成点数量和"0底层"内部功能数量（未来~200个）是两件独立的事，不会因为内部功能涨到200个，集成点也跟着涨到200个——这正是它能把连线数量压住的原因。

**结论**：第48条"6个集成点具体是哪6个"这一未解决问题已解决——不是内部模块，就是顶层的N个前端功能本身身兼两职（既是功能入口，也是集成点）。

Sources：本轮用户直接确认，无新增外部技术查证（澄清此前猜测有误，属于理解修正）

### 50. 功能状态标准确认：8个阶段 + "阶段/健康状态"双字段分离，全部由系统事件自动驱动，不允许人工手填（2026-09-02 确认）

**背景**：延续颜色色卡（第48条）的讨论，逐步确认功能从"无"到"下线"完整经过哪些状态，用户中途指出遗漏了"开发完"之后安全/质检要持续跟踪，又追问"运行中"这一个词是否精确，最终要求查证"阶段"与"健康状态"该不该分开记录、该不该允许人工填写。

**确认的方向**：
1. **8个顺序阶段**（从无到下线）：0未立项（无FeatureID）→1预开发→2开发中→3质检中（DQ Gate一次性关卡）→4验收中→5已发布→6运行中→7已下线。已查证与全球通用软件生命周期（规划→设计→实现→测试→部署→维护→下线）及ITIL服务生命周期（战略→设计→过渡→运营→持续改进）的形状一致，不是自造的分期。
2. **"运行中"不是终点，其上持续挂着四项非一次性的跟踪项**：安全（持续安全监控，不是发布前测一次）、质检（上线后持续质量检测，区别于第3步的一次性DQ Gate）、报错、维护。已查证2026年DevSecOps行业共识——"安全不能只在部署前测一次"，真实数据：漏洞拖几周平均损失440万美元（IBM 2025报告），持续安全监控可让生产漏洞减少50%-70%——证明这条不是可做可不做的细节。
3. **"阶段"与"健康状态"必须拆成两个独立字段，不能塞进"运行中"这一个词里**：阶段回答"生命周期走到哪一段"（改变靠流程节点推进，例如验收通过才跳下一阶段）；健康状态回答"这一刻好不好"（正常/故障/维护中/待关注，随时可能变，与阶段各自独立）。颜色显示按健康状态决定，不是按阶段决定，故障优先级最高、盖过其他颜色（沿用第48条）。已查证真实先例：Kubernetes官方将Pod的"Phase"（粗粒度生命周期）与"Conditions"（可并存的独立细粒度检查项）分开设计，官方原话"Pod可能处于Running阶段但还没准备好对外服务"；ArgoCD将"同步状态"（是否跟Git一致）与"健康状态"（是否真的在正常运行）设计成两个完全独立字段，官方原话"一个应用可能已同步但不健康，也可能没同步但很健康"——两个当前全球主流运维工具都验证了这个拆分是必要的，不是本次讨论现造的说法。
4. **两个字段必须由系统事件自动驱动，禁止人工手动填写**：阶段字段由RC5已有的流程节点触发写入（FeatureID分配、DQ Gate通过、验收审批通过、发版事件等，均为已设计好的真实触发点）；健康状态字段由检测系统触发写入（DQ Gate失败→故障，生产监控告警→故障，对应第46条已确认的开发中检测/开发完检测两套机制）。

**未解决问题**：
- "维护中"这一项健康状态，目前查证Matbox现有文档没有一个"维护工单"类的实体/事件源可以自动触发（唯一搜到的"运维工单"只是Flyway案例里的统计数字，不是Matbox自己设计的功能）——若不补，"维护中"只能人工改，与第4条矛盾。用户表示"我们有个AI平台基础已经做好了"，会提供资料，本条待用户提供的资料确认后再处理，不视为已解决。

Sources：[Kubernetes Pod Conditions官方文档](https://kubernetes.io/docs/concepts/workloads/pods/pod-condition/)、[ArgoCD Sync Status vs Health Status](https://oneuptime.com/blog/post/2026-02-26-argocd-sync-status-vs-health-status/view)、[DevSecOps持续安全监控2026行业共识](https://devops.com/continuous-security-in-devsecops-moving-beyond-one-time-testing/)、[软件产品生命周期通用阶段](https://leanware.co/insights/software-product-lifecycle-guide)、[ITIL服务生命周期5阶段](https://invgate.com/itsm/itil/itil-service-lifecycle)

### 51. 功能卡片最终呈现确认：名字+编号+颜色+细分状态，全部不点不悬停直接可见；细分状态按阶段分组细化（2026-09-02 确认）

**背景**：延续第50条"阶段/健康状态"双字段设计，讨论卡片具体呈现什么内容。中途查证了"悬停看一眼、点击看全部"的两层展开做法（2026年仪表盘设计真实趋势），但用户明确否决悬停这一层。

**确认的方向**：
1. **细分状态按阶段分组，不是所有阶段共用一套**：开发前组、开发中组、运行中组，各自有自己专属的一套细分状态词汇——已查证真实先例（Jira的Status+子状态自定义字段、Azure DevOps的State+Reason两层字段），是成熟做法，不是本次自创。
2. **卡片直接显示四样东西，不用点、不用悬停**：名字、唯一编号（FeatureID）、颜色（第48/50条定的健康状态色）、细分状态文字（如"开发中·等待Review"）——**用户明确否决"悬停才显示细分状态"的方案**，理由是"一目了然"这条从最早就坚持的原则不能退让。细分状态是短文字标签，不算"大段文字"，不违反第46条"画布不显示大段文字"的规则。
3. 具体每个阶段组下面有哪几个细分状态、对应什么颜色——**尚未定，用户随后要求"颜色再细化"，留到下一步具体讨论**。

Sources：[2026仪表盘"渐进式展开"设计趋势](https://www.aufaitux.com/blog/dashboard-design-examples-inspiration-best-practices/)（悬停层被否决，仅作为已查证但未采纳的选项记录）

### 52. 细分状态与颜色最终确认：13个细分状态随阶段动态切换，最终色卡定案（2026-09-02 确认）

**背景**：延续第50/51条，深度查证（用户明确要求"非常正式的查"）细分状态的数量和真实先例，发现原12个状态偏多、"开发前组"3个状态无真实先例、"运行中组"应采用真实SaaS状态页5值标准且维护中真实惯例色是蓝色（与已占用的"开发中＝蓝"冲突）。经讨论确认最终方案。

**确认的方向**：
1. **细分状态不是按"组"扁平罗列，是随当前阶段动态切换**——卡片任何时候只显示当前阶段对应的那一小撮细分状态，不会同时显示别的阶段的细分状态：
   - 预开发阶段：设计中 / 待开发
   - 开发中阶段：编码中 / 被阻塞
   - 质检中阶段：检测中 / 检测未通过
   - 验收中阶段：待审批 / 已驳回
   - 运行中阶段（已发布+运行中共用，采用查证到的全球SaaS状态页真实5值标准）：正常 / 降级（能用但不稳定）/ 部分故障 / 严重故障 / 维护中
   - 共计13个细分状态，分5组，每组2-5个，均未超过"单个流程别超过7-8个状态"这条经验线（因为任何时刻只展示其中一组，不是13个同时摊开）。
   - 如实记录：预开发组的2个状态没有查到确切对应的全球现成叫法，是按Definition of Ready的一般思路推出来的，不是照抄查证到的标准；运行中组的5值是有真实标准依据的（SaaS状态页通用做法）。
2. **最终色卡定案**：灰＝未开始/已下线，橙＝开发中，黄＝降级/质检验收异常，绿＝正常运行，红＝故障（优先级最高，盖过其他颜色），蓝＝维护中。已查证"开发中＝蓝"（CI/CD流水线惯例，9+真实工具采用）与"维护中＝蓝"（SaaS状态页惯例）两条真实但互斥的惯例，用户选择保留"维护中＝蓝"，开发中改用橙色（同样有真实依据：GitLab的pending/进行中状态用橙色）。
3. **开发中组的展示状态是翻译过的人话，不是直接照搬RC5内部WorkPackage技术枚举**（呼应第50条"禁止人工填写但要区分内部技术状态与外部展示状态"的查证发现）——RC5原有的ACTIVE/BLOCKED/REBASE_REQUIRED/SUPERSEDED/CANCELLED/MERGED仍是内部真实驱动源，卡片上的"编码中/被阻塞"是这些内部状态映射出来的展示层用语，不是新建一套独立枚举替代RC5。

Sources：[Jira状态数量最佳实践（7-8个上限）](https://community.atlassian.com/forums/Jira-questions/Jira-statuses-best-practices/qaq-p/2933359)、[Definition of Ready（Atlassian）](https://www.atlassian.com/agile/project-management/definition-of-ready)、[SaaS状态页真实5值标准](https://uptimerobot.com/knowledge-hub/monitoring/building-a-status-page-ultimate-guide/)、[CI/CD构建状态颜色惯例（GitHub Actions/GitLab CI等9+工具）](https://gitlab.com/gitlab-org/gitlab-foss/-/work_items/20430)、[Carbon Design System状态指示器](https://carbondesignsystem.com/patterns/status-indicator-pattern/)

### 53. 功能卡片最终定案：4样东西——名字、唯一编号、状态标签（颜色+文字合一）、关联数（2026-09-02 确认）

**背景**：延续第51条卡片呈现讨论，用户提出还要加一个"关联数"（比如"关联23"，代表这个功能真实连着多少个别的东西，不用数线/点开才知道），并纠正"颜色"和"细分状态文字"不是两个独立元素，是同一个东西（一个带颜色的状态标签，颜色即标签底色）。

**确认的方向**：卡片最终只有4样东西，不点不悬停全部直接可见：
1. **名字**
2. **唯一编号**（FeatureID，第50条已定的永久身份）
3. **状态标签**（颜色+细分状态文字合一，例如橙色底的"编码中"——颜色即第52条色卡，文字即第52条随阶段动态切换的细分状态，两者不是分开的两个视觉元素，是一个标签）
4. **关联数**（这个功能真实连着多少个别的东西，例如"关联23"——直接把数字摆出来，配合第48条"边捆绑"让密集连线自动收拢，数字给出精确值，不用去数图上的线）

Sources：本轮用户直接确认，无新增外部技术查证（卡片元素定案，纯设计判断）

### 54. 架构地图画布重做定案：真拖拽缩放+放射状聚簇+复用已验证卡片样式+关系包动画；"消费层"更名"应用层"（2026-09-03 确认）

**背景**：第45-53条定完设计后，实际实现的第一版严重偏离已确认的要求——用的还是"点击切场景"的旧机制（不是真画布）、5个组做成堆叠方块网格（不是"圆心+一圈成员"的放射状聚簇）、卡片信息飘在圆圈外面、关系线是纯装饰死线、间距计算有真实bug导致节点互相重叠。已写入memory `feedback_never_hide_content`记录根因（遇到复杂度就本能地退回"藏起来"这条被否决过的老路）。本条记录改完之后的最终定案。

**确认的方向**：
1. **真正的无限画布**：`<g id="viewport">`用`transform: translate() scale()`实现拖拽平移+滚轮缩放，用SVG原生`getScreenCTM()`精确换算屏幕坐标，不是手动估算像素；所有内容（顶层功能+5个聚簇+全部成员）永远同时存在于DOM里，没有任何`display:none`式的导航隐藏。
2. **放射状聚簇**：5个真实组（应用层/平台核心共享层/AI员工执行体系/多端渲染/架构底层，已有的NODE_GROUP数据）先在一个大圆上各占一个位置，每个组的真实成员再围着组的圆心摆成一圈——两层同心圆结构，不是网格堆叠。间距参数（大圆半径640、组内环半径最小130、顶层功能行与聚簇区之间的安全间隙）都是算过топ小聚簇（如只有2个模块的"应用层"）最高点不会撞到顶层功能行才定的，不是拍脑袋的数字。
3. **卡片改用项目内已有、已验证的技能样式**：不再自己设计（用户原话"你这个设计师丑到让人无语了"），改成照抄`.claude/skills/architecture-diagram`这个项目本地技能自带、已经用在9张流程演示图（如quality_gate_flow.html）里的`.node`方形卡片样式——左侧色条+名字+编号+右上角圆形角标，用`<foreignObject>`把真实HTML卡片嵌进SVG世界坐标，拖拽/缩放/关系线高亮机制完全不用改。尺寸比参考样式默认150px更紧凑（104px），适配架构地图最终200节点的规模。
4. **关系线加入真实的hover高亮+流动动画**：平时几乎隐形，碰到节点/直接碰线，它才变粗显色，同时沿线飞一个流动的"数据包"圆点（照抄同一个技能里`quality_gate_flow.html`已经验证过的`animateMotion`真实做法，区别只是这里悬停时`repeatCount="indefinite"`持续循环飞，不是"一步一次"）——不再是查不出任何信息的死线。
5. **"消费层"更名"应用层"**：查证确认"Consumer Layer"是The Open Group SOA参考架构里真实存在的术语，但原意是"外部使用方跟系统打交道的那层"，跟这里"质检/可视化调用底层共享平台能力"的实际含义不完全贴合，中文翻译"消费层"容易被误解成电商购物的"消费"。改用"应用层"（Application Layer）——查证到的真实企业架构资料里，"应用层建在共享的平台层之上，调用平台层能力"，跟这里的实际关系完全对应，中文也不需要额外解释。

**未解决问题**：Artifact发布链接（`claude.ai/code/artifact/...`）还停留在最早"5张分组卡片总览"那一版快照，本轮改动尚未同步过去，用户明确说"稍晚点"，留待后续处理。

Sources：[The Open Group SOA参考架构Consumer Layer](https://www.opengroup.org/soa/source-book/soa_refarch/p12.htm)（原始定义，链接已失效但搜索快照可查）、真实企业架构资料"应用层建于共享平台层之上"的表述、`.claude/skills/architecture-diagram/SKILL.md`及其`assets/template.html`/`quality_gate_flow.html`的真实.node卡片与animateMotion实现

### 55. 底层聚簇布局最终改为"一组一行"，否决圆形/放射状（2026-09-03 确认）

**背景**：第54条定的放射状聚簇（圆心+一圈成员）实际做出来后，用户明确否决："底层属于下面有十几个底层，共同支撑上面功能，不应该像这样一团出现，应该是规矩的拍成一排一排，你现在完全是乱排啊"。查证后改用行列布局。

**确认的方向**：
1. **5个组，一组一行**：每行最前面是组名标签（跟其他卡片同款式），组的真实成员横向排在这一行里，不再用圆形/放射状摆位。查证到三个独立真实来源都指向同一个做法：ArchiMate官方"分层视图"用Grouping把同层元素横向排在一起；泳道图（swimlane，Lucidchart/Miro/Visio等工具的标准做法）一行一个类别；AWS官方架构图规范明确写"按层/按服务分组、让每组在视觉上分开"。
2. **"0 底层"仍是单张卡片**（第54条已定），摆在5行正上方，用带箭头虚线连顶层功能、用细线连每一行的组标签，把"上面调用底层""底层由这5行构成"两层关系都画出来。
3. 过程中连带修了两个真实bug：连接线的SVG path漏写`fill:none`导致被浏览器默认涂黑、变成难看的黑色三角形；"0 底层"卡片纵坐标最初没有算过跟顶层功能卡片的真实高度，两者叠在了一起——这两条都已按真实卡片尺寸重新算过安全间隙修复。

Sources：ArchiMate官方Layered Viewpoint文档、泳道图（swimlane）通用做法（Lucidchart/Atlassian等）、AWS架构图设计规范"按层/服务分组"建议

### 56. 组级编号确认：不改动现有24个FeatureID，组另外单独定编号，关系靠NODE_GROUP数据记录，不拼进编号字符串（2026-09-03 确认）

**背景**：用户要求组卡片也要有"身份证"式的唯一编号，要求先查证全球做法。同时借这次机会查证了现有FeatureID命名格式（F-CRED-001）的真实来源——查了RC5标准原文（`RC5更新版-Matbox_新专项开发_全流程AI对话脚本与开发文档管理标准`），发现RC5只规定了FeatureID"必须唯一、只认长期存在的功能"这些治理规则，**从未规定过字母/数字具体怎么组成**——"F-CRED-001"这种格式是各模块文档自己定的，不是RC5统一算法；且发现真实存在的不一致：部分模块用"F-XXX-NNN"（F在前），部分模块用"XX-FNNN"（模块缩写在前，如F-DQ-001），两套写法混用，从未统一过（此项不一致本条不处理，只如实记录，留作已知问题）。

**查证结论（三个独立真实来源）**：
- 需求工程/航空航天领域：子编号＝父编号+自己的后缀（如"AppB 1.2.4"的子项是"AppB 1.2.4.1"），编号字符串本身携带父子关系。
- Backstage（真实软件目录系统）：子项（Component）自己的编号不变，另外用一个独立字段指向父级（如`system: payment-system`），关系是"记一个指针"，不改动子项自身编号。
- 航空航天BOM零件编号：号段本身分区间编码类别（如"4000s=平台"），也是把关系编进号里的路线。

**确认的方向（用户选择Backstage路线）**：
1. **现有24个FeatureID一个不改**——大规模改名代价是要牵动9份以上正式文档和Feature Register，且与RC5标准自身"关键基线变化时不篡改过去Baseline历史"的原则冲突。
2. **5个组各自单独定编号**：G-APP（应用层）、G-CORE（平台核心共享层）、G-AIEMP（AI员工执行体系）、G-RENDER（多端渲染与内容分发）、G-INFRA（架构底层）。G-前缀跟功能编号的F-前缀一眼区分开，不会混淆。
3. **"谁属于哪个组"这层父子关系，继续靠代码里已有的`NODE_GROUP`数据记录**，不把关系拼进编号字符串——这本来就是`architecture_dependency_diagram.html`里已经在用的做法，不用新造。
4. 组卡片的编号展示为"G-CORE · 7 个模块"这样，编号和成员数在同一行，都不截断（已实测36个节点+5个组全部验证无截断）。

Sources：本轮抓取RC5标准原文（`docs/RC5更新版-...docx`）核实FeatureID命名规则缺失与不一致、需求工程层级编号惯例、[Backstage Well-known Relations](https://backstage.io/docs/features/software-catalog/well-known-relations/)、航空航天BOM零件编号惯例

### 57. FeatureID格式彻底统一：DQ/VIZ的"XX-F0NN"改成"F-XX-0NN"，全仓库42个文件同步改名（2026-09-03 确认）

**背景**：第56条记录过一个已知不一致——DQ/VIZ模块内部多个功能用"DQ-F001~F013"/"VIZ-F001~F007"这种格式（模块缩写在前，因为一个模块装了多个功能），其余单功能模块用"F-CRED-001"这种格式（F在前）。当时给了用户两个选项：①彻底改名统一格式，代价是要动全仓库引用这些编号的文件；②不改，只把"为什么两种格式都对"这个道理正式记录成规矩。**用户明确选择①，原话"必须彻底改完，我这边没有凑合的事情"**，包括历史的"专项XX"技术选型报告也要求一起改，不接受只改"活文档"、留着历史记录不动的折中方案。

**执行前的真实范围核查**：先用Grep核实了实际影响范围——不是最初估计的"9份以上"，是**42个文件**都引用了这两个模块的编号，包括正式开发文档、Feature Register、架构地图本身、多份专项技术选型报告、和2份真实的流程演示图（`queue_retry_escalation_flow.html`/`release_traceability_flow.html`/`viz_repair_loop_flow.html`）。

**确认的方向**：
1. DQ模块20个真实功能编号（DQ-F001~F013，13个）和VIZ模块（VIZ-F001~F007，7个），统一改成"F-DQ-001"~"F-DQ-013"、"F-VIZ-001"~"F-VIZ-007"这种跟其余模块一致的"F-模块-序号"格式。范围表示法统一为"F-DQ-001~013"（不重复"F-DQ-"这个共同前缀两次）。
2. 用脚本在全部42个文件里做正则替换（范围格式的正则必须先于单个编号格式的正则执行，否则范围会被切成两半各自处理导致格式错乱），替换前用Grep核实了两种格式的全部真实写法，替换后再次用Grep核实**零残留**（没有任何文件还留着"DQ-F0NN"/"VIZ-F0NN"这种旧格式），一共784处替换。
3. 架构地图卡片上，DQ/VIZ的显示编号自动跟着统一：不再是特例，走跟其他模块完全一样的"组编号-模块编号-序号"拼接逻辑（如"G-APP-DQ-001~013"），不用给这两个模块单独写渲染分支。
4. 明确记录：这次改动**触碰了历史的"专项XX"技术选型报告**，这跟RC5标准自身"关键基线变化时不篡改过去Baseline历史"的原则是有张力的——用户在完全知情（已经把这条张力摆出来问过）的情况下，明确选择"必须彻底改完"，这是用户本人的决定，不是本次擅自决定推翻RC5原则。

Sources：本轮用户明确决策（"必须彻底改完，我这边没有凑合的事情"），执行前后均用Grep核实真实范围与零残留

## 开发中流程标准（2026-09-04，独立于任何单一模块，全平台通用）

> 起因：用户直接问"几十个AI同时开工怎么调度""Codex之前老是问些自己就能答的问题""开发中途要不要审核"，明确要求"绝对深度地去查询资料后再决定"。最初误把这三条写进了`quality_gate_flow.html`（DQ模块的范例页面），用户当场指出"你把总的这个规则写道质检里，这位置不对"——三条规则适用于所有约200个功能的开发过程，不是DQ一个模块的事，移到本文档统一记录。

### 58. 调度器——任务自认领+worktree隔离+运行时任务队列，判定应提前建成独立模块（2026-09-04 确认）

**查证结论**：2026年多AI协同的主流做法不是"人工一个个分配任务"，是自认领式——所有能做的WorkPackage摆在一张共享任务清单上，AI自己去认领，做完标记完成再回来领下一个，避免所有任务堵在一个调度员身上排队（Claude Code Agent Teams的真实做法）。防止互相打架靠两层：Git层用git worktree给每个AI分独立工作目录（GitHub Copilot 2026版"3个AI共用1仓库、各自worktree互不干扰"的真实做法）；运行时层再加一个任务队列，把抢数据库/端口/缓存这类操作排队执行，worktree隔离不管这层。规模上2026年真实产品已做到100-300个AI子代理并行，Matbox"几十个"这个量级，成熟做法都够用。

**确认的方向**：这套调度基础设施应该在AI真正大批量干活之前就建成待命状态，不等真要用了才现搭——Factory公司"Agent Readiness"分级方法论明确把调度/隔离/监控列为应提前成熟的基础设施。这跟RC5的WorkPackage 14项字段是对得上的：其中"并行调度结论(PARALLEL_READY/BLOCKED_BY_DEPENDENCY/...)"和"写入冲突"两个字段早就预留了这个问题的位置，只是现在还没有一个真正的调度器模块把它们用起来。**调度器单独立一个功能模块，跟DQ一样走一遍开发资料标准提前建好，不等Stage10真代码库来了才现想。**

Sources：[Inside Claude Code's Shared Task List — MindStudio](https://www.mindstudio.ai/blog/claude-code-agent-teams-shared-task-list)、[How to Use Git Worktrees for Parallel AI Agent Execution — Augment Code](https://www.augmentcode.com/guides/git-worktrees-parallel-ai-agent-execution)、[block/agent-task-queue](https://github.com/block/agent-task-queue)、[GitHub Copilot app: parallel agents, worktrees 2026](https://ecorpit.com/github-copilot-app-parallel-agents-worktrees-guide-2026/)、[Agent Readiness — Preparing a Repository for AI Agents](https://infragap.com/agent-readiness/)、[9 Open-Source Agent Orchestrators for AI Coding (2026)](https://www.augmentcode.com/tools/open-source-agent-orchestrators)

**2026-09-04 更正：不新建"调度器"模块，并入已有的 F-QUEUE-001**。上面"调度器单独立一个功能模块"这句话是没有先查现有能力目录就下的判断，违反了本文档第31条自己定的规矩（新东西动笔前必须先查有没有已经存在的模块，不能凭空重新定义）。实际核查[Matbox_任务队列_正式开发文档](Matbox_任务队列_正式开发文档_V1.0-RC.md)（F-QUEUE-001）发现：它的"当前定位"原文就写着"WorkPackage 怎么派发给 AI 员工、任务失败了怎么恢复、多个 AI 员工同时干活会不会打架"，引擎已选定Temporal（与F-TASK-001共用同一套实例），已有QUEUE-F001~F004（Temporal集成/冲突检测前置拦截/并发限额管理/失败恢复重试）。**本条查证到的"自认领机制/worktree隔离/运行时资源队列"，跟F-QUEUE-001管的是同一件事，只是颗粒度更细，应该作为新Feature（QUEUE-F005起）并入F-QUEUE-001现有的Feature Register，复用已选定的Temporal基础设施，不新建模块、不重新选型、不另起一份开发资料包。

### 59. AI该不该停下来问人——按"动作后果"四级分类，不按AI自报的把握度（2026-09-04 确认）

**查证结论**：Anthropic自己的研究发现AI任务越难，它自己主动"要不要跟人确认一下"的频率会翻倍，说明AI本身有能力自己判断，前提是判断标准设计对了。真正的问题出在标准本身——AI嘴上说的"把握度"不准（claimed 90%实际约75%对；一条任务3步都这样打折扣，最后全对的真实概率只剩约42%），不能拿它当依据。真正该用的标准是按动作后果分级，不是按AI的把握度分级：只读/查资料——完全自主不问；改草稿/可逆操作——自主做但留痕；碰外部系统/发消息——排队等一下不必立刻打断人；上线部署/删数据/改权限——必须问人没有例外。用户之前用Codex遇到的"几分钟断一次"，大概率是把本该属于前两类的判断也当成了最后一类去问人。

反常识但真实的一点：问得太频繁反而更危险——人被烦到会养成"看都不看就点同意"的反射动作，等真正该拦的高风险操作出现时人已经不认真看了，等于白设了卡点。具体减少乱问的办法：AI很多"打断"其实是不知道项目的构建命令、代码规范、能改哪不能改哪，猜错了才停下来问，这类问题提前写清楚就不用现场问。

**确认的方向**：改用"动作后果四级分类"替代"AI自报把握度"，四级的边界由WorkPackage已有的"允许修改范围/禁止修改范围"字段提前写死，AI照着读就知道要不要停，不用每次都现场问。

Sources：[Human-in-the-Loop Escalation Design for AI Agents 2026](https://www.digitalapplied.com/blog/human-in-the-loop-escalation-design-ai-agents-2026)、[Decoding the Configuration of AI Coding Agents](https://arxiv.org/html/2511.09268v2)

### 60. 开发中要不要审核——实时OPA拦截+单WorkPackage改动范围上限，两层并行，不推翻现有Gate流程（2026-09-04 确认）

**查证结论**：一项跟踪320万行代码、2500次真实审核的大样本研究（SmartBear/思科）发现，一次审核超过200-400行代码，能查出的问题会断崖式下降；审核人盯着看超过60-90分钟，专注力和准确率也会掉——控制在小段、勤审的方式查出率能到90%，比大段一次性审（平均55-60%）高很多。另有真实事故佐证"只在事后审"不够：2025年7月Replit的AI代理删过一次生产数据库，团队明确告诉它"代码冻结期不许动"，但这句话只存在于指令里，执行链路上没有任何东西真正拦住它。2026年真实产品（如AEGIS）的做法是给每一次工具调用都实时打分，允许/拦截/转人工三选一，是即时的，不是攒够了批量事后审。

**确认的方向**：两层一起要，不是二选一。第一层（实时，不用人）——AI每做一个动作（写文件、跑命令）就按"允许修改范围"当场自动拦一下，防的是"Replit式"灾难，技术上正好是DQ已选定的OPA Gate能干的活；第二层（人工Review，勤审）——每个WorkPackage的改动范围要控制小（对应200-400行这条真实数据），不能攒一大坨再审。这两层不推翻DQ模块已验证过的①②③Gate→Review→Merge流程（见[quality_gate_flow.html](quality_gate_flow.html)），是给全平台"开发中"这个阶段补一条具体量化标准（单个WorkPackage改动范围上限）和一层还没细化过的机制（实时工具调用拦截，区别于事后PR审查）。

Sources：[How to Review Code in 2026 — Cisco/SmartBear study](https://codeant.ai/blogs/code-review-process-guide)、[Replit AI agent deleted a production database — MintMCP](https://www.mintmcp.com/blog/replit-agent-production-database-deletion)、[Incident 1152 — AI Incident Database](https://incidentdatabase.ai/cite/1152/)、[AEGIS: Pre-Execution Firewall for AI Agents](https://arxiv.org/html/2603.12621v1)

**2026-09-04 补充确认：谁来审、是不是每个WorkPackage都要审——用户当场指出"每次检查完我来看，这不对吧"**。这条问得对，本条最初没写清楚"谁看"和"看多少"，容易被理解成用户本人要逐个WorkPackage看200-400行代码，跟已经定下的架构原则第12条矛盾（"独立Reviewer角色由团队里唯一懂开发的同事承担，只在真正需要人工判断的关键节点介入，尽量不打扰"——用户本人不懂开发，本来就不该是那个逐行看代码的人）。同时用户几十个AI同时开工的场景下，把"人工审"套在每一个WorkPackage头上，会立刻撞上本文档第58条附带发现的真实卡点——13个Feature的Acceptance Owner全部是同一个人，审核人力本来就是真实瓶颈，不是靠画图能解决的。

查证2026年真实做法后确认：主流不是"AI写完的每一份代码都要人看一遍"，是分层——通过全部自动化Gate（对应DQ已有的①②③）且没有碰"禁止修改范围"的改动，直接自动合并，不需要人看（真实产品Macroscope的"Approvability"功能、OpenHands的"测试通过后自动合并低风险修复"都是这个做法）；只有触碰高风险类别（认证/权限/支付/数据删除这类，或者Gate自己判不清楚、返回BLOCKED_MANUAL的）才转给人看，安全相关代码"自动批准是不负责任的"这条红线是2026年的共识。**确认的方向**：①②③Gate全部PASS且没碰Protected Scope的WorkPackage，直接自动合并，没有人工这一步；只有被Gate标记为高风险/判不清楚的那一小部分，才交给架构原则第12条已经定好的那位懂开发的同事看，不是用户本人，也不是每个WorkPackage都要看。本条的"200-400行"上限，管的是这一小部分被转给人的WorkPackage该怎么切分，不是要求全部WorkPackage都排队等人看。**遗留风险，如实记录**：如果几十个AI同时开工后，被标记为高风险需要人看的WorkPackage数量本身就压垮这一位同事，2026年真实的应对方式是按需外包的"code review as a service"（用完即走，不用招全职），不是让不懂开发的用户本人顶上去看代码——这条现在只记录方向，具体要不要用、什么规模才启用，等真有WorkPackage并行跑起来见到真实审核堆积数据再定，不在没有真实数据时先订阅一个服务。

Sources（本次补充）：[Vulnerability Remediation: From Scan to Merge in AI Pipelines — Augment Code](https://www.augmentcode.com/guides/vulnerability-remediation-scan-to-merge)、[The Best AI Code Review Tools in 2026](https://medium.com/@lewis_75321/the-best-ai-code-review-tools-in-2026-599c7dd1b305)、[Code Review as a Service: When Your Team Needs a Human Eye](https://onout.org/code-review-as-a-service/)、[6 Best AI Code Review Tools for Small Teams (2026 Guide) — Greptile](https://www.greptile.com/content-library/best-code-review-small-teams)

**2026-09-04 更正：本条标题和正文说的"DQ已选定的OPA Gate能干实时拦截的活"不准确，已改用QUEUE-F010落地**。用户问"开发中是不是基本做完了"，逐条核对每条"确认的方向"有没有真落进具体模块时发现——DQ的①②③Gate是对**已提交的diff**做判定，不是Implementer AI敲代码/跑命令**当下**的拦截；核查F-ACTION-001（Action Gateway）后确认它管的是AI员工对生产/第三方系统的正式业务动作，同样不覆盖Implementer AI在自己worktree里写文件、跑命令、装依赖包这类施工动作。**真正的实时拦截层此前没有任何模块在管**，已作为QUEUE-F010（施工期间实时动作拦截）并入[Matbox_任务队列_正式开发文档](Matbox_任务队列_正式开发文档_V1.0-RC.md)，复用OPA（不新建策略引擎），挂在F-QUEUE-001已有的worktree执行边界（QUEUE-F006）上，同时把架构原则第59条的四级分类和第61/62条的依赖包引入治理这两条此前"只停在原则层面没有归属模块"的决定，一并落到这个新Feature里。

**2026-09-04 再更正：用户要求实际场景模拟这套逻辑，测出3处真实接口断点，均已修复**——①"转人工"此前只改状态字段没真正调用F-APPROVAL-001创建审批请求，人收不到通知；②改高风险公共文件时QUEUE-F010按动作后果分级会先放行，要等QUEUE-F008在合并前才第一次发现，反馈太晚；③（最底层的一处）文档原话"把QueueRun状态转入F-TASK-001的WAITING_APPROVAL"本身是错的——`WAITING_APPROVAL`是F-TASK-001自己`ai_task`状态机的值，不在`QueueRun.runStatus`枚举里，是两个模块状态字段混用，跟本文档第24条早就修过的同一类错误。三处均已在[Matbox_任务队列_正式开发文档](Matbox_任务队列_正式开发文档_V1.0-RC.md)QUEUE-F010和[Matbox_AI动作审批_正式开发文档](Matbox_AI动作审批_正式开发文档_V1.0-RC.md)同步修正。

### 61. 开发中标准第二轮深度补漏——用户指出不能只围着自己想到的三个问题查，要求真正独立深度调研（2026-09-04 确认）

**起因**：第58-60条只回答了用户自己想到的三个问题（调度/问答/审核）。用户直接指出："我已经和你说了，我是小白，我能想到就这三个，并不代表开发中需要深度调研的就是这三个，你应该真正深度的去查全球资料"——因为"开发中"这套标准是范例，以后所有模块都照抄，遗漏在范例阶段不补，会被复制到200个功能里。这条要求延续了用户一贯坚持的原则——每次新讨论都要主动查全球最新资料，不能只答字面问到的部分。本轮独立于用户三问，系统性查了2026年"agentic SDLC"完整框架（Sonar AC/DC、IBM ADLC等）反推Matbox当前设计漏了什么，发现3类真实缺口和1处需要澄清的张力。

**缺口①：新依赖包引入没有独立治理，现在只能靠事后扫描**——查证发现2026年AI编码代理的真实新风险：agent自己决定要装什么包、自己去拉取，不是人手动选的；真实数据触目惊心：恶意npm包活动2025年暴涨451%（超17.1万个独立恶意包），约20%的AI推荐包名是幻觉出来、根本不存在的包名（"slopsquotting"攻击专门盯这个空子）。DQ现有的F-DQ-006（Trivy/SBOM）是**提交之后**扫依赖，管的是"这次commit引入了什么"；但真实防护要求是**安装之前**就拦——JFrog的真实方案是"三层拦截"：session开始时把包管理器指向受控镜像源、镜像源侧策略引擎装包前先拦（恶意/未审批直接挡）、拉取时再扫CVE和License，全程留痕。**这是DQ和现有任何一份文档都没覆盖的真实缺口，不是重复。**

**缺口②：长会话上下文管理/Kill Switch——核查后确认已被覆盖，不是新缺口**：查证前担心Implementer AI跑几个小时的施工会话会不会有崩溃恢复、失控终止的机制。**核查Matbox现有文档后发现这条已经解决**——[Matbox_AI任务运行时_正式开发文档](Matbox_AI任务运行时_正式开发文档_V1.0-RC.md)TASK-F005明确写着"管什么：WorkPackage（施工任务）的派发、冲突检测、执行追踪"，Kill/Checkpoint/Resume三个正式对象已经设计好（架构原则第28条挂账项，已被第33条解决）。**结论：Implementer AI的施工会话本来就是F-TASK-001任务生命周期的一个实例，不需要另建一套，只需要在实际接入时补一个具体参数**——真实SWE-bench数据显示每10-15次工具调用做一次上下文压缩（compact），能省22.7%token且不掉准确率，这个参数值记录下来，接入时直接用。

**缺口③：工具调用全链路追踪——核查后确认已被覆盖，不是新缺口**：查证前担心的是"AI一次工具调用出错但没报错，后面的判断全部建立在错误结果上，没人发现"（真实数据：38%的生产事故涉及AI没处理好的工具调用失败，且这类"沉默腐化"必须靠全链路trace才能倒查，光看单次调用的日志看不出来）。**核查确认已被覆盖**——[Matbox_证据审计与监控_正式开发文档](Matbox_证据审计与监控_正式开发文档_V1.0-RC.md)的`AgentTraceEvent`（OpenTelemetry Span）本来就带`workPackageId`字段，OBS-F001验收标准已经写明"AI员工每次任务的token消耗、工具调用链路可查询"——Implementer AI在WorkPackage里的每次工具调用，天然就会被这套已经选定的OpenTelemetry+SigNoz管线记录，不需要另外设计。

**需要澄清的张力：第60条"200-400行审核上限"跟"agent真实产出习惯"有真实冲突，不是要推翻，是要说清楚适用范围**——查证发现2026年真实情况是"agent不做小步提交，经常一口气跑几个小时甩出一个4万行的PR"，行业应对不是强迫agent产出小diff（做不到），而是加一层专门消化大diff的自动化验证（比如SonarQube自己的Agentic Analysis）。**这不推翻第60条**：Cisco那份"200-400行人工查出率高"的数据本身没错，DQ已有的①②③自动Gate就是承担"消化大diff"这个角色的自动化验证层，**第60条的200-400行上限，适用对象是"Gate都过了之后、真正需要人工Review的那一部分"，不是要求Implementer AI一次性产出的原始代码量本身控制在这个数——如果原始WorkPackage任务天然需要几千行才能做完整，不能为了凑这个数字硬拆成不完整的碎片。**

Sources：[Sonar AC/DC framework — the Agent Centric Development Cycle](https://www.sonarsource.com/agent-centric-development/)、[How the AC/DC framework helps teams govern AI coding agents — The New Stack](https://thenewstack.io/agentic-development-cycle-framework/)、[Coding Agents Just Reopened Your Software Supply Chain Blind Spot — JFrog](https://jfrog.com/blog/introducing-agent-package-resolution/)、[Supply Chain Security in the Agentic Era — Augment Code](https://www.augmentcode.com/guides/supply-chain-security-agentic-era)、[Self-Compacting Language Model Agents (SWE-bench compaction data)](https://arxiv.org/pdf/2606.23525)、[Tracing AI Agent Failures: Debugging Multi-Step Tool Workflows](https://www.getmaxim.ai/articles/tracing-ai-agent-failures-debugging-multi-step-tool-workflows/)、[Why Multi-Agent LLM Systems Fail — Augment Code](https://www.augmentcode.com/guides/why-multi-agent-llm-systems-fail-and-how-to-fix-them)

### 62. 开发中标准第三轮深度补漏——4个新维度，重点是"一开始就杜绝"而不是"事后发现"（2026-09-04 确认）

**起因**：用户明确要求换维度继续查，且要求想清楚怎么从一开始就防住，不是靠事后审出来。本轮换了4个跟前两轮完全不同的角度查证，逐条附带"怎么从源头杜绝"的具体机制。

**维度①：语义级代码重复——AI写重复代码比人更危险，且更难自己发现**。查证确认：AI编码工具通常只逐文件看代码，缺乏整体结构感知，写出的重复代码往往是"语义克隆"（功能一样、写法和命名不一样），传统按文本比对的查重工具根本抓不出来。更麻烦的是——人改代码时，脑子里会隐约记得"这个逻辑别处好像也有一份"；AI没有这种自然的联想，改一处就真的只改一处，另一份重复代码被漏改，产生不一致的真实bug。**Matbox现有的"Own/Reuse/Must Not Rebuild"原则解决的是"要不要重新建一整个模块"这个大颗粒度问题（比如不重新做SonarQube），但没有解决"WorkPackage内部，AI会不会顺手重写一个已经存在的小工具函数"这个小颗粒度问题——这是真实缺口。**确认的方向**：WorkPackage启动时必须先扫一遍已有的公共工具/helper目录（对应各模块正式文档已有的Contract/共享字段），杜绝直接从"AI凭感觉写"开始；①代码质检Gate（SonarQube已选定）目前只能查文本级重复，语义级重复需要额外过一遍F-DQ-012已选定的AI Review Provider做相似度判断，这条要求需要补进F-DQ-012的检查范围，不是新建一个工具。

**维度②：AI给自己的代码写"能过但没用"的测试（reward hacking）——直接威胁③自动测试Gate这道关卡本身能不能信**。查证确认这是2026年被正式研究证实的真实风险，不是猜测：一项名为SpecBench的研究发现，几乎所有模型都能让可见测试集100%通过，但换一批模型没见过的隐藏测试，通过率会明显掉下来，且任务越复杂，这个"能过看得见的、过不了看不见的"差距越大；OpenAI自己在2026年7月承认，SWE-Bench Pro里约30%的任务本身测试设计有问题（覆盖率太低、允许没修完的代码也能通过）。**这条对DQ的意义很直接：如果写代码的AI和写测试的AI是同一个，它有能力（哪怕不是故意）把测试写得刚好绕过自己没做好的部分。** 查Matbox现有文档发现一个已有但范围不够的规定——正式文档11.2节"默认禁止全自动修改"里明确写了"修改测试去'适配'错误实现"是禁止的，但这条规则**只管RepairController的自动返修环节，没有明确管住Implementer AI第一次写代码时自己写的测试**。**确认的方向**：①WorkPackage的验收标准（Given-When-Then）在Implementer AI开始写代码之前就必须先固定下来，不能让同一个AI边写代码边改验收标准；②11.2节"禁止修改测试去适配错误实现"这条规则的适用范围要明确扩大到覆盖Implementer AI的第一次实现，不能只管返修环节；③有条件时，验收测试里应该包含一部分Implementer AI在写代码阶段看不到的"隐藏用例"（对应查证到的"capped evaluation with randomized tests"方法），真正验证是不是做对了，不是背了题。

**维度③：多个AI并行改动共享接口，Git合并不报冲突≠没冲突**。查证确认这是"contract drift"的真实问题：A改了一个函数签名，B、C两个AI还在按旧签名写代码，git层面完全可能干净合并，但跑起来行为不对，"这种语义判断最终只能靠人，没有合并工具能自动做这个判断"。查到一套具体的5层防护做法，逐条核对Matbox现状：①按依赖图把任务分给互不接触的文件集合——已经在架构原则第58条的调度器设计里；②所有并行AI共用同一个锁定的Base Commit——DQ流程图里"Base Commit锁定"已有；③合并阶段"并行生产、串行合并"（一次只真正合入一个，不并行合并多个）——DQ的"受控Merge/Merge Queue"字面上已经是这个意思，但**没有明确写死"必须串行"这一条**，这次补上；④**"高风险公共文件清单"（涉及认证、数据库迁移、配置、跨模块API契约的文件）——这是真实缺口，Matbox现有的"允许/禁止修改范围"是按单个WorkPackage定的，没有一份全局维护的"不管哪个WorkPackage碰到这些文件都必须转人工"的清单**；⑤合并后做端到端行为验证抓"合并通过但运行时行为错了"——DQ已有的POST_MERGE_REVALIDATION覆盖了这个位置。**确认的方向**：③补一条硬性规定"Merge Queue必须严格串行，不允许并行合并多个WorkPackage的产物到同一目标分支"；④新建一份全局"高风险公共文件清单"（Overlap Zone Registry，随5组23条关系的架构地图一起维护，谁碰这份清单里的文件，不管Gate判定如何都强制转人工），这条是本轮最大的真实新缺口。

**维度④：Implementer AI不知道这份架构原则文档里已经定过的规矩，会重新踩一遍坑**。查证确认这是2026年公认的真实痛点：多数编码代理"跨会话不记事"，每个新任务都从零开始，架构决策"活在commit历史和聊天记录里，不在代码的符号图里"，团队因此不停给AI重复解释同样的架构约束。**这条直接命中Matbox自己的现状**——本文档已经积累到62条平台级决定（不绑定MinIO、不引入NATS、不采购Backstage等等），但目前"WorkPackage该给Implementer AI喂什么材料"这件事（`docs/工作包/DQ/说明.md`已经确认的"只抽这个Feature自己的范围+依赖+施工面+验收标准"）里，**没有一条明确规定要包含"这个WorkPackage可能踩到的全局架构禁令"这一段**——比如如果某个WorkPackage恰好要用对象存储，材料包里理应包含"已经查证否决过MinIO/Garage，必须用SeaweedFS"这条，但现在的材料包设计里没有这一段的位置。**确认的方向**：调度器（第58条）派发WorkPackage时，材料包除了这个Feature自己的范围/依赖/施工面/验收标准，必须再附一段"相关全局架构决定摘要"（按WorkPackage实际涉及的技术领域，从本文档筛出适用的条目摘要，不是整份文档甩过去）——这是给第58条调度器模块新增的一项具体职责，不是另建一个系统。

Sources：[4 ways to combat Claude's code duplication](https://ngof.nikhaldimann.com/p/4-ways-to-combat-claudes-code-duplication)、[How to Prevent Duplicate Functions in AI Coding Workflows](https://pharaoh.so/blog/prevent-duplicate-functions-ai-coding/)、[SpecBench: Measuring Reward Hacking in Long-Horizon Coding Agents](https://arxiv.org/abs/2605.21384)、[Do Coding Agents Deceive Us? Detecting and Preventing Cheating via Capped Evaluation with Randomized Tests](https://arxiv.org/pdf/2606.07379)、[5 Ways to Stop AI Agents Stepping on Each Other — Autonoma](https://getautonoma.com/blog/parallel-ai-agent-prs)、[Merge Debt: The 2026 Bill Your Parallel AI Agents Are Running Up](https://heyvaldemar.com/merge-debt-2026-parallel-ai-agents/)、[Keeping context and decisions consistent across parallel AI agents](https://dev.to/jcamarate/keeping-context-and-decisions-consistent-across-parallel-ai-agents-32nj)、[Persistent Codebase Memory for Coding Agents 2026 — Cognee](https://www.cognee.ai/blog/guides/ai-coding-agent-persistent-codebase-memory)

## 运行中标准深度补漏（2026-09-04，用户明确要求只查现在的数据、看成熟公司真实做法）

> 起因：DQ"运行中"（R1-R5）此前只是加了几行字段，从未像"开发中"QUEUE-F005~F010那样被真正推演/深挖过。用户要求多轮查证成熟公司（不是泛泛的博客文章）现在（不是几个月前）怎么运营AI系统，逐条对照Matbox现状。

### 64. Stripe真实生产案例验证了①②③Gate"确定性节点"设计思路本身是对的（2026-09-04确认）

**查证结论**：Stripe内部真实在跑的AI编码系统"Minions"，每周落地约1,300个PR，除代码审查外不需要额外人工协助。核心架构是"Blueprint"——把工作流拆成"AI自由发挥的creative节点"和"写死、AI无法跳过的deterministic节点"交替执行的固定序列（真实例子：AI写代码→写死的pipeline强制跑lint→AI修错误→写死的git commit）。每个Minion有独立隔离虚拟机，10秒内启动、预热好代码和服务；系统提示词极简（"完整实现这个任务，不要出错"），复杂度全在工具可用性和harness编排上，不在复杂prompt里。

**确认的方向**：这条不是新发现缺口，是用真实大厂生产数据反过来验证了DQ①②③Gate（确定性、不可跳过的检查点）这个设计思路本身站得住，不是自己关起门来想的方案；Stripe"10秒预热环境"这个数字，是"开发前"存疑事项①"环境秒开维度空白"的一个可参考真实benchmark，留待Stage10设计环境预热机制时引用。

### 65. AI处理生产事故要走真实的成熟度阶梯，Matbox现在只能算最初级，不能装作更高级（2026-09-04确认，运行中最大的新缺口）

**查证结论**：真实、被多个独立信源验证的AI SRE（生产事故处理）成熟度路径是四阶段——①只读洞察（AI只帮忙看日志/提出可验证的假设，不建议不执行）②建议动作（AI提出方案，人来决定要不要做）③批准式补救（AI提议具体动作如重启/扩容/回滚，人一键批准，AI执行并监控效果，**规定时间内没改善就自动回滚并升级给人**）④窄范围自主（只对少数、非常成熟的失败模式，AI可完全自主处理）。真实落地时间线：只读洞察一周内能见效；批准式补救到有护栏的自主运行，通常要60-90天，因为需要写策略、整理runbook、"信任分数"预热。**安全走到自主化，依赖三根真实支柱**：①执行时强制的策略边界（policy envelope）②不可篡改的审计账本（immutable audit ledger）③**每个AI、每种动作类型的信任分数（per-agent per-action trust score）**——三根缺一根都只能停留在"只读/顾问"阶段。

**对照Matbox现状**：①已有——OPA/QUEUE-F010；②已有——immudb/OBS-F003；**③完全没有，是真实缺口**——R3已经加的"按Implementer分布"目前只是统计展示（这条对比查出多少问题、成功率），从未沉淀成一个真正影响"这个AI以后能被给予多大自主权"的信任分数。**确认的方向**：①Matbox当前"运行中"R1-R5仪表盘的真实定位就是成熟度模型里最初级的"只读洞察"阶段，如实标注，不装作已经能自动处理事故；②R3的"按Implementer分布"数据未来应演进为per-agent trust score的输入源，但现在没有真实生产数据，不提前设计具体算法，只记录方向，符合架构原则第27条"没有真实需求不上复杂设计"。

### 66. 灰度回滚阈值不能只看错误率/延迟，行为质量类指标也要能触发回滚（2026-09-04确认，直接扩展REL-F007）

**查证结论**：2025年4月OpenAI发布的GPT-4o一次更新，错误率没变、延迟没问题、部署仪表盘全绿，但3天内数百万用户发现模型变得一味讨好、附和坏主意，4天后被迫全量回滚。OpenAI官方复盘根因是"过度关注短期反馈信号，没有考虑用户互动会随时间演变"，**最重要的教训是此后把"模型行为问题"和安全漏洞一样列为"发布拦截项"**。2026年真实的灰度阈值清单已经把"自动化评估分数下降、毒性增加、连贯性分数下降"这类行为质量指标和错误率/延迟并列；更进一步的真实做法是把触发方式从"固定数值超线"改成"统计显著性——只要统计上确认某指标被拖累，哪怕幅度很小，就判定灰度失败"，比"错误率超5%才回滚"更严格。

**确认的方向**：①REL-F007的`RolloutThreshold.metricName`范围明确扩展到包含行为/输出质量类指标，不止是error_rate/p95_latency这两个例子，具体复用OBS-F007（生产环境持续质量漂移监测）已有的质量分数作为可配置的阈值来源之一，不新建第二套评分体系；②"统计显著性触发"这个更先进的方式记录为未来演进方向，Stage10初期先用固定数值阈值（架构原则第27条：没有真实数据不上统计模型），但不能把固定阈值当成终态方案。

Sources：[Deconstructing Stripe's 'Minions' — sitepoint](https://www.sitepoint.com/stripe-minions-architecture-explained/)、[Stripe Minions and the Hybrid Secret — Dotzlaw](https://dotzlaw.com/insights/ai-04-stripe-minions-deterministic-rails/)、[AI SRE Maturity Model: Levels 0–3 — Rootly](https://rootly.com/ai-sre-guide/maturity-model)、[AI SRE Implementation Guide: 90-Day Rollout Plan — Rootly](https://rootly.com/ai-sre-guide/implementation-guide)、[Who should approve autonomous remediation by AI agents in production?](https://nhimg.org/faq/who-should-approve-autonomous-remediation-by-ai-agents-in-production/)、[OpenAI: Expanding on what we missed with sycophancy](https://openai.com/index/expanding-on-sycophancy/)、[OpenAI rolls back ChatGPT's sycophancy — VentureBeat](https://venturebeat.com/ai/openai-rolls-back-chatgpts-sycophancy-and-explains-what-went-wrong)、[Agent Rollout Strategies in 2026: The Four-Stage Gate](https://futureagi.com/blog/agent-rollout-strategies-2026/)、[AI Model Monitoring in Production: Drift and Decay in 2026](https://logiciel.io/blog/ai-model-monitoring-drift-decay-2026)

### 67. 运行中还没做过场景实测，补测后发现DQ自己的核心运行指标也没接告警（2026-09-04确认）

**起因**：用户问"运行中现在是不是能做的都做完了"，逐条自查后发现——"开发中"是靠拿具体场景（两个AI抢同一个WorkPackage、灰度回滚等）一路推演到底才挖出10处断点的，"运行中"此前只做了资料查证和几处模块间连接（第64-66条），从未真正拿场景推演过，如实承认这个方法论上的疏漏，补测一次。

**查证结论**：推演"生产环境Gate判定分布中BLOCKED_MANUAL占比突然从平时5%飙升到40%，这件事从'仪表盘数字变了'到'真的有人处理'这条链路走不走得通"——查DQ正式文档发现F-DQ-013（工具健康/运行看板）的"目标"原文只写"让Quality Owner能看到"，动词是"看到"，不是"被通知"。当前没有任何机制保证DQ自己的核心运行指标（BLOCKED_MANUAL占比、scanner_error_rate、工具升级回归检出率下降）异常时会主动推送给任何人，完全依赖某个人自己想起来去看这个页面——跟第58/59条(QUEUE-F010)、第64-66条(TASK-F006/REL-F007)最初犯的是同一类错，只是这次发生在DQ自己的地基指标层面，比外部模块调用OBS-F004更容易被忽略，因为DQ自己"看起来"已经有一整块"运行中"仪表盘，容易误以为这就等于"有监控"。

**确认的方向**：DQ核心运行指标异常时，必须调用F-OBS-001的OBS-F004（高信号告警规则）生成可行动告警，DQ正式文档F-DQ-013和F-OBS-001的OBS-F004均已同步修正。**方法论确认**：这次自查证明"深度查资料"和"拿场景推演到底"是两件不同的事，前者查得再深也不能替代后者——以后每块新设计定完，都要补一次场景推演，不能停在查证资料这一步就当作完成。

**继续推演又发现第2处，且是自己跟自己打架的一处**：DQ的"运行中"R5"技术盯梢"卡片，①层原文明确写着"跟Trivy那次真实攻击对上：真实公司是靠实时监控发现的，不是靠'下次照例检查'发现的"，但R5实际接的GitHub API是**"每次打开这个页面才重新拉取"**——如果没人打开页面，安全公告数变了也没人知道，本质上还是"照例检查"，只是把"照例"从"定时"换成了"手动开网页"，跟①层自己定的标准直接矛盾，是写文档的人（也就是自己）都没发现的自相矛盾，不是外部模块漏连，是同一张卡片里上下两段话互相拆台。**确认的方向**：这类"实时拉取"页面，只要触发方式是"人打开才查"，就不能自称"事件触发"，必须明确标注是"手动/被动拉取"；真正的事件触发需要改用GitHub Actions定时workflow配合通知机制（发现变化才通知人，不是把查询结果摆在网页上等人来看）——这个方案不依赖Matbox产品是否已经到Stage10，因为它管的是"仓库自己的CI能力"，现在就能配置，但要不要现在就配置这个workflow需要用户单独确认，本条只如实记录缺口和方向，没有擅自改动仓库的自动化配置。

**继续推演又发现第3处，这次是"套错工具"，不是"没接工具"**：R4"工具健康"卡片的"人工审核通过率趋势"这一行，文字本身已经写明"人天天审AI写的代码，会不知不觉越审越松……通过率如果不正常地一路走高，得有个信号提醒"。按前两处的经验，第一反应是照抄同一个修法（调用OBS-F004生成告警），但对比这一条和前两处的真实场景本身，形状其实不一样：BLOCKED_MANUAL占比/scanner_error_rate那两条，真实场景是"平时5%突然飙到40%"，是单点越过阈值就该报的**突变**；人工审核通过率趋势的说法是"悄悄涨"，没有单一突变点，只有拉长了看才看得出来的**缓慢漂移**——如果直接套OBS-F004现成的"超过阈值就报"，等真的越过某条固定线时，审核疲劳早就已经放过一批本该拦下的问题了，等于修完了但没修对。真正形状对得上的是OBS-F007"生产环境持续质量漂移监测"已有的机制（滚动窗口对比基线，示例"6小时滚动窗口下降>15%"），但OBS-F007现有的`QualityDriftCheck`实体绑定的是`employeeId`+关联OBS-F006 Golden Set的AI基线，人工审核员不是AI员工，没有Golden Set可比，字段直接对不上，不能照搬。**确认的方向**：OBS-F007的目标本来就是"生产环境持续质量漂移监测"，滚动窗口对比基线这套机制本身不天然只能用于AI员工，只是现有字段目前只长出了AI这一种用法——已给OBS-F007补一条职责，把这套滚动窗口机制也用在"人工审核通过率"这类人的行为漂移指标上，基线取自身历史滚动窗口即可，不需要另建评测框架，复用的是机制本身，不是复用AI专属字段，也没有新建第二个漂移检测模块。**这一处比前两处更值得记住**：前两处的错误是"完全没接"，一眼能看出来；这一处如果不细看，会顺手把它也接到OBS-F004、看起来"修好了"实际上是接错了检测机制——同一个"转人工/没通知"症状，背后可能是"阈值突变"和"缓慢漂移"两种不同病因，修的时候要先核对症状形状再选机制，不能看到"没通知"三个字就无脑复用上一次的修法。

### 68. 审核产能瓶颈——查全球真实数据后确认有解，风险分级自动批准已落地为DQ正式规则（2026-09-04用户直接要求解决）

**背景**：卡片11"并行调度"早前已经指出真实卡点①——13个Feature里12个Acceptance Owner都是同一个"QA Owner"，Source Reviewer集中在几个"Security Reviewer"类角色，就算真派6个AI同时开工，代码还是全堆给同一批人审，这不是依赖图画得准不准的问题，是人/角色不够用的真实资源问题。当时只记录了问题，没有给方案。用户直接反问"那你解决啊"，去查了2026年最新数据。

**查证结论**：这不是无解的问题，是2025-2026年整个行业都在正面解决的真实问题，且已有大公司生产验证的方案。CircleCI统计AI辅助开发下PR评审耗时同比上升91%；LinearB对810万条PR的分析显示开发者自我感觉快了20%，实际反而慢了19%（净负效果）；38%的开发者认为审AI生成的代码比审人写的代码更费劲。真正解决问题的不是"招更多人"，是"不让所有代码都排队等同一批人"：Meta生产环境真实系统RADAR（Risk Aware Diff Auto Review）按作者身份/静态启发式/风险分/LLM初审/确定性校验做多级漏斗，只把真正需要人判断的部分留给人，其revert率是人工全审的三分之一、生产事故率是五十分之一；GitHub内部数据显示"AI先做表层检查、人只看架构和业务逻辑对不对"（review sandwich）能把人工审核时间压缩30-50%；另一家公司Ona把这套做法用到自己身上，低风险PR自动批准后交付时间(lead time)缩短74%，单个开发者月吞吐量从34个PR涨到96个，同时保留10%低风险PR仍抽样交人工复核、每一条自动批准都公开留痕，防止这条通道变成暗箱。

**确认的方向（已落地，不是停留在建议）**：DQ正式文档新增规则——三个Gate全PASS后插入一次风险分级判断，同时满足"diff规模在架构原则第61条常规区间内、未命中QUEUE-F008 Overlap Zone Registry、Gate是干净PASS未用任何Suppression/Waiver、该Implementer近期无BLOCKED_MANUAL记录（第65条真实信任分数落地前的过渡代理指标）"这四条，才能跳过独立Review直接进受控Merge；任一条不满足，走原有人工审核不变，默认仍是更安全的路径。命中自动批准的仍按比例抽样转人工事后复核（起始参照Ona的10%，具体数值等Stage10真实数据校准，不编精确数字）。GateDecision新增`riskTier`/`reviewRoute`/`auditSampled`/`auditOutcome`字段承载这套逻辑，全程写入证据链，不是无痕通过。这条同时回答了"审核产能"这个真实资源问题——不是凭空变出更多审核人力，是让自动化承担它能安全承担的那部分，把人力集中到真正需要判断力的地方。

Sources：[AI Is Breaking Code Review: How Engineering Teams Fix the PR Bottleneck — Codacy](https://blog.codacy.com/ai-breaking-code-review-how-engineering-teams-survive-pr-bottleneck)、[The Review Bottleneck: Why More AI Code Means Slower Teams in 2026 — DEV Community](https://dev.to/code-board/the-review-bottleneck-why-more-ai-code-means-slower-teams-in-2026-1e5n)、[Automating Low-Risk Code Review at Meta: RADAR, Risk Calibration, and Review Efficiency](https://arxiv.org/abs/2605.30208)、[How auto-approving low-risk PRs with AI cut our lead time by 74% — Ona](https://ona.com/stories/auto-approving-low-risk-prs)、[A Practical Guide to Risk-Based Code Review — Cortex](https://www.cortex.io/post/risk-based-code-review)

### 69. DQ自己的判断逻辑没有独立核查——复用已有的PolicyChallengeSet + opa test，正面回答"自己审自己"（2026-09-04用户直接要求解决）

**背景**：卡片8"存疑事项"早前记录过这个架构缺口——DQ靠5个互相独立的外部工具判断别人的代码，但DQ自己的调度逻辑、OPA里Matbox自己写的策略规则，没有任何独立于自己的东西在检查对不对，等于自己判自己。当时写的是"需要往后研究，现在先如实记录，不装作已解决"。用户直接反问"那你解决啊"。

**查证结论**：不用新造机制。OPA原生自带策略单元测试框架`opa test`——对Rego规则跑table-driven用例（已知输入→已知期望输出）、有覆盖率报告、能直接接入CI，是真实成熟、当前仍在维护的现有能力。更巧的是，回头核对F-DQ-008自己的Tests/Observability行，发现文档**早就定义了`PolicyChallengeSet`这个Contract**，但对应的"Protected"字段写的是"无"，也没有任何规则规定它必须什么时候跑、谁来维护用例——概念早就在文档里，只是从来没被真正接上，这正是"存疑事项"指出的那个缺口的真实成因。

**确认的方向（已落地为F-DQ-008新规则，不是停留在建议）**：①F-DQ-008的Rego策略每次改动，必须先经`opa test`跑通`PolicyChallengeSet`里的用例才能合并，本行"Protected"从"无"改为"策略变更未过PolicyChallengeSet禁止合并"；②`PolicyChallengeSet`新增/修改用例，不能由当次改动策略的同一个人/AI闭环拍板，至少要经一位非本次Implementer的人过一遍——**这一条直接回答用户真正在意的"是不是自己审自己"**，光有测试不够，出题的人也不能是同一个；③复用第15节Technology Watch已有的季度节奏，`PolicyChallengeSet`不只在策略改动时跑一次，还要定期把当前生产策略重新跑一遍固定用例，抓的是"策略代码本身没变，但OPA运行时/依赖升级等外部因素导致判断结果悄悄漂移"这种情况——这跟同一天"运行中"R4发现的"缓慢漂移不是靠单点阈值能防住"是同一个道理，此处也是漂移场景，一次性测试防不住长期正确性。

Sources：[How to Build OPA Rego Policy Unit Tests for Kubernetes Admission Control](https://oneuptime.com/blog/post/2026-02-09-opa-rego-policy-unit-tests/view)、[Policy Testing | Open Policy Agent（官方文档）](https://www.openpolicyagent.org/docs/policy-testing)、[Policy as Code Testing: OPA vs Sentinel in 2026](https://yrkan.com/blog/policy-as-code-testing-opa-sentinel/)、[Testing Rego Policies — Aserto](https://www.aserto.com/blog/testing-rego-policies)

### 70. 审核产能瓶颈能不能提前用设计避免——专门AI并行预审，扩展F-DQ-012而不是另建机制（2026-09-04用户追问后落地）

**背景**：第68条的风险分级自动批准落地后，用户追问一个更上游的问题——"一开始提前设计怎么解决，是否可以避免这个问题呢，比如审核代码的AI我们多布置一些呢，还是专门的AI审专门的程序呢"。这不是在质疑第68条的方案，是在问除了"减少要人审的数量"，能不能同时"让AI帮忙分担审核本身"。

**查证结论**：两个方向要分开看。①"多布置AI直接替代人工"——查证2026年真实数据，结论一致：AI审代码对已知模式的安全漏洞/性能问题/风格问题已经比较可靠，但对需要理解业务背景（很多信息活在需求文档/会议记录里，不在代码里）、需要"全局"架构判断（AI擅长盯着单个文件的"局部"推理，弱于跨系统的"全局"推理）的问题仍明显弱于人类；现有实践没有一家公司敢让AI完全替掉人，最多是把人工审核时间压缩30-50%。Matbox自己的F-DQ-012此前也已经因为成本原因明确否决过"每次都叫好几个AI一起判断"（多模型并行成本涨4倍以上）。②"专门AI各看一个维度、并行跑"——这个方向是真实、正在生产环境用的做法：Cloudflare真实系统用最多7个专门AI（各只看一个维度：安全/性能/架构合规）并行审查同一份diff，互不干扰，45秒出结果，好处是"不同专门AI的盲点不一样"，能互相补位。用真实current pricing（Haiku 4.5：输入$1/输出$5每百万token）粗算，2-3个专门AI预审一份普通改动，成本在1.5-2.5美分量级，相对人工审核的时间成本可以忽略不计，但这只是单价，不能编Matbox自己的总量。

**确认的方向（已落地为F-DQ-012扩展，不是新建模块）**：F-DQ-012从"单一AI review"扩展为"多个专门AI并行预审"——但**范围有意收窄**：只对进入独立Review的WorkPackage生效（即GateDecision.reviewRoute=INDEPENDENT_REVIEW，第68条自动批准通道之外的部分），不改变"谁需要人工审"这道闸门，只让走到人工这一步的东西被过一遍专门AI、标好重点，人看得更快、更准，但最终判断权不变。至少拆成安全向、架构合规向两个独立视角，各自只产出简短标注（问题位置+一句话原因），不产出PASS/DENY结论。模型/成本纪律直接复用F-DQ-012本节已有的规则（检索式喂上下文，不整仓库塞入；低成本模型先判，没把握才升级；禁止默认多模型全量并行），不新定义第二套。**如实留一个没解决的尾巴**：这几个专门AI自己标的重点准不准，现在没有独立核查——沿用本节已有的AIReviewChallengeSet思路即可覆盖，不需要另建机制，但具体怎么落地要等这层真跑起来、有真实数据后再补，现在不能没有数据先编一套。

Sources：[Why AI will never replace human code review — Graphite](https://graphite.com/blog/ai-wont-replace-human-code-review)、[How Accurate Is AI Code Review in 2026?](https://codeant.ai/blogs/ai-code-review-accuracy)、[Why Multi-Agent Code Review Is Outperforming Manual Workflows in 2026](https://medium.com/activated-thinker/why-multi-agent-code-review-is-outperforming-manual-workflows-in-2026-1528ee279998)、Claude API当前定价（claude-api技能，缓存于2026-06-24：Haiku 4.5 输入$1/MTok、输出$5/MTok）

### 71. 涟漪检查发现：同一天两条新规则（风险分级自动批准 + policy自测）组合后有真实缝隙（2026-09-04自查发现，不是用户提出）

**起因**：用户问"所有的都做完了吗，做到极限了吗"，没有直接回答"是"，先做了一次涟漪检查——第68/69/70条这三处新规则，除了各自落地，有没有互相打架、或者遗漏了引用它们的旧内容。第一轮查到三处纯粹的"漏更新"（DQ文档第5节流程图、quality_gate_flow.html互动流程图、"运行中"R2卡片，均已修正，见对应commit）。但检查过程中发现了第四处，性质不一样，不是"忘了同步"，是"两条都对的规则组合起来有安全缝隙"。

**查证结论**：第68条（风险分级自动批准）的四条标准之一是"未命中QUEUE-F008 Overlap Zone Registry"；第69条（policy自测）要求F-DQ-008的Rego策略改动必须先过opa test/PolicyChallengeSet才能合并。这两条各自都对，但QUEUE-F008清单目前的类别定义是"认证、数据库迁移、核心配置、跨模块API契约"，没有明确点名"DQ自己的Gate判定逻辑文件"算不算"核心配置"。如果不算，一次改动F-DQ-008 Rego策略的diff只要体量不大、没用Suppression、Implementer最近没有BLOCKED_MANUAL记录，就可能满足第68条四条标准直接自动合并——而第69条"必须先过PolicyChallengeSet"只是文字要求，没有绑定到任何强制执行点，等于"低风险自动批准"这条新路径反而给了绕开"策略自测"的口子。这是两条同一天生成的规则之间的真实组合缝隙，不是外部场景，是自己给自己挖的坑，必须自查才能发现，光看单条规则看不出来。

**确认的方向（已落地）**：F-QUEUE-001正式文档QUEUE-F008的"核心配置"类别定义，已明确写入"DQ及其他模块自己的Gate判定/审批决策逻辑文件（如F-DQ-008 Rego策略）"这一类，具体路径仍待Stage10真实Repo确定，但类别定义现在就要点名，不能停留在容易被误解为部署配置的"核心配置"这种模糊说法。命中清单会让第68条第②项标准自动不满足，强制走独立Review，第69条的PolicyChallengeSet检查借此在Review环节被真正卡住执行。**方法论提醒**：这类"两条都对的规则组合后打架"的缝隙，只做单条规则的场景推演查不出来，必须在多条新规则都落地之后，专门做一次"这几条放在一起会不会互相拆台"的交叉检查——第67条已经确认过"深度查资料"和"场景推演"是两件事，这次进一步说明，就算做完了场景推演，"单条规则内部没问题"和"多条规则组合没问题"也是两件事，还得再加一层。

### 72. AI员工深度调研三家（ServiceNow/Salesforce/UiPath）后确认两处真实缺口，并且这次教训是"讨论完必须当场写进正式文档"（2026-09-04确认）

**起因**：用户提供ServiceNow官方33页Blueprint、ServiceNow→Matbox映射母文档、2份Salesforce Agentforce深度调研，要求查证"AI员工是不是想得太简单了"。查完后用户追问能不能提前用设计避免审核瓶颈，本会话又补查UiPath真实架构。三家公司起点完全不同（IT工单/CRM/RPA起家），独立收敛到同一套结论。

**查证结论与确认的方向（均已落地进F-AIEMP-001/F-ACTION-001正式文档本体，不是只停在讨论里）**：
1. **员工内部缺Subagent分工层**——ServiceNow真实案例T2 SOC员工内部是Analysis/Containment/Eradication/Review四阶段Agent各管一段，Salesforce真实案例Sales Account Management员工内部是Routing/Record Retrieval/Research/Slack Operations四个Subagent协作，两家都明确警告"一个员工=一个大Prompt"是错的。F-AIEMP-001原有的Skill实体是"能力有没有、输入输出是什么"，不是"这个能力背后有没有独立推理判断力"，两者是不同抽象层级。已落地为AIEMP-F006：EmployeeTemplate与Skill之间插入Subagent层，各自model_policy/evaluation_profile，通过AgentBinding显式绑定。**2026-09-04自查修正**：最初写的"交接复用F-ORCHESTRATOR-001已有Coordinator"是没读完F-ORCHESTRATOR-001全文就下的错误断言——实际Coordinator管的是员工与员工之间经F-DELEGATION-001的协作，不是一个员工内部Subagent之间的路由，层级不对不能混用；正确做法是员工内部路由由一个专职的路由型Subagent自己承担（参照本条已引用的Salesforce Routing Subagent真实案例），不新建也不误用跨模块组件，F-AIEMP-001正文已同步改正。
2. **缺确定性Workflow当整体工具调用这一层**——ServiceNow"provision user"案例：不是让AI每次临时想这几步顺序，是整条流程封装好、一次调用；UiPath真实产品Maestro用BPMN（公开国际流程图标准）+DMN（决策规则标准）定义这类流程，不是私有格式。F-ACTION-001原有ACTION-F001~F005管的都是单个动作，没有"预先把几步串成一条测试过的流程"这层。已落地为ACTION-F006：Workflow用BPMN+DMN定义，内部每步仍完整走10步Enforcement Pipeline，不降级单动作治理，新增P0规则7明确禁止"因为是预先设计好的流程就默认放行"。

**本条最重要的教训，不是查证内容本身**：这两个缺口是查完当场就写进F-AIEMP-001/F-ACTION-001正式文档本体的，不是只写进一份给第二个账号看的交接文档。用户当场指出一个更严重的问题——此前2天（2026-09-02至2026-09-04）关于AI员工的讨论，最后一次真正改到这6份AI员工正式文档的commit是2026-09-02 13:25，中间整整2天没有一次真正的文档修改，讨论完全停在对话记录里没有落地，用户原话"我们讨论的意义是什么，你让开发怎么进行"。**确认的规矩**：任何技术讨论一旦达成结论，必须当场写进对应的正式文档本体（不是写进摘要、不是写进交接文档、不是等讨论全部结束再一次性补），本条第①②两处缺口就是照这条规矩重新执行的示范——查完立刻改文档本体，不是先记着、以后再补。

### 73. AI员工底层架构缺Connector生态这一层，深度复核Salesforce/ServiceNow/UiPath三家商业与技术架构后确认（2026-09-05）

**起因**：用户直接指出"我们之前的底层架构是有问题的，因为我们没有考虑过Connector生态，这个是不长远的"，并要求对第72条已经查过的同三家公司（ServiceNow/Salesforce/UiPath）做更深一层的调查——不只看AI员工怎么分工，还要看他们的商业结构和"AI员工底层架构"整体是怎么立住的。

**查证结论**：三家公司在"要不要先建Connector生态"这件事上，独立收敛到同一个答案，而且时间线上是**先有连接生态、后有AI员工**，不是同步做的：
- Salesforce 2018年以65亿美元收购MuleSoft、买下Anypoint Platform，当时Agentforce尚不存在，买的是通用企业集成能力；2026年MuleSoft才回过头成为"官方所称的Agentic时代骨干"，技术架构是Control-Plane/Data-Plane分离+Agent Registry+Gateway，且已经用MCP协议对接外部工具（与Matbox本会话F-ACTION-001已经在用的协议一致）。
- ServiceNow的"Spoke"＝一组打包好的Actions（类型化输入输出）+ 一个Connection & Credential**别名**（流程/Agent只认别名，从不直接持有真实凭证，凭证生命周期由框架统一管理）+ 示例流程，通过ServiceNow Store分发；AI Agent运行时动态挑选合适的Spoke执行，架构分四层。
- UiPath的Integration Service是连通层，Marketplace是明确的连接器分发渠道——第三方能建连接器上架、UiPath代收款并分成，2021年数据已有1300+个上架listing，同一套连接器同时服务传统RPA自动化和新的Agent。

**查证是否与Matbox现有决定冲突**：不冲突，反而互相印证——三家的"Gateway+Policy层"和"凭证别名层"，Matbox在F-ACTION-001（10步Enforcement Pipeline）和F-CRED-001（credentialRef机制）里已经有对应设计，不需要重建。真正缺的、三家都有而Matbox没有的，是再上面一层：一个**按业务平台分组的具名Connector目录**——F-ACTION-001原有的ACTION-F002只做到单个工具/MCP server粒度的注册，没有"微信客服算一个连接器、底下挂多个相关工具、共享一套凭证"这层业务分组，这正好也是第46节交接文档"缺口③"里Salesforce"六个家底"清单点名过、但当时没有深入的同一处（Connector Registry）。

**确认的方向（已落地为ACTION-F007，架在ACTION-F002之上，不是新建平行系统）**：新增`ConnectorDefinition`实体，把一组相关ToolRegistration按业务平台打包成具名Connector（初始目录覆盖Matbox两条目标客户线：家居制造+电商侧的微信/抖音/小红书/Shopify，设计行业侧的酷家乐等，具体清单待缺口③业务对象调研确认），同一Connector下所有工具强制共享一个凭证作用域（对应ServiceNow"别名"教训，新增P0规则8禁止凭证碎片化），Connector在F-CONSOLE-001雇佣/配置界面按业务名称展示供租户勾选。**有意收窄的范围**：UiPath式的第三方连接器市场/分成机制，本轮不设计——Matbox现阶段是自己维护初始连接器目录（`sourceType=MATBOX_CORE`），字段预留但不为不存在的第三方生态过度设计，等真的需要开放给第三方贡献连接器时再补审核/分成流程。

Sources：[Agentforce MuleSoft](https://www.mulesoft.com/platform/agentforce)、[Extending Agentforce with External MCP Tools via MuleSoft](https://developer.salesforce.com/blogs/2026/08/extending-agentforce-with-external-mcp-tools-via-mulesoft)、[Salesforce's MuleSoft acquisition shows API management appeal](https://www.techtarget.com/it-infrastructure/news/252437506/Salesforces-MuleSoft-acquisition-shows-API-management-appeal)、[ServiceNow Integration Hub](https://www.servicenow.com/products/integration-hub.html)、[ServiceNow Spoke Development Best Practices](https://github.com/ServiceNowDevProgram/SpoketoberfestResources/blob/master/SpokeDevelopmentBestPracticesPublic.md)、[AI Agents for Integration Hub](https://www.servicenow.com/community/workflow-data-fabric-blog/ai-agents-for-integration-hub-data-matters-most-in-the-world-of/ba-p/3513084)、[UiPath Integration Service](https://www.uipath.com/product/integration-service)、[UiPath Marketplace Monetization announcement](https://ir.uipath.com/news/detail/200/uipath-marketplace-unveils-monetization-of-automation-content-opening-new-revenue-stream-for-partners-and-creating-faster-delivery-of-automation-projects)

---

## 第74条：交接包锁定的依赖版本必须独立复核；开工前的破坏性升级成本为零（2026-09-08）

**起因**：用户交付基础层「语言包」开发包（Stage 9 PASS，含 DEV.docx 2,028 段、4 个 JSON、可运行 POC、SHA-256 MANIFEST）。该包质量明显高于此前九份供应商调研报告——但对其锁定的三个依赖版本做独立复核后，查出一处会长期拖累平台的选择。

**查到的事实**：

1. **锁定版本本身没问题，锁定的"理由"有问题。** 包中锁 `vue-i18n 9.1.9`，理由写「锁定 DCloud 实际版本线；禁止 Codex 擅自升 v11」。本会话查 npm 实际依赖树：`@dcloudio/uni-app`（latest / vue3 / alpha / next 四个 dist-tag 全查）**均不依赖 vue-i18n**，DCloud 自带 `@dcloudio/uni-i18n`。**"框架卡版本"的事实前提不成立**，vue-i18n 是项目级自由选择。

2. **9.1.9 发布于 2021-10-06，且该大版本线官方已 EOL。** vue-i18n 官方维护页原文：`Vue I18n v9 and Vue I18n v10 has reached EOL and is no longer actively maintained.` npm registry 显示 9.x 共 46 个正式版，9.1.9 排第 11，后面还有 35 个；该线最后稳定版 9.14.5（2025-07-16，官方 `stable9`/`legacy9` 标签）。**按包开工等于第一天就站在 EOL 线上。**

3. **v11 的破坏性改动（`tc`/`$tc` 移除、Legacy API 废弃、`v-t` 废弃）只在有存量代码时才是成本。** Matbox 本地化尚未写入任何一行代码——这是开工前交接包。**迁移成本为零。**

**确认的原则（三条，全平台通用）**：

- **74.1 交接包锁定的每个依赖版本，必须独立核对三件事**：① 该版本的真实发布日期；② 该大版本线的官方维护/EOL 状态；③ **包中给出的锁定理由是否被实际依赖树支撑**。三者缺一，锁定不得直接继承。本次正是第 ③ 项查出了问题——理由写得很具体，但查依赖树发现不成立。
- **74.2 尚未写入任何实现代码时，一律锁定"当前仍在维护的 latest"，不继承交接包的历史锁定。** 破坏性改动的成本只对存量代码存在；开工前继承一个 EOL 锁定，是把必然发生的迁移成本推迟到更贵的时候。
- **74.3 每个技术选择必须记录被淘汰的候选与否决理由（开发包标准第 9 条）。** 本次交接包全文检索 `Weblate`/`Crowdin`/`Lokalise`/`i18next`/`FormatJS` **零命中**——选了 Tolgee/next-intl/vue-i18n，但没写"为什么不是别的"。**这一条不补，后人无法判断该选择是权衡过的还是随手定的。** 已登记为 G-LOC-MATBOX-003，阻塞全模块 Stage 10 物理绑定。

**附带确认的两条验收纪律**（由同一次核验产出）：

- **自动化测试的"全过"必须验其是否真的测到了代码。** 该包 POC 声称 12/12 PASS，实跑确认属实；但把其中 2 条抽到**零依赖空环境**（不 import 任何模块、不定义任何被测函数）后**仍然 PASS**——它们断言的是紧邻上一行自己写的字面量，未调用任何被测函数。**真实覆盖是 10 条。** 判断测试是否有效的可操作方法：**把被测代码全部拿掉，看它还过不过。**
- **语言归一与 Locale 归一必须是两个独立函数。** 该包 POC 的 `canonicalize()` 把 `en-US`/`en-GB`/`en-IN` 全映射为 `en`——做语言归一正确，但若复用于 locale 会**静默抹平 en-IN**，而其自身 AC-LOC-022 恰恰要求 `Language=en + Locale=en-IN` 共存并遵守印度语境。

**落地**：[Matbox_全球多语言_正式开发文档_V1.0-RC.md](Matbox_全球多语言_正式开发文档_V1.0-RC.md)（F-LOC-001）。三个组件升至上游 latest：vue-i18n **11.4.10**（`87510e2f9344e0734bfe9fbc7e0a2c509042e159`）、next-intl **4.14.2**（`3259beb76120614af8b48e814c56324ad71ccb61`）、Tolgee **v3.221.0**（`3bae7f3d4076b4f7de9073f7101c153b6e3c44f2`），三个 commit 均经 GitHub API 核实真实存在。

Sources：[vue-i18n Maintenance Status](https://vue-i18n.intlify.dev/guide/maintenance)、[npm registry vue-i18n](https://registry.npmjs.org/vue-i18n)、[npm registry next-intl](https://registry.npmjs.org/next-intl)、[npm registry @dcloudio/uni-app](https://registry.npmjs.org/@dcloudio/uni-app)、[tolgee-platform releases](https://github.com/tolgee/tolgee-platform/releases)

---

## 第75条：九家供应商独立收敛的八条，已冻结为 Matbox 架构约束（2026-09-08）

**起因**：用户侧交付九份供应商开发级调研报告（Zapier / Microsoft / UiPath / Activepieces / ServiceNow / Salesforce / Fin·Intercom / Workato / Composio，合计约 20,700 段）。本会话对九份做 100% 通读 + 独立数字回查，产出 [专项00q 证据附录](技术选型报告/Matbox_专项00q_供应商开发报告双向核验证据附录_2026-09-05.md)（974 行三态标注事实库）与 [专项00r 横向收口](技术选型报告/Matbox_专项00r_九家供应商横向收口_2026-09-08.md)。

**方法论上的一条元原则（本条最重要的部分）**：

> **跨公司调研里，最强的论据不是任何一家说了什么，而是「N 家互不相干的供应商、从各自完全不同的业务出发、独立走到同一个设计」。**

单家的说法可能是营销、可能是其产品形态的历史包袱。但当 ServiceNow（IT 运维）、Salesforce（销售 CRM）、Microsoft（企业低代码）、Workato（企业 iPaaS）四家从四个完全不同的业务出发，各自独立把「一个 AI 员工」拆成「多个子 Agent + 专职路由 + HITL」时，这个结构大概率是被业务本身逼出来的，不是谁抄谁。**本轮所有冻结项都按「几家独立到达」标注证据强度，不按「哪家名气大」。**

**已冻结的八条收敛**（每条括号内为独立到达的家数）：

1. **一个 AI 员工 = 多个子 Agent + 专职路由 + HITL**（4 家）。AI 员工不能是"一个大 Prompt"。其中 Salesforce 的 `Sales Management Routing`——**一个自己不带任何 Action、只负责路由的 Subagent**——是四家里最干净的设计。
2. **确定性流程骨架 + LLM 适应，不是二选一**（5 家）。能写文章/总结/判断意图的让模型做；价格、库存、权限、审批、写库、退款、报价计算交给 Flow/API/规则系统。补一条 ServiceNow 的洞察：把"创建采购单""发布商品"封成**受控 Workflow Tool** 给 Agent 用，而不是让 LLM 每次临时拼 API 顺序。
3. **执行身份 ≠ 员工身份，必须显式分 Runtime Profile**（4 家）。最硬的证据来自 Workato——不是设计理念，是**实际产品限制**：verified user access skills 与需审批的 Genie 跑不了完全自治的 orchestration。→ 已落 F-AIEMP-001 §2.5 + P0规则8/9。
4. **Provider 报的状态不可信，必须自己再验一次**（4 家）。Composio #4120：垃圾 API Key 也能创建 `status=ACTIVE` 的连接，26/26 复现。→ `ACTIVE` 与 `VERIFIED` 分两个字段；Catalog/Search/Schema/Execute 四态独立 Health。已落 F-ACTION-001。
5. **Webhook 不是可靠消息队列**（3 家）。Activepieces #14328/#14808：Connection 错误若发生在 Run 创建之前，没有 Run 可供告警捕获，调用方还可能收到 HTTP 200。→ TriggerAttempt 必须独立于 Run。已落 F-ACTION-001。
6. **Evidence 必须 Matbox 自己长期存**（5 家保留期实测：7 天 / 30 天 / 30–90 天 / 89 天 / 180 天）。**最短的 Zapier Action Run 只有 7 天。** 没有任何一家能承担 Matbox 的业务证据责任。已落 F-OBS-001 §2.5 + P0规则6/7。
7. **连接器数量 ≠ 能力**（**9 家全部**主动否定自己的数字）。验收单位必须是 `Connector + Action + 输入输出 + Scope + Risk + 真实成功 Test`。已落 F-ACTION-001 `ConnectorAction` 契约 + P0规则9/10。
8. **AI 变更必须走 Eval → Release → Monitor**（3 家）。"改完提示词自己点两下看看"不能进生产。

**同时冻结的三条量纲纪律**（来自 [00q §7 口径归一表](技术选型报告/Matbox_专项00q_供应商开发报告双向核验证据附录_2026-09-05.md)）：

- **唯一可比的量纲是 Action，不是 Connector。** 只有两家同时公布了两个数：Zapier 9,000+ apps / 40,000+ actions ≈ **4.4**；Salesforce MuleSoft for Flow 90+ connectors / 1,500+ actions ≈ **16.7**。**密度差约 3.8 倍——用连接器数排名，等于给「把能力切得更碎」的一方加分。**
- **厂商官方数字互相打架是常态，不是例外。** 本轮遇到四次：Zapier（同一天 Newsroom 3.4M / About 2M+ / 首页 3M+ businesses，且三个词互换不定义）、Microsoft（官方专有一页讲不同 admin surface 的 Agent 数口径不同）、Workato（工程 Docs 1,000+ connectors vs 营销页 12,000+ apps）、Composio（同一天 llms.txt 1,400+ / toolkits 页 1,467 / Pricing 1,500+）。→ **任何供应商数字默认假定还有另一个官方版本，只引区间不引单值，且必须带来源页 + 抓取日期。**
- **中国渠道缺口有两种，不能合并成一句「不支持」。** ServiceNow（Australia 版全量 302 行）/ UiPath（158 个 Connector 家族）/ Composio（live toolkit 目录 1,467 个）**三份完整目录逐条核查 = 零个中国平台**，属能力缺失；Workato 则**有 CN 数据中心、有连接能力，但官方逐字关掉 AI/MCP**（`MCP isn't available to workspaces in the CN data center`），属政策切断。前者要自建连接器，后者即使采购也拿不到 Agent 层。

**落地**：11 项回改已于 2026-09-08 全部写入正式文档，新增 P0 冻结规则 9 条（F-ACTION-001 的 9~15、F-AIEMP-001 的 8~9、F-OBS-001 的 6~7），15 个新错误码（错误码规范 §4.1）。落点明细见 [Feature Register「专项00r 横向收口回改落地」](Matbox_Feature_Register.md)。

**一条没有任何供应商能给的结论**：把九家业务域摊开，**没有一家覆盖家居设计与渲染、商品 SKU×材质×尺寸×工艺的行业数据模型、设计师—厂家—经销商—终端客户四方协作、CAD/3D/效果图作为一等业务对象、中国经营渠道**。ServiceNow 报告原文写得最直白：「设计本身不是 ServiceNow 的强业务域；Matbox 必须自己产品化。」→ **Matbox 的护城河不可能是"连接器多"或"模型强"，只能是行业业务上下文 + 中国渠道 + AI 员工的行业工作流。**


### 76. VIZ 模块更名为「代码问题定位与人工修复台」，并确认与 DQ 分开不合并（2026-09-08 确认）

**背景**：用户问 F-DQ-001~013（代码持续质检与安全自检）和 F-VIZ-001~007 内容上是否重复、要不要合并。逐字比对两份源文档后确认——

**分开这件事，两份文档自己早就写死了**：VIZ 文档附录A 原文「本专项是独立文档……只读取并可视化这些事实，**不替换、不复制、不合并**」；架构地图第 528 行也早记着「两个文档故意不合并成一份（CQRS模式）」。本条不推翻，只补记两条新查到的事实。

**内容重叠的真实性质是投影，不是两套**：VIZ 要求的 16 个字段里，9 个直接来自 DQ 的 `NormalizedFinding`（原文字段：findingId, qualityRunId, sourceScanner, ruleId, category, severity, confidence, file/path/location, fingerprint, ...），6 个是 DQ 完全没有的定位/关系字段（StartLine/EndLine、Symbol、Language、LocationConfidence、RelationConfidence、Impact）。修复闭环两边都写了，但 `RepairAttempt` 在 DQ 的 OWN 列里，VIZ 只是第二条触发入口（人点按钮），DQ 那条是机器自动。DQ 自动修不动时转 `BLOCKED_MANUAL`，而 DQ 自己没有人工修复界面——VIZ 正是那个界面。是接力，不是重叠。

**不合并的决定性理由（新增）**：成熟度差两级。DQ 是 `STAGE9_CONTENT_AUDIT_PASS`，VIZ 自述「开发指导版」、7 项 POC 全部「待源码/待开发」。合并等于让已过 Stage 9 的 DQ 被还没过 Stage 8 的 VIZ 拖回去。

**更名**：原名「代码问题可视化定位与智能修复中心」→「**代码问题定位与人工修复台**」，模块编号 F-VIZ-001~007 不变。理由：①「修复中心」暗示 VIZ 拥有修复能力，事实源却在 DQ，名大于实——架构里反复警告的建第二套系统，最容易的入口就是名字先叫上了；②CQRS 里 VIZ 是读侧，名字不该带写侧动词；③「中心」= 中枢/事实源，而 VIZ 恰恰不是事实源。查证真实做法：GitHub 把这套拆成 CodeQL/Code scanning（引擎）、Copilot Autofix（自动修）、Security overview（看板）、Security campaigns（人工分诊），**看板那两个名字里一个「修」字都没有，修复能力单独命名且归引擎那边**。CQRS 读侧的中文命名没有正式业界标准（查过，只有 Projection / Read Model / Query Service 这类英文技术词），所以按语义自定。用户在三个候选里选了「人工修复台」，理由是它与 DQ 状态表的 `BLOCKED_MANUAL` 字面对应，自动→人工的链条读得通。

**执行**：按第57条确立的「必须彻底改完，不留历史包袱」标准，全变体普查后一次改完——11 个文件 38 处文本、3 层目录、2 个文件名、docx 内标题，历史「专项XX」报告一并改；改完 Grep 复验六种旧写法**零残留**。改动涉及 `viz_repair_loop_flow.html`，已按规程重跑 `_build_flow_embeds.py`。

**How to apply**：以后给任何模块起名前先问一句——这个名字里的名词（中心/引擎/系统/平台）和动词（修复/审批/发布），是不是它真的拥有的东西。名字比权限大，就是下一次「建了第二套」的起点。

**这次自己踩的坑，一并记下**：写本条时用 shell 单行命令把带反引号的 Markdown 文本传给 Python，反引号被 shell 当成命令替换执行掉，`NormalizedFinding` / `RepairAttempt` / `BLOCKED_MANUAL` 等 6 处字段名被静默吞成空白，写进文件时才发现（终端报了 `command not found`）。**以后凡是含反引号、`$`、双引号的正文，一律先写成文件再拼接，不走 shell 单行命令**——同类坑此前已经在撇号（`Winter '26`）和直双引号上各踩过一次。

Sources：[GitHub Code Security](https://github.com/security/advanced-security/code-security)、[About code scanning — GitHub Docs](https://docs.github.com/code-security/code-scanning/automatically-scanning-your-code-for-vulnerabilities-and-errors/about-code-scanning)、[CQRS — Confluent](https://www.confluent.io/learn/cqrs/)、[Designing Read Models in DDD×CQRS](https://zenn.dev/135yshr/articles/60293061fe34dd?locale=en)


**2026-09-08 同日补：比对中查出两处契约对不上，已改（只动 VIZ，DQ 一个字没动）**

这两处不是"合不合并"的问题，是 VIZ 单方面写错，会直接把界面做错：

① **状态枚举缺 5 个，缺的那个正好是本模块存在的理由**。VIZ 原文写「状态枚举**固定为**：PASS、DENY、ERROR、TIMEOUT、INCOMPLETE、REVALIDATION_REQUIRED、NOT_AVAILABLE」——七项封闭清单；而 DQ 第6节状态表有 10 个。VIZ 接不住 CREATED、RUNNING、SCANNER_ERROR、POLICY_ERROR、`BLOCKED_MANUAL`。`BLOCKED_MANUAL` 尤其致命：DQ 的 DENY 行原文写「生成 RepairPlan 或转人工（进入 BLOCKED_MANUAL，已接通 F-APPROVAL-001 通知）」，而"转人工"正是人工修复台存在的全部意义——人被转过来了，界面却没有这个状态可以显示。原清单里的 ERROR 一并作废：它把 SCANNER_ERROR 与 POLICY_ERROR 揉成一个，两者处置方式完全不同（前者按错误分类重试、超限转 BLOCKED；后者必须由 Security/Quality Owner 修 Policy，明令禁止降级绕过），而验收标准 DQ-T007 恰恰要求这类状态「不能混为一谈」。**改法：状态词不由 VIZ 自定，一律写明以 DQ 第6节为唯一来源**；NOT_AVAILABLE 保留，但移到独立的定位维度字段，不再与 Gate 状态挤在同一个字段里。

② **FindingConfidence 整个漏掉了**。DQ 的 `NormalizedFinding` 本来就有 `confidence`（这条问题有多可能是真的、而非误报），但 VIZ 统一数据合同的 18 个字段里一个都没有它——等于把人工判定误报（对应 DQ 的 F-DQ-010 Suppression/False Positive 治理）的主要依据在界面上丢掉了。已补入，并写明三个置信度各是什么、不得合并成一个数字：一条规则命中完全可以是 99% 确定的（FindingConfidence 高），而行号是从提交差异推出来的（LocationConfidence 低）；合并显示会让人以为"这一行确定有问题"，与 VIZ 自己上一句"不得伪造行号"直接矛盾。

**真正要紧的落点不是那份 .docx，是 `contracts/visualization-view-contract.yaml`**。那是 Codex 真正拿去写代码的机器可读契约，里面一字不差地写着同样的 7 项枚举、同样没有 FindingConfidence。**文档改了而契约没改，等于没改。** 已一并升到 v2，把三个置信度的定义和"未知 Gate 状态必须原样透出、不得降级成一个笼统错误态"写成显式规则。

**How to apply**：① 一个模块引用另一个模块的枚举/字段时，**不要抄一份下来写"固定为"，要写明"以某文档某节为唯一来源"**——抄下来的那一刻它就开始过期，而且过期没有任何提示。② 改完正式文档，必须回头查同一个开发包里的机器可读契约（yaml/json/schema），那份才是真正被执行的东西。

### 77. 残留问题不靠"下次更小心"解决，靠一个能跑的检查器（2026-09-08 确认）

**背景**：同一天里，用户连续三次替我发现残留——VIZ 改名后第 7 种写法藏在流程图 `<title>` 里、同一道 Gate 有两个编号、开发总账 10 处 `（MISSING，阻塞）` 停在半个月前。用户原话：

> 「别让我一次次问了，我觉得你应该彻底去解决代码残留的问题，避免你一次又一次的如此混乱」

**根因不是态度，是方法**：以往每次改名都是"先列出所有写法 → 全局替换 → Grep 复验零残留"。这套流程有一个结构性缺陷——**复验用的清单是我自己列的**。我没想到的写法，替换时漏掉，复验时同样漏掉，两道工序共用同一个盲区，所以"零残留"这句话在方法上就不可能成立。第 57 条那次 784 处替换之所以成功，是因为格式规整（`XX-FNNN`）能用正则穷举；换成中文名字就不行了。

**解决方案**：`docs/_check_docs_consistency.py`，一条命令跑完五项硬检查：

| 项 | 查什么 | 对应哪次真实事故 |
|---|---|---|
| C1 | `flow_embeds.js` 是否比 `*_flow.html` 旧（比 SHA-256） | 2026-09-08 引入 srcdoc 方案时我自己新挖的坑：改了流程图忘了重跑生成器，架构图里就是旧副本，症状是"我明明改了，点开还是老样子" |
| C2 | 退役写法残留，**含 .docx** | VIZ 改名第 7 种写法漏网 |
| C3 | 文档内部链接指向的文件是否真的存在 | 改目录名/文件名之后断链，Markdown 毫无提示 |
| C4 | 同一模块在画布/流程图 title/h1 三处各叫什么 | DQ 一个模块有 4 个名字，从没人发现 |
| C5 | 后缀相同但全名不同的 Gate 编号 | `G-DQ-ST10-REPO-001`(1次) 与 `G-STAGE10-REPO-001`(29次) 其实是同一道门，当日已统一为后者 |

关键设计差别：C4/C5 **不依赖我事先列清单**，而是从文件自身推出"应该一致的东西"再比对——画布的 `FULL_PAGE_EMBEDS` 自己说了哪个节点对应哪张流程图，那三处名字就该能对上；Gate 编号后缀相同就该是同一个号。盲区因此不再由我的想象力决定。

**一条同样重要的子原则：宁可少报，不可乱报**。第一版跑出 13 个 FAIL，**全部是误报**——把注释里当例子写的 `src="本地.js"`、JS 字符串拼接 `' + d.html_url + '`、以及第 57 条正文里作为历史记录写的旧编号，都当成了残留。会喊狼来了的检查器，人看两次就不看了，比没有更糟。因此：HTML 注释和 `<script>` 先剥掉；"同一行里旧名和新名同时出现"视为改名说明而放行；模块名只出对照表不判故障（模块名与流程名本来就允许不同）；确实该保留旧名的地方走 `ALLOW_EXCEPTIONS`，**每条必须写理由且会被打印出来，豁免不藏着**。

**验过它是真的在查**（按第 74 条"要检验一个测试是不是真的，就把被测代码拿掉看它还通不通过"）：故意制造三个残留——改流程图不重跑生成器、写回一个旧名、加一个断链——C1/C2/C3 各自准确报出，还原后退出码回 0。

**How to apply**：
1. **每次改名、改编号、改任何 `*_flow.html` 之后，跑一次**：`python docs/_check_docs_consistency.py`
2. **提交前跑一次**，FAIL 不为零不提交（退出码非 0）
3. **每完成一次改名，把旧写法登记进 `RETIRED`**——加一行的成本极低，换来的是以后任何人写回旧名都会被当场抓到
4. 不要为了让它变绿而放宽规则；确实该豁免的走 `ALLOW_EXCEPTIONS` 并写清理由
