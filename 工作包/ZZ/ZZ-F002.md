# ZZ-F002 · 哨兵功能 2

> ⚠️ **这不是真模块。** 这是「写死检测哨兵」——内容全是占位的，
> 唯一用处是让生成器与检查器面对一个**没人给它开过小灶**的模块。
>
> 为什么要有它：2026-09-09 全球多语言那一轮，6 处「代码里只认 DQ」
> 全靠用户一个个撞出来。只有两个模块时，写死几乎不会露馅——
> 因为可以给第二个也单独加一行，凑合就跑通了。
> 第三个模块没人开小灶，**写死的地方会一次性全炸出来**。
>
> 用户 2026-09-10 定：**留着当常驻哨兵**，不是跑完就删。

## 0.5 · 开工五步

```bash
git clone https://github.com/xyhzai/matbox.git
cd matbox
git checkout -b wp/zz-f002
mvn -B verify
```

Java 包名 `com.matbox.sentinel`；代码写在 `backend/modules/sentinel/` 下。

## 1 · 你要做什么

**目标**：占位。哨兵模块不产出真实代码。

## 2 · 你能改哪里，不能碰哪里

- ✅ `backend/modules/sentinel/src/main/java/com/matbox/sentinel/**`
- ✅ `backend/modules/sentinel/src/test/**`
- ❌ 其它模块目录

## 3 · 七类施工面

### Frontend/UI｜N/A
### Backend/API/Service｜N/A
### Data/DB/Migration｜N/A
### Async/Runtime｜N/A
### Provider/External｜N/A
### Infra/Config｜N/A
### Tests/Observability｜N/A

## 4 · 你的验收标准

| TestID | 场景 | PASS 标准 |
|---|---|---|
| ZZ-T002 | 占位 | 占位 |

### 4.1 测试方法名必须带 TestID —— 这是硬要求

```java
@Test
@Tag("ZZ-T002")
void zzT002_placeholder() { }
```

### 4.2 测试上的三条红线（会被自动查）

1. 不许动不归你的测试
2. 不许删掉或禁用已有测试
3. 不许把测试掏空留壳

## 5 · 代码怎么写

### 5.1 你要实现的 API 端点
| 方法 | 路径 | 幂等键 |
|---|---|---|
| GET | `/zz/ping` | — |

### 5.2 你要建的表
```sql
CREATE TABLE zz_placeholder (id uuid PRIMARY KEY);
```

### 5.3 你的代码放哪
```
backend/modules/sentinel/src/main/java/com/matbox/sentinel/
```

### 5.4 拆好的开发任务
占位。

### 5.5 技术栈（不用再查）
Java 17 / Spring Boot 3.3.5 / **PostgreSQL** / Flyway。

### 5.6 鉴权、权限与多租户
占位。

### 5.7 错误码、重试、超时与降级
占位。

## 6 · 开工前必读的架构禁令

- **架构原则第58条**：模块之间不许互相 import。

## 7 · 完工时必须交出什么

- AcceptanceRunID

## 8 · 怎么起本地环境

占位。
