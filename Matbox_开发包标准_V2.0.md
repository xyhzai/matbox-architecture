# Matbox 开发包标准 V2.0

**适用范围**：全部约 200 个功能包，无例外。
**立标日期**：2026-09-10（V1.0 于 2026-09-09 立）
**取代**：《Matbox 开发包标准 V1.0》。V1.0 留档不删，用于对照本版改了什么（见 §12）。
**来源**：V1.0 抽自 DQ 样板；**V2.0 抽自「拿第二个、第三个模块去撞 V1.0」的实测结果**。

---

## 0｜V1.0 哪里不够（这不是修辞，是三条实测）

V1.0 立完之后，我假装成 opencode，拿全球多语言的 `LOC-F001` 从头走一遍真实流程。
仓库当时**全部检查都是绿的**。走下来撞出 8 处，外加事后清点又出 3 处：

| 出事的地方 | 写死/漏掉了什么 | 后果 |
|---|---|---|
| `_check_test_integrity.py` | `dqT(\d{3})_`、`dq_test_ownership.json` | 开发中三条红线对第二个模块**跑不起来** |
| `_check_delivery.py` | `"DQ-T"` 前缀 | 串模块时把 `locT001_` 记成 `DQ-T001`，**发了一条假绿** |
| `_build_all.py` | 整张步骤表只有 DQ | 改了 LOC 源头重跑全链，**LOC 不重算**，下游照渲染旧内容，全绿 |
| 反向测试 | 根本不存在 | 这两个检查器**从来只被正向跑过** |
| `_build_public_export.py` 的 `MODULES` | 登记表的第二份手抄件 | 站上只有 1 个取件页，被指派 LOC 的 AI 找不到自己那份 |
| 同上，工作包目录扫描 | 不认 `public: false` | **哨兵模块被发上了公开站** |
| 同上，`^F-[A-Z]+-\d{3}\.md$` | 写死编号**形状** | LOC 取件页渲染出**只有表头的空表**，页面还是绿的 |
| 同上，`EXTRA` | 写死 DQ 的三份材料 | LOC 取件页的边界文件点开 **404** |
| `_check_ready_to_build.py` 的 R9 | `if os.path.exists(...)` 静默跳过 | DQ 判 32 条、LOC 判 31 条，**三个模块都显示全绿**——分母偷偷变小 |
| 同上，哨兵参与开工判定 | 没有"不适用"这一档 | 整份检查永远停在「不能开工」，而那两条红是设计如此 |
| V1.0 文档自身 | 引用了 **P29~P32** 四个编号 | 这四条**从来没被定义过**，也没有任何程序实现——凭空的四条标准 |

**这 11 处里没有一处是 V1.0 能推出来的。** V1.0 的 41 条讲的是「材料要有什么」，
一条也没讲「判这些材料的程序本身可不可信」。

### V1.0 真正的缺口，一句话

> **V1.0 假定检查器是对的，只去判材料。
> 而这一轮全部 11 处，坏的都是检查器本身，材料一个字没错。**

而且它们坏的方式高度一致：**不报错，安静地少判或判到别人头上，然后发绿灯**。
DQ 十三个包次次全绿，恰好把「它只认一个模块」盖得严严实实——
**正向跑一万遍都是绿的，因为正向跑的一直是同一个模块。**

V2.0 因此多出两章：§2 假绿名录、§3 三条结构规则。这两章是 V2.0 的实质增量，
其余部分是 V1.0 经实测校正后的沿用。

---

## 0.5｜验收权在谁手里（用户 2026-09-09 定，位阶高于本文其余全部条款）

用户原话：

> **「你现在所有的标准都是自己制定自己的，你没有站在开发的交付审核。」**

这句话是对的，必须写在最前面：本文第 5~7 节那几十条，**是我定的、我产的、我判的**，
三个角色是同一个。「31/31 全满足」的真实含义只是
**「我按自己出的题给自己判了满分」**——那不叫验收，叫自评。

**本条与下列各条冲突时以本条为准：**

| | 谁说了算 |
|---|---|
| **最终验收标准** | **opencode 拿到材料能不能马上开发，无问题** |
| 本文第 5~7 节全部条目 | **只是代理指标** —— 猜「材料齐到什么程度它才能开工」 |

**代理指标全绿 ≠ 通过。** 全绿只意味着「可以送去试」。

**代理指标是可被推翻的**：opencode 每卡一次，就说明代理指标漏了一条 →
补进标准，并注明**是被哪一次实跑逼出来的**。
V2.0 的 §2、§3 整整两章，就是被这条规则逼出来的：
2026-09-10 那一轮不是 opencode 真的卡了，是我**假装成它走了一遍**，
走到第三步就卡住了。

### 0.5.1 四条硬性约定（用户 2026-09-09 定，V2.0 原样保留）

1. **每个开发包改完，必须全绿才算通过，并且要把「全是通过」摆出来给人看。**
   不接受「大致齐了」「主要的都有了」。摆的方式见 §9。
2. **最终验收以 opencode 能否马上开发为准**，本文的检查只是它的前置筛子。
3. **每个开发文档必须全部通过才算「准备就绪」**——**差一条就是没就绪**。
4. **就绪之后进对应模块的卡片弹窗**，与「代码持续质检与安全自检 Gate」现有弹窗同款，
   分 **开发前 / 开发中 / 运营中** 三段展示。

### 0.5.2 一条不许走的路

现有 ⚪（标准已定、检查未建）**5 条**，逐个点名：

| 条目 | 是什么 | 现在的后果 | 什么时候建 · 卡在什么上 |
|---|---|---|---|
| **M6** | 变异测试：把被测代码拿掉，测试必须失败 | 交一堆假测试也能全绿 | 第一个模块有真实现之后（现在 0 行业务代码，没东西可变异）·卡在 opencode 尚未开工 |
| **M7** | 留一手：留几条验收标准不给实现方，事后考它 | 选题算出来了，**没地方跑这场考试** | 同 M6，且要第二个 agent 独立写测试 · 卡在没有真 CI 作业位 |
| **D2** | 留一手那批也要通过 | 同上，跑不了 | 跟 M7 同一件事，M7 建成即建成 |
| **B** | 页面的开发前资料摘要（状态徽章） | 只知道块在，不知道每个徽章算得对不对 | 下一轮 · 判据：徽章状态必须逐个等于 module_phases.js 里的值（无外部依赖，可立即建） |
| **E** | 运行中：没数据必须写"待接入"，不许编数字 | 完全没查 | 第一个模块上线之后 · 卡在没有真实运行数据，现在建只能查"有没有写待接入" |

> 这个数字自己也被机器判：§10.2 的 **S7** 比对「这里声明几条」与「表格里实际标 ⚪ 几条」，
> 对不上就红。加这条的直接原因是——2026-09-10 我这里写「3 条 + 5 条」、
> 口头说「5 条」、表格里实际是 7 条，**三个数互不相同，谁都没发现**。
> 2026-09-10 晚：B2 和 C 已由 C15 建掉，从 7 条降到 5 条 —— 这个数字由 S7 盯着，改一处就必须两处都对。

**未建的检查不许显示成绿。** 只有两种合法做法：

- 把检查建出来，全绿才是真绿；
- 或者如实写「N 条已守 / M 条未建」。

**把未建当成通过，就是 §1 要拆掉的那种假绿。**

### 0.5.3 ⚪ 管的是「有没有程序拦」，不是「内容能不能缺」

> **没有检查 ≠ 可以不做。**

2026-09-10 撞出来的：§8.1 把 B（摘要）、B2（施工面展开）、C（12 张卡）标成 ⚪，
我就把它读成"这几块可以先空着"，于是把一个 **B2 和 C 全是空壳**的页面交了出去。
用户翻到底问「下面就没有了啊」。

标准当时只写了「⚪ 不许显示成绿」——那管的是**显示**。内容能不能缺，一个字没写。
所以补死这三条：

1. **标 ⚪ 的项，内容照样必须齐。** ⚪ 只表示"现在没有程序拦得住你"，
   不表示这块可以不做。
2. **标 ⚪ 的项，交付前必须人工逐条确认它有内容**，并在交付说明里写明
   "这几条是人看的，不是机器判的"。
3. **⚪ 是欠条，不是许可。** 每一条 ⚪ 都要在 §0.5.2 那张表里写明
   **什么时候建、卡在什么上**；建出来之前，它每次交付都要被人重看一遍。

> **这三条不是口号，是有人盯的。** §10.2 的 **S9** 逐条查：
> §0.5.2 的每一行 ⚪ 都必须填了「什么时候建 · 卡在什么上」，空的当场红。
>
> 加 S9 的直接原因：上面第 3 条写完的那一刻，**这张表自己就没有这一列** ——
> 跟 V1.0 写了「新增标准必须附负向验证」然后自己没执行，是同一种。
> 写完规矩不给它配一道检查，那条规矩下一次就会被我自己违反。

---

## 1｜铁律：机器判不了的，不算标准

> **一条要求，如果不能由程序判定「满足 / 不满足」，它就不是标准，只是建议。
> 建议会烂，而且烂了没有提示。**

- ❌「文档要写清楚」—— 判不了，不收
- ✅「验收标准必须有编号，且每个编号必须被某个功能认领，未认领的当场报错」—— 收

本标准每一条必须写满**六样**，缺一样这条就进不来。
**（前四样是 V1.0 的，后两样是 V2.0 新加的，加的原因就是 §0 那 11 处。）**

| 字段 | 意思 |
|---|---|
| **要什么** | 这条要求的内容 |
| **怎么判** | 哪个程序、判什么、判据是什么 |
| **拦在哪** | 不满足时被谁拦下、拦在哪个环节 |
| **出处** | 从哪抽出来的，或哪次事故催生的 |
| **反向验过没有** ⭐ | 故意违反一次，它**真的红了**吗？没红过 = 这条不算数 |
| **在哪几个模块上验过** ⭐ | 只在 DQ 上验过的，一律记 ⚪，不许记 ✅ |

> **为什么后两样是硬字段，不是"最好有"**
>
> V1.0 §6 第 4 条已经写了「新增标准必须附负向验证」，然后**这一条自己没被执行**：
> `_check_test_integrity.py` 和 `_check_delivery.py` 从建立起就没被反向跑过一次。
> 写在"标准怎么不烂"那一节里的要求，没有任何机制保证它发生。
> 所以 V2.0 把它提到**每条的必填字段**，并由 `_negtest_devphase.py` 逐模块执行。
>
> 「验过哪几个模块」是第二次教训：反向验证如果一直拿 DQ 去违反，
> **单模块盲区照样发现不了**。必须换模块。

> **格式细则**：「拦在哪」允许在整节节首统一声明，前提是该节所有条目确实由同一个程序拦。
> **整节不声明就是漏。一份自己都不守自己规矩的标准，没有资格要求别人守。**

---

## 2｜假绿名录（V2.0 新增 · 本版核心）

绿灯撒谎的方式不止一种。下面每一条都**真实发生过**（F15/F16 是 2026-09-10 写 §10.2 那道自检时，被它自己的反向测试当场考出来的），出处写在最后一列。
新写任何检查器 / 生成器，**必须逐条对照这张表自查**。

| # | 假绿的样子 | 真实案例 | 对策（机器的） |
|---|---|---|---|
| **F1** | **少判还不吭声** —— 分母偷偷变小 | R9 遇到没有自述文件的模块直接 `continue`；DQ 判 32 条、LOC 判 31 条，三家都显示"全绿" | 跳过必须落成显式 `⏭` 行并打印理由；`skip` 不进分母也不算满足 |
| **F2** | **扫到 0 条然后发绿** | 正则只认 `dqT\d{3}_`，换模块一条测试都匹配不到 | 检查器必须报**它扫到了什么**（几条、哪几个编号），0 条要当故障 |
| **F3** | **算到别人头上** | `locT001_ok` 被读成 `DQ-T001`，打印「✅ DQ-T001 通过 1 个测试」 | 编号前缀、方法名前缀一律从登记表取；串模块用例进反向测试 |
| **F4** | **跑不起来 ≠ 没问题** | `--wp LOC-F001` 直接报「归属表里没有」退出 1；上游若不看退出码就当"没跑" | 全链任一步非零退出即停，**不许继续往下生成半成品** |
| **F5** | **生成器不重算，下游照旧渲染** | `_build_all.py` 步骤表只有 DQ，改了 LOC 源头重跑全链，LOC 内容不变，全绿 | 登记表驱动全链 + guard：登记了模块却没申报生成步骤 → 直接停 |
| **F6** | **页面渲染成功但里面是空的** | LOC 取件页工作包表只有表头，0 行 | 该列几条就得列几条；**0 条当故障，不当空** |
| **F7** | **页面有链接但点开 404** | LOC 取件页的 `loc_protected_scope.yml` 没被拷进站 | 产出后逐条验链接可达，不可达即停 |
| **F8** | **只增不删的生成器** | Word 导出只加不删，撤掉的包旧文件永远留着还会发到公开站。**2026-09-10 又犯一次**：新写的架构图自动登记只会加不会删，撤掉模块后留下指向不存在文件的登记项 | 生成即全量重算，产出目录里多出来的要主动清理；登记类的按「文件在不在」剪枝 |
| **F9** | **⚪ 未建被显示成绿** | 立标当天 10 条⚪，汇总里混进"已满足" | ⚪ 单列，不进"已满足"；弹窗按 §0.5.2 如实标 |
| **F10** | **关键词命中 ≠ 能力具备** | 命中「Migration」实为「依赖别的模块的迁移」；命中「sla」是从 `Tran-sla-tor` 里抠出来的 | 改结构判定：找真的 `CREATE TABLE`、真的非空路径、真的方法+路径同现 |
| **F11** | **找不到就当没事**（静默兜底） | 测试里 4 处 `or [None,"?"]`，两边都没找到也判过 | 抠不到值必须**当场停并点名哪条正则过期**，不许退化成默认值 |
| **F12** | **声明了却没人执行** | 登记表明写 `public: false`，导出照样把哨兵发上公开站 | 声明字段必须有唯一的执行点（`is_public()`），且反向验过 |
| **F13** | **标准里的编号根本不存在** | V1.0 引用 P29~P32，四条从未定义、无程序实现 | 标准文档本身被机器判：编号必须能在检查器里找到（§10.2） |
| **F15** | **自己的注释成了自己的证据** | `_check_standard_doc.py` 的 S3 全文搜 `_check*.py` 找编号，而它自己的注释里就举了一个编造的 C 编号当例子，于是那个号被判成"检查器里有" | 判据限定为**会被打印出来的字符串**（紧跟引号），不是"源码里出现过" |
| **F16** | **正则在中文语境下静默失效** | 同上 S2/S3 用 `(?<![-\w])`，而 Python 的 `\w` 在 str 上**默认含中文**——编号紧贴汉字时（中文文档里的常态）一个都扫不到 | 中文文档里的匹配一律显式写 ASCII 字符类；`\b`/`\w` 碰中文必须先验一遍 |
| **F17** | **产出了但从没接上** | 《开发包标准》自己：文件生成了，**没有任何页面链接它、不在导出白名单里、也没有 Word 版**——V1.0 从建立那天起，定了一周的规矩谁都下载不到 | 凡是要求别人遵守的文档，必须出现在取件页上；取件页链接自检覆盖它 |
| **F18** | **看不见的字符** | 正文里混进一个退格符（shell 转义被吞掉写进去的），肉眼完全看不出，S1~S7 一条都没查到，**只有 Word 导出崩了才暴露**，而报错跟「标准文档」毫无关系 | S8：正文不许有控制字符 |
| **F19** | **判据只有判的人知道** | D1/D4 读 surefire XML 判「做完没做完」，而**工作包里一个字都没写报告在哪、怎么产生**；R3 一直在查 Base Commit，那个 SHA 也从没写进工作包 | 凡是用来判交付的东西，判据、位置、怎么产生，都必须印在被判的人手里那一份上 |
| **F14** | **喊狼来了**（含误报） | 死链告警里 5 条是正文举例；哨兵让开工闸门永远红；**判据绑排版**——V1.0 判「专项21b 缺 7 个章节」实为误报（它写 `/` 而判据找 `、`），R7 把 20 份已指派的工作包判成 UNASSIGNED 也是同一根因 | 具名豁免 + 写出处；不适用的判成 ⏭ 而不是 ❌ |

> **这张表是活的。** 每发现一种新的假绿方式就往里加一行，并注明是哪次撞出来的。
> **不许在这里写"理论上可能"** —— 只收真实发生过的。

---

## 3｜三条结构规则（V2.0 新增）

§2 那 19 种假绿，根因收敛到三条。这三条是**结构性**的，守住它们，一整类错不会再发生。

### 3.1 单一事实源：`docs/modules.json`

**任何脚本不许再抄一份模块信息。** 需要什么字段就往登记表里加。

已经因为"抄一份"栽过的：
- `_build_public_export.py` 的 `MODULES` 字典（只抄了 DQ）
- `_build_all.py` 的步骤表（只写了 DQ）

> 抄下来那一刻它就开始过期，**而且过期没有提示**（架构原则第 76 条）。

登记表现有字段：`name / feature_prefix / test_prefix / sources / source_kind /
java_package / module_dir / branch_prefix / wp_dir / ledger / public / sentinel /
build_steps / flow / scope / ready_state / implementer`。

**新增模块只改这个文件，不改任何检查器。**

> ⚠️ **这句话我先写进了标准，然后才去验——结果它当时是假的。**
> 2026-09-10 试装了一个第四模块（TT），只改登记表跑全链，连撞三处：
>
> | 撞到的 | 性质 |
> |---|---|
> | `build_steps: []` 被当成"没申报" | 报错信息叫人「写成空数组并在 note 里说明」，而代码用 `if not steps:`，`[]` 和缺键走同一分支——**照它指的路走，走不通** |
> | 必须手改 1.5 MB 的架构图两处 | C14 查得出漏登记，但查出来之后**仍然要人手动补**。查得出来 ≠ 不用人记得 |
> | 撤掉模块后架构图留下死登记项 | 新写的自动登记只加不删（假绿 F8，写对策的当天自己又犯一次） |
>
> 三处都已修。**现在这句话是验过的**，验的方式是真的加了一个模块、
> 跑通六条闸门、再原样撤掉并核对工作区逐字还原。

### 3.1.1 加一个新模块要交什么（2026-09-10 实测清单）

| 要交的 | 谁交 | 不交的后果 |
|---|---|---|
| `docs/modules.json` 里一条登记 | 我 | 全链根本不认识它 |
| 该模块的工作包（每功能一份） | 我，从源文档切 | R6 红 |
| 工作包里必须有 Java 包名、能改哪/不能碰哪 | 同上 | C10 逐份点名说缺哪一项 |
| `<mod>_protected_scope.yml` | 同上 | P19 红、C10 红 |
| 登记表 `ledger`（总账文档） | 同上 | 取件页会写「登记表没填」 |
| **Implementer 指派到具体名字** | **只有用户能定** | R7 红 |
| **代码仓库里该模块的落脚目录** | 需要先建骨架 | R8 红 |

**代码一行不用改。** 上面除了最后两项，其余都是这个模块自己的材料。
最后两项是现实类——文档写了不算，要真存在。

### 3.2 第二个模块规则

1. **任何检查器 / 生成器，必须在「不是 DQ 的模块」上跑通过一次才算建好。**
   只在 DQ 上验过的，一律记 ⚪。
2. **每条检查必须有反向验证，且反向验证要覆盖登记表里的每一个模块。**
   V1.0 只要求"故意违反一次"——违反的如果一直是 DQ，照样发现不了单模块盲区。
3. **不许写死编号的形状。** 不只是模块名：`F-DQ-001` 与 `LOC-F001` 前后缀顺序都不同，
   `^F-[A-Z]+-\d{3}\.md$` 这种正则就是写死形状。一律用 `feature_prefix`。
4. **页面上每一块内容，每个模块都必须拿得到。** ⭐
   这一条是 2026-09-10 补的：B2 和 C 区的生成器是
   `if not exists("<key>_blocks.json"): return ""` —— 只有全球多语言手工建过
   那份数据，别的模块**静默拿到空字符串**。脚本里一个模块名都没写死，
   照样只有一个模块有内容。
   **「不写死模块名」只是必要条件，不是充分条件**：还要保证每个模块都真的
   被喂到数据。判据是 §8.1 那张表里每一行，对每个模块都非空。
   落在 `_check_docs_consistency.py` 的 **C15**。

**落地**：`_negtest_devphase.py`，按登记表逐模块跑，当前 **39 条**（3 模块 × 13 种情形）。

> **业界对照**：这就是把变异测试（mutation testing，"用故意的错去考你的测试"）
> 上移了一层——**用故意的错去考检查器**。判据同样是 kill / survive：
> 该红的红了叫 kill，该红没红叫 survive，survive 的一条都不许放过。

### 3.3 哨兵模块常驻（ZZ）

ZZ 不是真模块，工作包内容全是占位，**唯一价值是存在**：
任何"只认 DQ / 只认 LOC"的写死，都会在第三个模块上露出来。

三条硬规矩：

1. **不许删。** 删了它，单模块盲区就再也没有自动发现的手段。
2. **必须参加检查器的反向测试。** 2026-09-10 发现哨兵自己缺席了——它没有归属表，
   反向测试遇到它直接跳过，三个模块只考了两个。
   **一个不参加考试的哨兵等于没有哨兵。**
   现由 `_build_sentinel_ownership.py` 从它的工作包正文导出归属表。
3. **不参与「能不能开工」的判定，但必须显式判成 ⏭。**
   它永远不会被开发、没有 Implementer、没有落脚目录；算进闸门会让整份检查永远红
   （F14 喊狼来了）。但静默过滤又会变成 F1，所以要打印出来。
   它在登记表里 `public: false`，不上公开站。

---

## 4｜四个阶段

| 阶段 | 回答的问题 | 谁来判 |
|---|---|---|
| **开发前** | 这个包能不能开工 | `_check_standard.py` + `_check_ready_to_build.py` |
| **开发中** | 在建过程中不许发生什么 | CI，按 diff 判 |
| **开发后** | 这个包算不算做完 | CI，按测试结果判 |
| **运行中** | 上线以后跑得怎么样 | 运行指标（本标准不覆盖，样板已有） |

---

## 5｜开发前 · 31 条

> **V2.0 的编号规则（重要变更）：标准里的编号 = 检查器里的编号，一个字不差。**
>
> V1.0 用 P1~P28 一套编号讲标准、检查器用 P/R 两套编号跑，
> 结果 **P29~P32 四个编号被引用却从未定义**，也没有任何程序实现——
> 凭空四条标准，谁都没发现（假绿 F13）。
> V2.0 取消并行编号：下表就是 `python docs/_check_ready_to_build.py` 和
> `python docs/_check_standard.py` 实际打印的那 31 行。
> **标准里出现检查器没有的编号，由 §10.2 的自检当场拦下。**

### 5.1 材料类 P1~P21（判据 = 结构化证据，不是关键词）

**拦在哪**：`_check_standard.py`。任一条不满足 → 该模块判为「不能开工」。

| # | 要什么 | 怎么判 |
|---|---|---|
| P1 | 技术栈定死（语言/框架/DB/迁移工具） | 有技术栈章节且点名到具体版本 |
| P2 | 源码目录结构 + 依赖方向铁律 | 含目录树与依赖方向声明 |
| P3 | API 端点清单 | 真的 HTTP 方法 + 路径同现 |
| P4 | Request / Response Schema | 章节存在且非空 |
| P5 | OpenAPI 3.0 文档 | 同上 |
| P6 | 鉴权 / 权限 / 多租户规范 | 同上 |
| P7 | 数据库 DDL + Migration | 真的 `CREATE TABLE` 语句 |
| P8 | Provider Adapter 接口规范 | 章节存在且非空 |
| P9 | Webhook / 回调 / 异步任务规范 | 同上 |
| P10 | 错误码 / 重试 / 超时 / 降级 | 真的错误码值 |
| P11 | 测试集 / Golden Set / 验收脚本 | 章节存在且非空 |
| P12 | 前后端开发任务拆分 | 同上 |
| P13 | 成本 / 性能 / 并发 / 安全要求 | 同上 |
| P14 | 开发阶段 / 优先级 / 工作量估算 | 同上 |
| P15 | 风险 / 阻塞项 / 备用方案 | 同上 |
| P16 | 给未来 AI 开发者的完整执行说明 | 同上 |
| P17 | 七类施工面逐条核实 | 每个功能七类都有内容，无空缺 |
| P18 | 验收标准 Given-When-Then **且逐条有稳定编号** | 匹配 `<test_prefix>\d{3}`，连续无跳号 |
| P19 | 允许 / 禁止修改的具体路径（可机检） | `<mod>_protected_scope.yml` 存在且**非空**，目录名能在目录树里找到 |
| P20 | 工具版本锁到完整 40 位哈希 | 正则 `[0-9a-f]{40}`，不接受 tag 或短哈希 |
| P21 | **验收标准 ↔ 功能的对应表存在，且标注了来源** | 见 §5.4 |

> **⚠️ 判不了这一档必须真的用上。** `_check_standard.py` 第一版定义了 ⚠️ 却从来没赋过值，
> 定义了不用就是假绿（F9）。材料里有读不了的格式（PDF/二进制）时必须判 ⚠️，
> **⚠️ 不计入满足**。

### 5.2 现实类 R1~R8、R10、R11（文档说了不算，要去问真实世界）

**拦在哪**：`_check_ready_to_build.py`。判据一律去问 git / gh / CI / 真实仓库，
**任何一条只要是"读我们自己写的文档"，就不该出现在这一类里。**

| # | 要什么 | 怎么判 |
|---|---|---|
| R1 | 真实代码仓库可访问 | `gh api` 拿到默认分支与文件清单 |
| R2 | 仓库里有可编译工程 | 真有 `pom.xml` / `build.gradle` / `package.json` |
| R3 | 有可用的 Base Commit | 拿得到最新提交 SHA |
| R4 | 有 CI 配置 | 真有 `.github/workflows/` 或 `.gitlab-ci.yml` |
| R5 | 有数据库迁移目录 | 真有 `db/migration` 或 `V\d+__*.sql` |
| R6 | [每模块] 工作包已生成 | `docs/工作包/<MOD>/` 下 `<feature_prefix>NNN.md` 份数 > 0 |
| R7 | [每模块] Implementer 已指派 | 不是 `UNASSIGNED`；**排版无关**，表格式与加粗式都认 |
| R8 | [每模块] 多个功能共用的落脚目录都真实存在 | 按**功能数**判共用，不是按路径条数 |
| R10 | 公开站点没过期 | 去问线上那一页生成自哪个 commit，之后还动过 `docs/` 就算旧 |
| R11 | 代码仓库 CI 是绿的 | 默认分支最近一次 run 的 conclusion 必须是 success |

> **R11 为什么必须单独一条**（2026-09-09 真实发生）：给全球多语言建完
> `backend/modules/localization` 骨架后，R8 当场转绿、整套检查跑出 **9/9 全满足**
> ——**而 CI 是红的**（8 处 `cannot find symbol`，随后又撞 Flyway 历史表打架）。
> **「目录建了」和「它编得过」是两件事。**

> **原 R9「资料清单全绿」已于 2026-09-10 撤出闸门**，理由两条，各自都够：
> ① 它只对 DQ 生效且是静默跳过（假绿 F1）；
> ② 它读的是自述断言文件，而 §0.5 定死自评不算验收。
> 现改为每模块都打印的信息行，**不占分母**。撤掉一条检查必须留痕，
> 不许"文件里悄悄没有了"——那和当初静默跳过是同一种毛病。

**已撤销编号**：R9、P22、P23、P24、P25、P26、P27、P28、P29、P30、P31、P32

> 这一行是给机器读的，不是备注。§10.2 的自检据此区分两种情况：
> **「本文提到但没定义」是故障**（V1.0 的 P29~P32 就是这么混进来的），
> **「本文提到且明确写着已撤销」是正常**。
> 撤销必须留在这里，不许从文件里悄悄消失——那和静默跳过是同一种毛病。
> P22~P28 并入 R 系列（它们本来就由 R 判）；P29~P32 从未有过定义与程序；
> R9 见上。

### 5.2b 工作包必须自带的四样（opencode 2026-09-10 实跑逼出来的）

**拦在哪**：`_check_docs_consistency.py` 的 C10，逐份查，缺一样这份包就不算能动手。

**强制执行验过**：`_negtest_wp_must_have.py` 把 C10 的**每一条**必备项各删掉一次，确认它红、而且报得出是哪一条（当前 23/23）。新增必备项不用改那个文件，它自动多考一条。

> 加这道反向测试的直接原因：四条写进 C10、44 份工作包全过之后，用户问「写上强制执行了吗」—— **我从没让它在缺了的时候红过一次**。
> 全绿证明不了任何事：正则写错、锚点选错、根本没跑到，一样是全绿。

opencode 第一次拿到开发包，回过来说它还缺四样。按 §0.5，
**它每卡一次就是代理指标漏了一条**，所以逐条补进来并注明出处：

| 要什么 | 判据（C10 逐份查） | 缺了会怎样 |
|---|---|---|
| **确定的工作区** | 工作包有 `### 0.6 · 你的工作区`：仓库 / 落脚目录 / 分支 / 并行怎么隔离 | 只给 clone 地址和分支名，AI 不知道在哪儿干活，几十个同时开工会互相踩 |
| **确定的源码基线** | 有 `base_commit:` 后跟真实 40 位 SHA | R3 一直在查「有没有 Base Commit」，那个 SHA **却从没写进工作包**——AI 手里没有基线，事后无法复现 |
| **可读取的真实测试报告** | 出现 `surefire-reports`，写明怎么产生、路径、谁来读、判据 | **判据在我这边，被判的人不知道往哪交**（假绿 F19） |
| **包登记与代码地图状态闭环** | 出现 `code_map.json` 的链接 | 登记表/归属表/三段状态各存一块，没有一份机器能一次读全的 |

`code_map.json` 由 `_build_code_map.py` 生成，排在全链最前（工作包要从它读基线）。
它里面的「状态」是**算出来的**——去看仓库里真有什么文件，不是人填的。

### 5.3 每个模块判的必须是同一份 31 条（可当场核对）

```
DQ   31 条 {ok: 31}
LOC  31 条 {ok: 31}
ZZ   31 条 {ok: 12, no: 16, na: 3}   ← na 是哨兵的 R6/R7/R8，显式 ⏭
```

**分母必须一样。** 某条对某模块不适用，判成 `na` 并打印理由；
**不许从清单里消失**（假绿 F1）。

> 上面列的是**举例**，不是清单——200 个包时不可能在正文里逐个列。
> §10.2 的 S6 判的是不变量本身：**登记表里每一个模块，判的必须是同一份条目**，
> 有一个模块的条目集合不同就当场报出差在哪几条。
> 正文里列几个，只用来让人一眼看出「分母一致」长什么样。

### 5.4 P21 详解 —— 对应表的来源必须标注

**问题**：源文档给了两张表（N 条验收标准、M 个功能），**但没给它们之间的连线**。
判错的后果：某个功能以为自己不用管某条标准 → **那条没人测，直接漏出去**。

**三选一，按优先级**：

1. **源文档直接带这一列**（最好）。验收标准表加一列「考哪个功能」。
   这一条从"判的"变成"抄的"，问题永久消失。**新写的专项报告必须这样。**
2. **从第三张表机械推**。若有「组件目录 → 功能编号」表且验收标准正文点名了组件，
   可自动连线。**实测：DQ 用这个方法自动推对 13/24。**
3. **两方独立连线 + 比对**。第二方（不同模型/上下文）独立连一遍，
   **一致的放行，不一致的必须有人裁**。人只看分歧那几条。

**判据**：对应表必须标注自己是哪一种来源。标 3 的必须附比对结果；
标 2 的必须列出机器推不出来的那几条并说明怎么定的。
**不允许出现无来源标注的对应表。**

---

## 6｜开发中 · 8 条红线

**判据全部按 diff / CI 判，不看任何一方的自述。**

依据：实测编码模型有 **12%** 的情况会主动破坏那些用来发现它自己行为不端的代码；
最强模型检测别的 agent 作弊，54 类手法只查得出 **63%**（TRACE 基准）。
**模型互审兜不住，确定性检查不能省。**

| # | 要什么 | 怎么判 | 拦在哪 | 状态 | 反向验过 |
|---|---|---|---|---|---|
| M0 | 测试引用的编号必须真实存在 | HEAD 里每个 `<mod>T\d{3}_` 都要在验收标准里查得到 | `_check_test_integrity.py` T0 | ✅ | 3 模块 |
| M1 | 测试方法名带 TestID | 方法名匹配 `<mod>T\d{3}_`；`@Tag` 不算数（Surefire 不写进 XML）| C10 + CI | ✅ | 3 模块 |
| M2 | 不许动不归自己的测试 | 比对方法**原文**，非归属方法有任何改动即拦 | T1 | ✅ | 3 模块 |
| M3 | 不许删掉或禁用已有测试 | base 有 head 无；或新增 `@Disabled`/`@Ignore` | T2 | ✅ | 3 模块 |
| M4 | 不许把测试掏空留壳 | 同一方法断言条数变少 | T3 | ✅ | 3 模块 |
| M5 | 改动不许越出允许范围 | `git diff --name-only` 比对 `<mod>_protected_scope.yml`；越界文件**逐个列出**不合并 | `_check_scope.py` | ✅ | DQ |
| M6 | 测试必须是真的（变异测试） | 把被测代码拿掉，测试必须失败 | **未建** | ⚪ | — |
| M7 | 留一手：每类留 1 条，由验收方写 | 隐藏比例 30–60% 且每类都有；验收方 ≠ 实现方 | `_plan_holdout.py` 已算出选题，**跑的机制未建** | ⚪ | — |

> **M1 为什么编号必须写进方法名**：JUnit 5 的 `@Tag` **不会写进 Surefire XML**，
> 而 `<testcase name="...">` 永远在。开发后 D1/D4 只能从方法名反推编号。

### 6.1 M7 补充：为什么第二个 AI 就够，不必卡人工

> **同一个 AI 写代码又写测试，你拿到的不是测试，是一面镜子。**

成熟做法是 Coordinator–Implementor–Verifier：一个 agent 照契约写实现，
**另一个 agent 照同一份契约写测试、且不看实现**。
**独立性来自它读的是什么，不是它是谁。**

跟「让模型当裁判审另一个模型」要分开：那条只能查出 63%。
**这里模型只负责写测试，判定是测试跑出来的，是确定的。**

留一手留的是**对答案的机会**，不是题目本身：实现方看得到全部验收标准（它得照着建），
但拿不到验收方那份测试，也没有反复试的机会。

---

## 7｜开发后 · 5 条完工判据

**「做完了」的唯一定义：下面 5 条全绿。任何一方说「做完了」都不作数。**

| # | 要什么 | 怎么判 | 拦在哪 | 状态 | 反向验过 |
|---|---|---|---|---|---|
| D1 | 认领的每条验收标准都有对应测试**且通过** | 读 surefire XML；**失败、跳过、没有报告，都不算通过** | `_check_delivery.py` | ✅ | 3 模块 |
| D2 | 留一手那批也通过 | 验收方的测试在 Gate 时跑一次，全绿 | Gate（待建） | ⚪ | — |
| D3 | 无孤儿测试 | 编号必须**真实存在**（T0）+ 必须**归自己**（T1）；T0 扫 HEAD 全部测试文件，所以"标准被撤销、旧测试变孤儿"也查得到 | `_check_test_integrity.py` | ✅ | 3 模块 |
| D4 | 无覆盖缺口 | 认领的编号必须全部出现在测试里，缺的逐条列出 | `_check_delivery.py` | ✅ | 3 模块 |
| D5 | 三 Gate 全 PASS | 代码质检 / 安全质检 / 自动化测试三关 | DQ 本身 | ✅ | 由 DQ 提供 |

**D1 + D4 是双向的**（DO-178C / IEC 62304 的正反向追溯）：
正向查「有标准没测试」= 覆盖缺口；反向查「有测试没标准」= 孤儿测试。**两边都要查。**

---

## 8｜模块页面文件包标准

用户原话（第 4 次指出后立的）：

> **「你理解的标准是随心所欲的乱放位置、想丢内容就可以丢内容？」**

**是我丢了内容。** DQ 样板的「开发前」有三块，我给全球多语言做的那一页只有第一块。
**丢的原因不是源文档没有**，是我没去取。

### 8.1 每个模块页面必须有的位置，顺序不许变

| # | 位置 | 里面必须装什么 | 内容从哪来 | 谁检查 |
|---|---|---|---|---|
| **A** | **开发交接包存放处** | **① 总账（完整开发文档）在最前**，② 该模块全部工作包 + Word；都可点开可下载，工作包份数 = 功能数 | `modules.json` 的 `ledger` + `工作包/<MOD>/` + `导出_Word/` | **C12** |
| **B** | **开发前资料摘要** | 31 条的状态徽章，**不用点开也不用悬停就能看见** | `module_phases.js` | ⚪ 未建专项检查 |
| **B2** | **施工面检查展开** | **每个子功能 × 七类施工面逐条列出**，一个不落；每格含 Owner / 允许范围 / 禁止 / 契约依赖 / 验收证据 | 源文档的施工面章节 | **C12**（块顺序）+ **C15**（列出的功能数 ≥ 工作包份数）|
| **C** | **开发资料 Ready-to-Build Package** | **12 张卡**，见 8.2 | 见 8.2 逐张来源 | **C15**（必须正好 12 张）|
| **D** | 开发中 | 该模块的流程演示（场景 / 阶段 / tab 切换） | `<mod>_flow.html` | **C1 + C4** |
| **E** | 运行中 | 运行指标；**没有真实数据就写"待接入"，不许编数字** | 运行观测 | ⚪ 未建 |

**顺序不许变**：A（先拿东西）→ B（一眼看齐不齐）→ B2（逐条核实）→ C（完整资料）→ D → E。
乱序 = 取件的人得自己找，等于没定标准。

> **A 区为什么单独强调「总账在最前」**：DQ 的存放处第一行就是 147 KB 的完整开发文档，
> 而全球多语言那一版**总账整行没有**，用户问「开发文档下载去哪了」。
> 更要命的是我的检查也没抓到——它查了「存放处在不在、下载按钮几个」，**就是没查总账在不在**。
> 原因：那些检查是照**我自己列的清单**写的，不是照 DQ 那一块**实际长什么样**写的。
> **拿自己的理解替代标准，检查就会跟着一起错。**
> C12 现在直接比对 `modules.json` 里登记的 `ledger` 份数。

> ⚠️ **B / C / E 三块目前没有专项检查（⚪）。**
> 按 §0.5.2，这三条**不许在弹窗里显示成绿**。
> 这是 V2.0 相对 V1.0 的一处**如实降级**：V1.0 把它们写成 P29~P32 四条，
> 看着像有人在管，实际上那四个编号从未定义、也没有任何程序实现（假绿 F13）。

### 8.2 C 区那 12 张卡，每张装什么

| 卡 | 标题 | 必须装 |
|---|---|---|
| ① | 身份与状态 | DocID / 版本 / 状态 / FeatureID 范围 |
| ② | Scope In / Out | In Scope 每个子功能一句；Out of Scope 明确列 |
| ③ | Own / Reuse / Must Not Rebuild | 自研什么、复用什么、**禁止重造什么** |
| ④ | 依赖 & 冻结 Contract | 上游依赖 + 冻结契约字段 |
| ⑤ | 允许 / 禁止修改范围 | 具体路径；**给不出来就如实说给不出来并写明为什么** |
| ⑥ | Implementation Surface 施工面 | 七类逐项 Applicable / N/A + 负责人 |
| ⑦ | **验收标准 Given-When-Then** | **全部**验收标准，G-W-T 句式 + 稳定编号 + 原文对照 |
| ⑧ | 存疑 / 待确认 | **不脑补，显式标注**：推导的、缺的、被阻塞的 |
| ⑨ | 安全威胁检查 | STRIDE-lite 六类逐条 |
| ⑩ | 责任角色 | Owner / Implementer / Reviewer / Approver |
| ⑪ | 并行调度 | 谁能同时开工；**审核产能是真实上限** |
| ⑫ | 验收交付清单 | 通过时必须交出什么 |

### 8.3 五条不许犯的

1. **不许用自己造的东西替换标准里的块。** 我拿一张自己设计的检查清单放进了 C 区，
   把 12 张卡挤掉了。检查清单可以有，但它是**额外**的，不能顶替标准位置。
2. **不许把内容藏进 tooltip / 折叠。** 违反「任何功能都不许藏在点击/悬停后面」。
3. **抽不到内容时不许留空，要显式标"缺"并写原因。**
   `_extract_loc_surfaces.py` 抽出 19/20，第 20 个源文档里根本没有——
   具名 + 写理由 + 页面上显示成缺。**留空 = 又丢一次内容，而且看不出来丢了。**
4. **不许出现只有表头的空表**（假绿 F6）。该列几条就列几条，0 条当故障。
5. **不许出现点开 404 的链接**（假绿 F7）。产出后逐条验可达。

### 8.4 关于「内嵌副本」

模块页面要能**双击打开**（`file://`）。那种情况下 `fetch`、XHR、`<iframe src>`
全部被浏览器拦死，**只有 `<script src>` 和 `srcdoc` 还能用**。所以内容被内联进架构图。

**但要说清楚**：拷一份就会过期，这个问题不会因为放进同一个文件就消失。
真正防过期的只有「每次自动重新生成 + 有检查拦住」：

- 生成端：`_build_flow_embeds.py` **扫目录自动发现**，不再手工登记
- 检查端：C1 **从警告升为 FAIL**（出事那次它报了却仍打印「✅ 硬性检查通过」——发现了没拦住）
- 自包含：C13 要求架构图 **0 个外部脚本依赖**，且内联段里 `</script>` 全部转义

### 8.5 后面 200 个包一律照此

新模块进来时，A~E 六个位置**逐个对**，缺一个就是没就绪。
**位置是固定的，内容由各自源文档填**——这就是"标准"和"随心所欲"的区别。

---

## 9｜交付时必须摆出来的证据

§0.5.1 第 1 条要求「必须给我看全是通过」。**摆的方式定死如下，不许换别的说法。**

```bash
python docs/_build_all.py && python docs/_check_ready_to_build.py && python docs/_negtest_devphase.py && python docs/_check_standard_doc.py && python docs/_negtest_standard_doc.py && python docs/_build_public_export.py
```

六条必须全部退出码 0，并把这六行摘要贴出来：

| 跑什么 | 期望看到 |
|---|---|
| `_build_all.py` | `✅ 全链跑通：生成 N 步（M 个模块）+ 一致性自检全部通过。` |
| `_check_ready_to_build.py` | `✅ N/N 全部满足` + ⏭ 行 + ℹ️ 不计入分母的行 |
| `_negtest_devphase.py` | `✅ N/N 条全部达到预期：该绿的绿，该红的每一条都红了` |
| `_check_standard_doc.py` | `✅ 标准文档自检通过` —— S1~S6 全绿 |
| `_negtest_standard_doc.py` | `✅ N/N 条全部达到预期` —— 标准自检自己也红过 |
| `_build_public_export.py` | `取件页链接自检：N 页全部可下载，无 404` + 硬底线审计 6 项全无 |

**只贴结论不贴数字 = 没摆。** 数字必须是这次真跑出来的，
不许从上一次的记录里抄（假绿 F5 的同类）。

---

## 10｜这份标准自己怎么不烂

### 10.1 五条

1. **每一条都必须指向一个真实存在的检查程序。** 指向不存在的 = ⚪，必须如实标。
2. **⚪ 不得计入满足。** 任何汇总数字必须把 ⚪ 单列。
3. **判据变了要同步。** 生成器改了措辞而检查器没跟上，就是「10 轮 180 条」死掉的方式。
   → 检查器抠不到值时必须**当场停并点名哪条正则过期**，绝不许退化成默认值继续跑。
4. **新增标准必须附反向验证，且要在非 DQ 的模块上验。**
   **没红过的检查不算数。**
5. **每撞出一种新的假绿方式，必须往 §2 加一行**，并注明是哪次撞出来的。

### 10.2 标准文档本身也被机器判

V1.0 出现 P29~P32 四个从未定义的编号（假绿 F13），说明**光靠人读是读不出来的**。
所以 V2.0 加一道自检：

- 本文表格里出现的每个编号（P/R/M/D/C），必须能在真实检查器里找到；
- 本文引用的每个编号，必须在本文里被定义成表格行；
- 标 ✅ 的条目，必须在 `_negtest_devphase.py` 或对应反向测试里有对应用例；
- 标 ⚪ 的条目，必须同时出现在 §0.5.2 的 ⚪ 清单里。

**拦在哪**：`_check_standard_doc.py`，任一条不满足即非零退出。

---

## 11｜与现有文件的对应

| 本标准 | 落在哪 |
|---|---|
| 开发前 P1~P21 | `_check_standard.py` |
| 开发前 R1~R8 / R10 / R11 | `_check_ready_to_build.py` |
| 开发中 M0~M4（T0~T3）| `_check_test_integrity.py` |
| 开发中 M1（TestID 在方法名）| `_check_docs_consistency.py` C10 |
| 开发中 M5 | `_check_scope.py` |
| 开发中 M6 / M7 | **未建（⚪）** |
| 开发后 D1 / D4 | `_check_delivery.py` |
| 开发后 D3 | `_check_test_integrity.py`（T0+T1）|
| 开发后 D2 | **未建（⚪）** |
| 开发后 D5 | 由 DQ 自身的三 Gate 提供 |
| **上面每一条的反向验证** | `_negtest_devphase.py`（逐模块） |
| 页面块顺序 + 总账份数（§8.1 A/B2）| `_check_docs_consistency.py` C12 |
| 流程图存在 + 内嵌不过期（§8.1 D）| C1 + C4 |
| 架构图自包含 | C13 |
| 每个公开模块都有架构图入口 | C14 |
| 全链接线（登记了就必须接进来）| `_build_all.py` 的 `module_steps()` guard |
| 哨兵模块归属表 | `_build_sentinel_ownership.py` |
| 公开站按模块隔离 / 不公开模块不上站 | `_build_public_export.py` 的 `is_public()` |
| 取件页每条链接都能下载 | `_build_public_export.py` 取件页链接自检 |
| 模块登记表（唯一事实源） | `docs/modules.json` |
| 工作包自带工作区/基线/测试报告/代码地图 | `_check_docs_consistency.py` C10（4 条必备项）|
| 包登记与代码地图状态闭环 | `_build_code_map.py` → `code_map.json` |
| 上面那四段的唯一实现 | `_wp_workspace.py`（三个工作包生成器共用一份）|
| C10 每条必备项的反向验证 | `_negtest_wp_must_have.py`（23 条）|
| **本标准文档自身** | `_check_standard_doc.py`（S1~S9）|
| 上一行那道自检自己的反向验证 | `_negtest_standard_doc.py`（9 条）|

---

## 12｜从 V1.0 改了什么

| 改动 | 为什么 |
|---|---|
| **新增 §2 假绿名录（14 类）** | V1.0 只判材料，不判"判材料的程序可不可信"。这 14 类全部真实发生过 |
| **新增 §3 三条结构规则** | 11 处事故收敛到三条根因：不是单一事实源 / 只在 DQ 上验过 / 哨兵缺席 |
| **每条新增两个必填字段**（反向验过没有、验过哪几个模块） | V1.0 §6 写了"必须附负向验证"，然后这条自己没被执行 |
| **取消 P/R 并行编号，标准编号 = 检查器编号** | V1.0 的 P29~P32 被引用却从未定义，也无程序实现 |
| **P22~P28 并入 R 系列** | 它们本来就是由 R 系列判的，两套编号只会分叉 |
| **原 R9 撤出闸门** | 只对 DQ 生效的静默跳过（F1）＋ 自评不算验收（§0.5） |
| **§8 的 B/C/E 三块如实标 ⚪** | V1.0 写成 P29~P32 四条，看着像有人管，实际没有 |
| **新增 §9 交付证据格式** | 「必须给我看全是通过」以前没定怎么摆，就会变成只贴结论 |
| **新增 §10.2 标准文档自检** | 光靠人读读不出 F13 那种错 |
| 开发前条数 29 → **31** | 按检查器实际跑的条数据实写，不是按文档里数出来的 |

---

**出处**：本标准全部条目抽自 DQ 已完成的样板、2026-09-09 的立标实测，
以及 **2026-09-10 假装成 opencode 拿 LOC-F001 走真实流程撞出的 11 处**。
凡标「事故」的，是撞出来的，不是设计出来的 —— **那正是立这份标准的理由。**
